# 🔑 Настройка GITHUB_TOKEN для мониторинга workflows

## Зачем нужен GITHUB_TOKEN?

GitHub API имеет ограничения на количество запросов:

- **Без токена**: 60 запросов/час (очень мало)
- **С токеном**: 5000 запросов/час (достаточно для мониторинга)

## Как создать GitHub Personal Access Token

### Шаг 1: Перейдите в настройки GitHub

1. Откройте [GitHub.com](https://github.com)
2. Нажмите на ваш аватар (правый верхний угол)
3. Выберите **Settings** (Настройки)

### Шаг 2: Создайте Personal Access Token

1. В левом меню выберите **Developer settings**
2. Нажмите **Personal access tokens**
3. Выберите **Tokens (classic)**
4. Нажмите **Generate new token** → **Generate new token (classic)**

### Шаг 3: Настройте права доступа

**Название токена**: `DMarket Bot Monitoring`

**Срок действия**:

- Рекомендуется: `90 days` (обновлять раз в 3 месяца)
- Или: `No expiration` (для постоянного использования)

**Выберите права** (scopes):

- ✅ **`repo`** (полный доступ к репозиториям)
  - Включает: `repo:status`, `repo_deployment`, `public_repo`
- ✅ **`workflow`** (доступ к GitHub Actions)

**Другие права НЕ нужны** (оставьте пустыми для безопасности)

### Шаг 4: Сгенерируйте токен

1. Прокрутите вниз и нажмите **Generate token**
2. **ВАЖНО**: Скопируйте токен СРАЗУ (он больше не будет показан!)
3. Токен выглядит так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

## Настройка в проекте

### Вариант 1: Локальное использование (разработка)

1. Откройте файл `.env` в корне проекта
2. Найдите строку `GITHUB_TOKEN=`
3. Вставьте ваш токен:

   ```env
   GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

4. Сохраните файл

### Вариант 2: GitHub Actions (CI/CD)

GitHub автоматически предоставляет токен в workflows:

```yaml
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

**Ничего дополнительно настраивать не нужно!**

### Вариант 3: Переменная окружения (временно)

**Windows PowerShell**:

```powershell
$env:GITHUB_TOKEN = "ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
python scripts/github_actions_monitor.py
```

**Linux/macOS**:

```bash
export GITHUB_TOKEN="ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
python scripts/github_actions_monitor.py
```

## Проверка настройки

После настройки токена запустите мониторинг:

```bash
python scripts/github_actions_monitor.py
```

**Ожидаемый результат**:

```
🚀 Запуск GitHub Actions Monitor...

📊 Анализ GitHub Actions Workflows

┏━━━━━━━━━━━━━━━━━━━━┳━━━━━━━┳━━━━━━━━━━┳━━━━━━━━┳━━━━━━━━━━━━━━┳━━━━━━━━┓
┃ Workflow           ┃ Total ┃ ✅ Success┃ ❌ Failed┃ Success Rate ┃ Status ┃
┡━━━━━━━━━━━━━━━━━━━━╇━━━━━━━╇━━━━━━━━━━╇━━━━━━━━╇━━━━━━━━━━━━━━╇━━━━━━━━┩
│ Python Tests       │    15 │       14 │      1 │ 93.3%        │   ✅   │
│ Code Quality       │    12 │       12 │      0 │ 100.0%       │   ✅   │
└────────────────────┴───────┴──────────┴────────┴──────────────┴────────┘

📊 Общая статистика:
  • Всего запусков: 27
  • Успешных: 26
  • Overall Success Rate: 96.3%
  • Среднее время: 4.2 мин

✅ Цель достигнута! Success rate >= 80%
```

## Безопасность токена

### ⚠️ ВАЖНЫЕ правила безопасности

1. **НИКОГДА не коммитьте токен в Git**
   - Файл `.env` добавлен в `.gitignore`
   - Проверьте перед коммитом: `git status`

2. **Не делитесь токеном**
   - Это ваш личный ключ доступа
   - Не отправляйте токен в чаты, email, screenshots

3. **Регулярно обновляйте**
   - Создавайте новый токен каждые 3-6 месяцев
   - Удаляйте старые токены на GitHub

4. **Ограничьте права**
   - Выдавайте только необходимые scopes
   - Для мониторинга достаточно `repo` + `workflow`

### Что делать если токен скомпрометирован?

1. **Немедленно отзовите токен**:
   - GitHub → Settings → Developer settings → Personal access tokens
   - Найдите токен → Delete

2. **Создайте новый токен** (см. инструкцию выше)

3. **Обновите `.env`** с новым токеном

4. **Проверьте GitHub Activity**:
   - Settings → Security log
   - Убедитесь в отсутствии подозрительной активности

## Альтернатива: GitHub CLI

Если у вас установлен [GitHub CLI](https://cli.github.com/), можно использовать его для аутентификации:

```bash
# Установка (Windows)
winget install GitHub.cli

# Аутентификация
gh auth login

# Использование в скрипте
gh api /repos/Dykij/DMarket-Telegram-Bot/actions/runs
```

## Troubleshooting

### Проблема: "403 rate limit exceeded"

**Решение**:

- Проверьте, что `.env` содержит корректный `GITHUB_TOKEN`
- Убедитесь, что токен не истек (проверьте на GitHub)
- Создайте новый токен, если старый удален

### Проблема: "401 Unauthorized"

**Решение**:

- Токен неверный или истек
- Проверьте копирование (все символы, без пробелов)
- Создайте новый токен

### Проблема: "404 Not Found"

**Решение**:

- Проверьте `REPO_OWNER` и `REPO_NAME` в скрипте
- Убедитесь, что токен имеет доступ к репозиторию
- Для приватных репозиториев нужен scope `repo`

### Проблема: Токен работает, но медленно

**Решение**:

- Это нормально для первого запроса
- Последующие запросы будут быстрее (кэширование)
- Проверьте интернет-соединение

## Дополнительные ресурсы

- [GitHub Personal Access Tokens Documentation](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)
- [GitHub API Rate Limits](https://docs.github.com/en/rest/overview/resources-in-the-rest-api#rate-limiting)
- [GitHub Actions Monitoring Best Practices](https://docs.github.com/en/actions/monitoring-and-troubleshooting-workflows)

---

**Версия**: 1.0
**Дата**: 13 ноября 2025 г.
**Проект**: DMarket Telegram Bot
