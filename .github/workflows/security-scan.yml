name: Security Analysis

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  schedule:
    - cron: '0 0 * * 0'  # Run weekly on Sunday at 00:00 UTC
  workflow_dispatch:  # Allow manual triggering

# Управление параллелизмом для предотвращения одновременных запусков
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Ограничение прав доступа GITHUB_TOKEN
permissions:
  contents: read
  security-events: write # Нужно для загрузки результатов анализа CodeQL

env:
  PYTHON_VERSION: '3.11'

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high

  analyze:
    name: CodeQL Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: python

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:python"

  security-check:
    name: Security Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install security tools
        run: |
          echo "Установка инструментов для анализа безопасности..."
          python -m pip install --upgrade pip
          pip install safety bandit pip-audit || {
            echo "::warning::Не удалось установить все инструменты безопасности"
            pip install safety || echo "::warning::Safety не установлен"
            pip install bandit || echo "::warning::Bandit не установлен"
            pip install pip-audit || echo "::warning::pip-audit не установлен"
          }
          echo "✅ Инструменты для анализа безопасности установлены"

      - name: Check for dependency files
        id: check-deps
        run: |
          echo "Проверка наличия файлов зависимостей..."
          if [ -f "requirements.txt" ]; then
            echo "has_requirements=true" >> $GITHUB_OUTPUT
            echo "✅ Найден файл requirements.txt"
          else
            echo "has_requirements=false" >> $GITHUB_OUTPUT
            echo "⚠️ Файл requirements.txt не найден"
          fi

      - name: Run Safety check
        if: steps.check-deps.outputs.has_requirements == 'true'
        run: |
          echo "Проверка зависимостей на уязвимости с помощью Safety..."
          mkdir -p security-reports

          # Safety check с обработкой ошибок и игнорированием непиннированных зависимостей
          if command -v safety &> /dev/null; then
            # Используем --ignore-unpinned-requirements для совместимости
            safety check --file requirements.txt --output json --save-json security-reports/safety.json 2>&1 || echo "::warning::Safety обнаружил уязвимости"
            safety check --file requirements.txt --output text > security-reports/safety.txt 2>&1 || echo "::warning::Safety завершился с предупреждениями"
          else
            echo "::warning::Safety не установлен, пропускаем проверку"
            echo '{"vulnerabilities": []}' > security-reports/safety.json
          fi
        continue-on-error: true

      - name: Run Bandit code scan
        run: |
          echo "Сканирование кода на уязвимости с помощью Bandit..."
          mkdir -p security-reports

          if [ ! -d "./src" ]; then
            echo "::warning::Директория ./src не найдена, пропускаем Bandit scan"
            exit 0
          fi

          if command -v bandit &> /dev/null; then
            bandit -r ./src -f json -o security-reports/bandit.json 2>&1 || echo "::warning::Bandit обнаружил потенциальные уязвимости"
            bandit -r ./src -f txt -o security-reports/bandit.txt 2>&1 || true
          else
            echo "::error::Bandit не установлен"
            exit 1
          fi
        continue-on-error: true

      - name: Generate security summary
        if: always()
        run: |
          echo "# 🔒 Результаты анализа безопасности" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Проверка зависимостей" >> $GITHUB_STEP_SUMMARY
          if [ -f security-reports/safety.json ]; then
            echo "- **Safety**: Проверка выполнена" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Safety**: ⚠️ Пропущена" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Сканирование кода" >> $GITHUB_STEP_SUMMARY
          if [ -f security-reports/bandit.json ]; then
            echo "- **Bandit**: Сканирование выполнено" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Bandit**: ⚠️ Пропущено" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload security results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-results
          path: security-reports/
          retention-days: 7
        continue-on-error: true
