# 🔄 GitHub Actions Workflows

**Дата обновления**: 13 ноября 2025 г.

Эта директория содержит GitHub Actions workflows для автоматизации CI/CD процессов проекта.

---

## 📊 Активные Workflows

### 🧪 [python-tests.yml](python-tests.yml) - Тестирование Python

**Триггеры:**
- Push в `main`, `master`, `develop`
- Pull requests
- Ежедневно в 06:00 UTC (schedule)
- Ручной запуск

**Возможности:**
- ✅ Тестирование на Python 3.11 и 3.12
- ✅ Измерение покрытия кода (минимум 80%, цель 85%)
- ✅ Генерация HTML отчетов о покрытии
- ✅ Загрузка результатов в Codecov
- ✅ JUnit XML отчеты для интеграции с CI/CD
- ✅ Автоматическая проверка порога покрытия
- ✅ Детальные summary в PR

**Цель покрытия**: 80%+ (стремимся к 85%)

---

### 🔍 [code-quality.yml](code-quality.yml) - Качество кода

**Триггеры:**
- Push в `main`, `master`, `develop`
- Pull requests
- Ручной запуск

**Проверки:**
- **Ruff Format** - проверка форматирования
- **Ruff Lint** - статический анализ кода
- **MyPy** - проверка типов
- **Safety** - проверка уязвимостей в зависимостях
- **Bandit** - сканирование безопасности кода

**Артефакты:**
- `code-quality-reports/` - отчеты о качестве кода
- `security-reports/` - отчеты о безопасности

---

### 🚀 [ci.yml](ci.yml) - Упрощенный CI

**Триггеры:**
- Push в `main`, `master`, `develop`
- Pull requests
- Ручной запуск

**Jobs:**
1. **lint-and-format** - проверка стиля кода
2. **test** - запуск тестов с покрытием
3. **security-basic** - базовое сканирование безопасности
4. **summary** - генерация общего отчета

**Особенности:**
- ✅ Устойчив к ошибкам (continue-on-error)
- ✅ Быстрый и надежный
- ✅ Подходит для быстрой проверки PR

---

### ⚡ [quick-tests.yml](quick-tests.yml) - Быстрые тесты

**Триггеры:**
- Push в ветки с изменениями `src/**/*.py` или `tests/**/*.py`
- Pull requests
- Ручной запуск

**Возможности:**
- ✅ Быстрая проверка только измененных модулей
- ✅ Поддержка simplified test suites
- ✅ Fallback на базовые тесты при отсутствии специальных

---

### 🔒 [security-scan.yml](security-scan.yml) - Сканирование безопасности

**Триггеры:**
- Push в `main`, `master`, `develop`
- Pull requests
- Еженедельно по воскресеньям в 00:00 UTC
- Ручной запуск

**Проверки:**
1. **Dependency Review** - анализ зависимостей в PR
2. **CodeQL Analysis** - статический анализ безопасности
3. **Security Check** - проверка уязвимостей

---

### 📝 [pre-commit.yml](pre-commit.yml) - Pre-commit хуки

**Триггеры:**
- Push в `main`, `master`, `develop`
- Pull requests
- Ручной запуск

**Возможности:**
- ✅ Автоматическое применение pre-commit хуков
- ✅ Проверка trailing whitespace, YAML, больших файлов
- ✅ Форматирование с Black и Ruff
- ✅ Сортировка импортов с isort

---

## 🎯 Рекомендуемый workflow для разработки

### 1. Локальная разработка

```bash
# Установить pre-commit хуки
pre-commit install

# Запустить проверки локально
ruff check src tests
ruff format src tests
mypy src
pytest --cov=src --cov-report=html
```

### 2. Перед push

```bash
# Проверить все изменения
pre-commit run --all-files

# Убедиться, что тесты проходят
pytest --cov=src --cov-report=term-missing

# Проверить покрытие (должно быть >= 80%)
coverage report --fail-under=80
```

### 3. После создания PR

- ✅ `quick-tests.yml` проверит измененные файлы
- ✅ `python-tests.yml` запустит полный набор тестов
- ✅ `code-quality.yml` проверит качество кода
- ✅ `security-scan.yml` просканирует на уязвимости

---

## 📈 Метрики качества

### Текущие цели

| Метрика                    | Минимум | Цель          | Статус       |
| -------------------------- | ------- | ------------- | ------------ |
| **Покрытие тестами**       | 80%     | 85%+          | 🎯 В процессе |
| **Success rate workflows** | 80%     | 95%+          | 🎯 Мониторинг |
| **MyPy строгость**         | Базовая | Строгий режим | 🎯 В процессе |
| **Ruff ошибки**            | 0       | 0             | ✅ Достигнуто |

### Мониторинг workflows

Используйте скрипт для отслеживания success rate:

```bash
python scripts/github_actions_monitor.py
```

**Цель**: Поддерживать success rate ≥ 80% для всех workflows.

---

## 🔧 Композитные Actions

Проект использует переиспользуемые composite actions:

### 📦 [setup-python-env](../actions/setup-python-env)

Настраивает Python окружение с автоопределением менеджера зависимостей.

**Использование:**

```yaml
- uses: ./.github/actions/setup-python-env
  with:
    python-version: '3.11'
    install-poetry: 'true'
    install-dev-deps: 'true'
    create-env-file: 'true'
```

### 🔒 [security-check](../actions/security-check)

Выполняет комплексное сканирование безопасности.

**Использование:**

```yaml
- uses: ./.github/actions/security-check
  with:
    python-version: '3.11'
    scan-dependencies: 'true'
    scan-code: 'true'
    fail-on-high: 'false'
```

---

## 🚀 Локальное тестирование workflows

### Установка act

```bash
# Windows (Chocolatey)
choco install act-cli

# Linux
curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash

# macOS
brew install act
```

### Запуск workflows локально

```bash
# Запустить все workflows
act

# Запустить конкретный workflow
act -W .github/workflows/python-tests.yml

# Запустить конкретную job
act -j test

# С переменными окружения
act -j test -s GITHUB_TOKEN=your_token
```

---

## 🔑 Необходимые Secrets

Для полной функциональности workflows настройте следующие secrets в GitHub:

### Обязательные

| Secret         | Описание                             | Используется в |
| -------------- | ------------------------------------ | -------------- |
| `GITHUB_TOKEN` | Автоматически предоставляется GitHub | Все workflows  |

### Опциональные (для production)

| Secret          | Описание                              | Используется в   |
| --------------- | ------------------------------------- | ---------------- |
| `CODECOV_TOKEN` | Токен для загрузки покрытия в Codecov | python-tests.yml |

**Примечание**: Тестовые значения по умолчанию используются, если secrets не настроены.

---

## 📊 Отчеты и артефакты

### python-tests.yml

- `test-results-py3.11/` - результаты тестов для Python 3.11
- `test-results-py3.12/` - результаты тестов для Python 3.12
- Содержат:
  - `junit.xml` - JUnit отчет
  - `coverage.xml` - XML отчет покрытия
  - `html/` - HTML визуализация покрытия

### code-quality.yml

- `code-quality-reports/` - отчеты Ruff и MyPy
- `security-reports/` - отчеты Safety и Bandit

### Просмотр артефактов

1. Перейдите на страницу workflow run в GitHub
2. Прокрутите вниз до секции "Artifacts"
3. Скачайте нужный артефакт
4. HTML отчеты можно открыть локально в браузере

---

## 🐛 Устранение неполадок

### Workflow завершается с ошибкой "POETRY_AVAILABLE not found"

**Решение**: Это предупреждение можно игнорировать. Workflow использует fallback на pip.

### Низкое покрытие тестами

```bash
# Локально проверить покрытие
pytest --cov=src --cov-report=html

# Открыть отчет
open htmlcov/index.html  # macOS
start htmlcov/index.html  # Windows
```

### Rate limit от GitHub API

**Решение**: `GITHUB_TOKEN` автоматически предоставляется и используется во всех workflows.

---

## 🔄 Обновление workflows

### Добавление нового workflow

1. Создайте файл в `.github/workflows/`
2. Используйте существующие workflows как шаблон
3. Следуйте best practices:
   - Используйте `concurrency` для отмены устаревших runs
   - Ограничивайте `permissions` до необходимого минимума
   - Используйте `continue-on-error` для некритичных шагов
   - Добавляйте `timeout-minutes` для предотвращения зависаний

### Использование composite actions

Вместо дублирования кода используйте композитные actions:

```yaml
# ❌ Плохо - дублирование кода
- uses: actions/setup-python@v5
  with:
    python-version: '3.11'
- run: pip install -r requirements.txt

# ✅ Хорошо - переиспользование
- uses: ./.github/actions/setup-python-env
  with:
    python-version: '3.11'
```

---

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Workflow syntax](https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions)
- [Events that trigger workflows](https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows)
- [Context and expression syntax](https://docs.github.com/en/actions/learn-github-actions/contexts)
- [Security hardening](https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions)

---

## 🤝 Вклад в workflows

При внесении изменений в workflows:

1. ✅ Тестируйте локально с `act` (если возможно)
2. ✅ Документируйте изменения в этом README
3. ✅ Следуйте принципам безопасности
4. ✅ Используйте версионирование actions (`@v4`, не `@main`)
5. ✅ Добавляйте информативные комментарии

---

**Поддержка**: Если у вас возникли вопросы по workflows, создайте Issue с меткой `ci/cd`.
