# Copilot Agent Setup (callable workflow)
name: Copilot Setup

on:
  workflow_call:

jobs:
  setup:
    name: "🤖 Setup"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: |
          pip install -q -r requirements.txt 2>/dev/null || true
          pip install -q ruff mypy pytest pytest-asyncio 2>/dev/null
      - name: Verify
        run: |
          python -c "import asyncio; import httpx; print('✓ Ready')"
