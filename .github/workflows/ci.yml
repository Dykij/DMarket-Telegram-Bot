# Legacy CI - redirects to main.yml
# Kept for backwards compatibility
name: CI (Legacy)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use main.yml instead."
