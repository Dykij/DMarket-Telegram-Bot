# ISPRAVIT - Список проблем и рекомендаций по исправлению

## 📊 Общая статистика отладки

| Метрика | Значение |
|---------|----------|
| **Unit тесты** | 574 passed, 5 skipped |
| **DMarket тесты** | 2239 passed, 5 failed, 10 skipped |
| **Telegram Bot тесты** | 2519 passed, 60 failed, 11 skipped |
| **Integration тесты** | 195 passed, 27 failed, 35 errors |
| **Smoke тесты** | 19 passed ✅ |
| **Ruff ошибки** | 655 |
| **MyPy ошибки** | 59 |
| **Coverage (unit)** | ~12% (низкий, но unit тесты работают) |

---

## 🎯 ПРИОРИТЕТЫ ВЫПОЛНЕНИЯ

| # | Приоритет | Задача | Время | Влияние | Статус |
|---|-----------|--------|-------|---------|--------|
| 1 | 🔴 P0 | Добавить недостающие зависимости | 5 мин | Критично для запуска | ✅ Выполнено |
| 2 | 🔴 P0 | Исправить неиспользуемые await | 15 мин | Утечка ресурсов | ✅ Выполнено |
| 3 | 🟠 P1 | Автофикс whitespace (ruff --fix) | 5 мин | 449 ошибок | ✅ Выполнено (372 fixed) |
| 4 | 🟠 P1 | Автофикс импортов (ruff --fix) | 5 мин | 23 ошибки | ✅ Выполнено |
| 5 | 🟠 P1 | Reset Circuit Breaker в тестах | 10 мин | Изоляция тестов | ✅ Выполнено |
| 6 | 🟡 P2 | Обновить устаревший API в тестах | 30 мин | 5 тестов | ⏳ Ожидает |
| 7 | 🟡 P2 | logging.error → logging.exception | 20 мин | Улучшение отладки | ✅ Выполнено |
| 8 | 🟢 P3 | Рефакторинг вложенности | 2 часа | Code quality | ⏳ Ожидает (требует тщательного тестирования) |
| 9 | 🟢 P3 | str+Enum → StrEnum | 30 мин | Современный синтаксис | ✅ Выполнено (13 файлов) |
| 10 | 🟢 P3 | datetime с timezone | 30 мин | Корректность времени | ✅ Выполнено (3 файла) |

**Легенда**: 🔴 P0 = Критично | 🟠 P1 = Важно | 🟡 P2 = Средний | 🟢 P3 = Низкий

**Прогресс**: 8/10 задач выполнено ✅

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (P0 - исправить срочно)

### 1. Отсутствующие зависимости в requirements.txt [P0]

**Проблема**: Некоторые модули не установлены, что вызывает ошибки импорта.

**Файлы**: `requirements.txt`, `pyproject.toml`

**Решение**:
```bash
# Добавить в requirements.txt или requirements.in:
apscheduler>=3.10.0  # Для DailyReportScheduler
aiolimiter>=1.1.0    # Уже есть в pyproject.toml, но может отсутствовать в venv
```

**Примечание**: `fastapi` и `uvicorn` уже присутствуют в requirements.txt (uvicorn==0.40.0).

**Безопасность**: Не влияет на логику бота, только добавляет недостающие зависимости.

---

### 2. Неиспользуемые await (unused-coroutine) [P0]

**Проблема**: MyPy обнаружил неиспользуемые корутины - потенциальная утечка ресурсов.

**Файлы**:
- `src/main.py:569` - `health_check_server.start()` без await (async метод в HealthCheckServer)
- `src/main.py:888` - `health_check_server.stop()` без await (async метод в HealthCheckServer)
- `src/utils/shutdown_handler.py:53`
- `src/utils/extended_shutdown_handler.py:276`
- `src/dmarket/arbitrage_scanner.py:506`
- `src/telegram_bot/handlers/callback_registry.py:768`

**Подтверждение**: В `src/telegram_bot/health_check.py`:
```python
class HealthCheckServer:
    async def start(self) -> None:  # Line 77
        ...
    async def stop(self) -> None:   # Line 108
        ...
```

**Решение**:
```python
# До:
health_check_server.start()

# После:
await health_check_server.start()
```

**Безопасность**: Исправление улучшит стабильность, не изменяя логику.

---

### 3. Проблемы с Circuit Breaker в тестах [P1]

**Проблема**: Circuit Breaker остается в состоянии OPEN между тестами.

**Файлы**: `tests/integration/test_circuit_breaker_integration.py`

**Решение**: Добавить reset Circuit Breaker в фикстуры тестов:
```python
@pytest.fixture(autouse=True)
def reset_circuit_breakers():
    """Reset all circuit breakers before each test."""
    from src.utils.api_circuit_breaker import reset_all_circuit_breakers
    reset_all_circuit_breakers()
    yield
    reset_all_circuit_breakers()
```

**Примечание**: Функция `reset_all_circuit_breakers()` существует в `src/utils/api_circuit_breaker.py:312`.

**Безопасность**: Только тестовая изоляция, не влияет на production код.

---

## 🟠 ВАЖНЫЕ ПРОБЛЕМЫ (P1-P2 - рекомендуется исправить)

### 4. Устаревший API в тестах [P2]

**Проблема**: Тесты используют устаревшие методы API.

**Файлы**:
- `tests/integration/test_dmarket_vcr.py`

**Ошибки**:
- `DMarketAPI.get_market_items()` получает `game_id` вместо `game` (актуальная сигнатура: `game: str = "csgo"`)
- `create_target` переименован в `create_targets`
- `cancel_target` отсутствует

**Решение**: Обновить тесты в соответствии с актуальным API:
```python
# До (устаревший параметр в тестах):
await api.get_market_items(game_id="csgo")

# После (актуальная сигнатура из src/dmarket/dmarket_api.py:1484):
await api.get_market_items(game="csgo")
```

**Безопасность**: Только исправление тестов, не влияет на логику бота.

---

### 5. Whitespace проблемы (W291, W293) [P1]

**Проблема**: 449 ошибок trailing whitespace и blank lines с пробелами.

**Главный файл**: `src/dmarket/lock_status_filter.py` (80+ ошибок)

**Решение**:
```bash
# Автоматическое исправление
ruff check src/ --fix
# или
ruff format src/
```

**Безопасность**: Форматирование не влияет на логику.

---

### 6. Unnecessary `elif` after `return` (RET505) [P3]

**Проблема**: 24 случая неоптимального кода с elif после return.

**Файлы**:
- `src/dmarket/endpoints.py:272`
- `src/dmarket/multi_platform_aggregator.py:313`
- `src/dmarket/opportunity_scorer.py` (5 случаев)
- `src/ml/trade_classifier.py:325`

**Решение**:
```python
# До:
if condition:
    return value1
elif other_condition:
    return value2

# После:
if condition:
    return value1
if other_condition:
    return value2
```

**Безопасность**: Рефакторинг не изменяет логику.

---

### 7. Использование `logging.error` вместо `logging.exception` (TRY400) [P2]

**Проблема**: 19 случаев потери stack trace при логировании ошибок.

**Файлы**:
- `src/dmarket/adaptive_polling.py` (4 случая)
- `src/dmarket/long_polling.py` (4 случая)
- `src/dmarket/multi_platform_aggregator.py` (3 случая)
- `src/ml/enhanced_predictor.py` (3 случая)
- `src/ml/price_predictor.py` (3 случая)

**Решение**:
```python
# До:
except Exception as e:
    logger.error(f"Error: {e}")

# После:
except Exception as e:
    logger.exception(f"Error: {e}")  # Включает stack trace
```

**Безопасность**: Улучшает отладку, не изменяет логику.

---

### 8. str+Enum наследование (UP042) [P3]

**Проблема**: 17 enum классов наследуются от str и Enum одновременно.

**Файлы**:
- `src/waxpeer/waxpeer_api.py` (ListingStatus, WaxpeerGame)
- `src/ml/trade_classifier.py` (RiskLevel, TradeSignal)

**Решение**:
```python
# До:
class WaxpeerGame(str, Enum):
    CS2 = "cs2"

# После:
from enum import StrEnum

class WaxpeerGame(StrEnum):
    CS2 = "cs2"
```

**Безопасность**: Современный синтаксис, поведение идентичное.

---

### 9. Too many nested blocks (PLR1702) [P3]

**Проблема**: 15 функций с вложенностью > 5 уровней.

**Файлы**:
- `src/dmarket/intramarket_arbitrage.py:177, 319` (6 уровней)
- `src/dmarket/smart_arbitrage.py:493` (7 уровней)
- `src/telegram_bot/handlers/main_keyboard.py:866` (7 уровней)
- `src/telegram_bot/handlers/panic_handler.py:64`
- `src/telegram_bot/commands/logs_command.py:61`

**Решение**: Применить early returns и выделить подфункции.

**Безопасность**: Рефакторинг не изменяет логику, но требует тестирования.

---

### 10. datetime без timezone (DTZ001, DTZ003, DTZ007) [P3]

**Проблема**: 16 случаев использования naive datetime без timezone.

**Файлы**:
- `src/dmarket/intelligent_hold.py:208-209`
- `src/dmarket/market_sentiment.py:342-343`
- `src/ml/trade_classifier.py:257`

**Решение**:
```python
# До:
datetime.datetime.now()
datetime.datetime.strptime(date_str, "%Y-%m-%d")

# После:
datetime.datetime.now(tz=datetime.UTC)
datetime.datetime.strptime(date_str, "%Y-%m-%d").replace(tzinfo=datetime.UTC)
```

**Безопасность**: Улучшает корректность работы со временем.

---

### 11. Неотсортированные импорты (I001) [P1]

**Проблема**: 13 файлов с неотсортированными импортами.

**Файлы**:
- `src/dmarket/lock_status_filter.py:15`
- `src/utils/performance_monitor.py:9`
- `src/utils/extended_shutdown_handler.py:13`

**Решение**:
```bash
ruff check src/ --fix --select=I001
```

**Безопасность**: Не влияет на логику.

---

### 12. Unused imports (F401) [P1]

**Проблема**: 10 неиспользуемых импортов.

**Файлы**:
- `src/dmarket/long_polling.py:35` - `datetime.timedelta`

**Решение**: Удалить неиспользуемые импорты или добавить `# noqa: F401`.

**Безопасность**: Не влияет на логику.

---

## 🟢 MINOR ПРОБЛЕМЫ (P3 - при наличии времени)

### 13. Use set literal for membership testing (PLR6201) [P3]

**Проблема**: 19 случаев использования списка вместо set для in.

**Решение**:
```python
# До:
if value in ["a", "b", "c"]:

# После:
if value in {"a", "b", "c"}:
```

---

### 14. Unnecessary assignment before return (RET504) [P3]

**Проблема**: 6 случаев лишнего присваивания перед return.

---

### 15. Possible binding to all interfaces (S104) [P3]

**Проблема**: 4 случая биндинга на 0.0.0.0.

**Файлы**:
- `src/telegram_bot/webhook.py:36, 118`
- `src/telegram_bot/health_check.py:38, 358`

**Решение**: Использовать переменную окружения для HOST:
```python
host = os.getenv("HOST", "127.0.0.1")
```

---

### 16. Async functions using blocking methods (ASYNC230, ASYNC240) [P3]

**Проблема**: 4 случая использования блокирующих методов в async функциях.

**Файлы**:
- `src/utils/extended_shutdown_handler.py:123, 167` - `open()`
- `src/telegram_bot/command_center.py:457` - `open()`
- `src/telegram_bot/webhook.py:170` - `pathlib.Path`

**Решение**: Использовать `aiofiles` для async файловых операций.

---

## 🔵 ПРОВАЛИВШИЕСЯ ТЕСТЫ (требуют внимания)

### Integration тесты

| Тест | Проблема | Решение |
|------|----------|---------|
| `test_api_with_httpx_mock.py` | Моки не соответствуют реальным запросам | Обновить URL-ы моков |
| `test_arbitrage_scanner_integration.py` | Атрибут `arbitrage_mid` отсутствует | Добавить или удалить тест |
| `test_dmarket_integration.py` | `asyncio` не импортирован | Добавить import |
| `test_dmarket_vcr.py` | Устаревший API | Обновить вызовы |
| `test_targets_edge_cases.py` | Circuit Breaker OPEN | Добавить reset в фикстуры |

### Telegram Bot тесты

| Тест | Проблема | Решение |
|------|----------|---------|
| `test_notifier.py` (16 failed) | Логика notifier изменилась | Обновить тесты под новый API |
| `test_smart_notifier.py` | Deprecation warning не срабатывает | Проверить механизм warning |
| `test_telegram_bot_modules.py` | `sales_analysis_callbacks` отсутствует | Добавить модуль или удалить тест |

### DMarket тесты

| Тест | Проблема | Решение |
|------|----------|---------|
| `test_connection_pooling.py` | `keepalive_expiry` 60 вместо 30 | Обновить expected value |
| `test_dmarket_api_phase4_part1.py` | Cache тесты fail | Проверить TTL логику |
| `test_adaptive_polling.py` | Force poll не детектит изменения | Проверить логику polling |

---

## 📋 ПЛАН ДЕЙСТВИЙ

### Этап 1: Критические исправления (1-2 часа)
1. ✅ Добавить недостающие зависимости
2. 🔲 Исправить неиспользуемые await в main.py
3. 🔲 Добавить reset circuit breakers в тестовые фикстуры

### Этап 2: Автоматические исправления (30 минут)
```bash
# Исправить whitespace и форматирование
ruff check src/ --fix

# Отсортировать импорты
ruff check src/ --fix --select=I001
```

### Этап 3: Обновление тестов (2-3 часа)
1. 🔲 Обновить test_dmarket_vcr.py под новый API
2. 🔲 Исправить test_notifier.py под новую логику
3. 🔲 Добавить/удалить sales_analysis_callbacks

### Этап 4: Рефакторинг (4-6 часов)
1. 🔲 Уменьшить вложенность в критических файлах
2. 🔲 Заменить logging.error на logging.exception
3. 🔲 Использовать StrEnum вместо str+Enum

---

## ⚠️ ПРЕДУПРЕЖДЕНИЯ

1. **НЕ удалять** файлы без понимания их назначения
2. **НЕ изменять** API endpoints без обновления клиентов
3. **ВСЕГДА** запускать тесты после изменений
4. **Circuit Breaker** имеет state - при тестировании требуется reset

---

## 📚 Полезные команды

```bash
# Запуск только unit тестов (быстро)
pytest tests/unit/ --no-cov -q

# Проверка типов
mypy src/

# Линтинг с автоисправлением
ruff check src/ --fix

# Форматирование
ruff format src/

# Smoke тесты (критический путь)
pytest tests/smoke/ -v

# Проверка coverage
pytest tests/unit/ --cov=src --cov-report=html
```

---

**Дата создания**: 2026-01-07
**Автор**: GitHub Copilot
**Версия**: 1.0
