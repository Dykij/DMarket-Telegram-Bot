# 🗺️ ROADMAP: DMarket Telegram Bot

**Дата обновления**: 23 ноября 2025 г.
**Статус**: В активной разработке

---

## 🎯 Приоритетные задачи

### 🔴 P0 - КРИТИЧНО (Защита от потери денег)

> **⏱️ Общее время**: ~1.5 часа
> **Цель**: Защита от случайного запуска торговли на реальные деньги

#### DRY_RUN режим (⏱️ 30 мин) ✅ ВЫПОЛНЕНО

- [x] Добавить `dry_run: bool = True` в `Config` (по умолчанию True!)
- [x] Добавить проверку в DMarket API перед реальными ордерами
- [x] Логировать все операции с меткой `[DRY-RUN]` или `[LIVE]`
- [x] Добавить переменную окружения `DRY_RUN` в `.env.example`
- [x] Обновить документацию (QUICK_START.md, README.md)

#### Debug Suite скрипт (⏱️ 1 час) ✅ ВЫПОЛНЕНО

- [x] Создать `scripts/debug_suite.py` с 6 тестами:
  - Подключение к DMarket API + проверка баланса
  - Подключение к базе данных
  - Проверка/создание пользователя в БД
  - Тест получения цены и расчёта профита на реальных данных
  - Симуляция ордера в DRY-RUN режиме
  - Отправка тестового уведомления в Telegram
- [x] Добавить в README инструкцию: запускать перед каждым деплоем
- [x] Интегрировать в CI/CD как обязательный шаг
- [x] Все 6 тестов проходят успешно ✅

---

### 🟠 P1 - ВЫСОКИЙ ПРИОРИТЕТ (Мониторинг и защита)

> **⏱️ Общее время**: ~4 часа
> **Цель**: Полный контроль над действиями бота + защита от багов

#### Telegram уведомления на каждый ордер (⏱️ 1 час) ✅ ВЫПОЛНЕНО

- [x] Уведомление перед покупкой (INTENT):

  ```
  [DRY-RUN] 🔵 НАМЕРЕНИЕ КУПИТЬ
  AK-47 | Redline (FT)
  💵 Цена: 12.50$
  📊 Продать: 13.89$
  📈 Профит: +1.39$ (+11.1%)
  🎯 Источник: arbitrage_scanner
  ```

- [x] Уведомление после покупки (SUCCESS/FAIL)
- [x] Уведомление после продажи
- [x] Добавить кнопки "Отменить" для ручного контроля
- [x] Создать TradingNotifier wrapper класс
- [x] Реализовать callback обработчики для отмены
- [x] Документация: docs/TRADING_NOTIFICATIONS_GUIDE.md

#### INTENT логирование (⏱️ 30 мин) ✅ ВЫПОЛНЕНО

- [x] Логировать намерение перед каждой покупкой/продажей:

  ```python
  logger.info("BUY_INTENT", extra={
      "item": name, "price_usd": price, "profit_usd": profit,
      "profit_percent": profit_pct, "source": "arbitrage",
      "dry_run": settings.dry_run, "timestamp": utcnow()
  })
  ```

- [x] Добавить команду `/logs` для просмотра последних 20 INTENT логов

#### Проверка "здравого смысла" цен (⏱️ 45 мин) ✅ ВЫПОЛНЕНО

- [x] Добавить `MAX_PRICE_MULTIPLIER = 1.5` (макс 50% выше средней)
- [x] Перед покупкой сравнивать с историей цен за 7 дней
- [x] При превышении лимита:
  - Логировать `PRICE_SANITY_CHECK_FAILED` (CRITICAL)
  - Отправлять уведомление в Telegram с алертом 🚨
  - Блокировать покупку
  - Опционально: останавливать бота полностью
- [x] Создан модуль `src/utils/price_sanity_checker.py`
- [x] Добавлены константы в `Config.trading_safety`
- [x] Реализован метод `get_price_history` в `DatabaseManager`

#### Автоматическая остановка при проблемах (⏱️ 30 мин) ✅ ВЫПОЛНЕНО

- [x] Счётчик `consecutive_errors`
- [x] При 5+ ошибках подряд:

  ```python
  await notifier.send_message("🚨 БОТ ОСТАНОВЛЕН: 5 ошибок подряд")
  await shutdown_gracefully()
  ```

- [x] Добавить команду `/resume` для ручного возобновления

#### Crash notifications (⏱️ 45 мин) ✅ ВЫПОЛНЕНО

- [x] Интегрировать уведомления о крашах бота через Telegram (в `notifier.py`)
- [x] Автоматические алерты при критических ошибках
- [x] Создан полноценный набор тестов для crash notifications

---

### 🟡 P2 - СРЕДНИЙ ПРИОРИТЕТ (Оптимизация и UX)

> **⏱️ Общее время**: ~20 часов
> **Цель**: Улучшение производительности и пользовательского опыта

#### Интерактивные дашборды (⏱️ 3 часа) ✅ ВЫПОЛНЕНО

- [x] Реализовать меню с inline-кнопками для управления сканером
- [x] Добавить графики статистики (через `quickchart.io`)
- [x] Создан `dashboard_handler.py` с полным функционалом
- [x] Создан `chart_generator.py` для генерации графиков

#### Настройка фильтров уведомлений (⏱️ 2 часа) ✅ ВЫПОЛНЕНО

- [x] Гибкие фильтры уведомлений (по игре, профиту, уровню)
- [x] Группировка уведомлений (дайджесты)
- [x] Создан `notification_filters_handler.py`
- [x] Создан `notification_digest_handler.py`

#### Pydantic схемы для DMarket API (⏱️ 2 часа) ✅ ВЫПОЛНЕНО

- [x] Создать схемы для валидации всех ответов DMarket API
- [x] Валидировать все ответы через `.model_validate()`
- [x] Обрабатывать ValidationError → уведомление в Telegram об изменении API
- [x] Реализовано в рамках модуля `dmarket_api.py`

#### Ежедневные отчёты (⏱️ 45 мин)

- [ ] Автоматическая отправка сводки раз в день (через APScheduler):

  ```
  📊 Дневной отчёт (23.11.2025)
  💼 Сделок: 15 (12 успешных, 3 отменены)
  💰 Профит: +45.32$ (+12.3%)
  ⚠️ Ошибок API: 2 (rate_limit: 1, timeout: 1)
  🔴 Критических: 0
  ```

#### Rate limit защита и мониторинг (⏱️ 30 мин)

- [ ] Отслеживать 429 ошибки
- [ ] При превышении лимита: увеличивать delay экспоненциально
- [ ] Уведомлять в Telegram о приближении к лимиту (90% использования)

#### Pydantic схемы для DMarket API (⏱️ 2 часа)

- [ ] Создать `src/dmarket/schemas.py` с моделями:

  ```python
  class DMarketOffer(BaseModel):
      offer_id: str = Field(..., alias="offerId")
      price_usd: Decimal = Field(..., alias="price")
      item_name: str = Field(..., alias="title")

      class Config:
          populate_by_name = True
  ```

- [ ] Валидировать все ответы через `.model_validate()`
- [ ] Обрабатывать ValidationError → уведомление в Telegram об изменении API

#### Интерактивные дашборды (⏱️ 3 часа)

- [ ] Реализовать меню с inline-кнопками для управления сканером
- [ ] Добавить графики статистики (через `matplotlib` или `quickchart.io`)

#### Настройка фильтров уведомлений (⏱️ 2 часа)

- [ ] Гибкие фильтры уведомлений (по игре, профиту, уровню)
- [ ] Группировка уведомлений (дайджесты)

#### Caching оптимизация (⏱️ 4 часа)

- [ ] Добавить in-memory cache с TTL для частых запросов (цены, история)
- [ ] Реализовать query caching в `database.py` для популярных запросов
- [ ] Заменить `json` на `orjson` для ускорения сериализации

#### Database оптимизация (⏱️ 3 часа)

- [ ] Миграция на `aiosqlite` для асинхронных операций с SQLite
- [ ] Добавить индексы для частых запросов

#### Integration тесты (⏱️ 5 часов)

- [ ] Добавить integration тесты с `httpx-mock` для полных сценариев
- [ ] Mock DMarket API для тестирования edge cases (downtime, rate limits)
- [ ] Увеличить coverage до 90%+

#### CI/CD Pipeline (⏱️ 3 часа)

- [ ] Настройка GitHub Actions для авто-тестов и линтинга
- [ ] Pre-commit hooks для MyPy + Ruff

---

### 🟢 P3 - НИЗКИЙ ПРИОРИТЕТ (Будущие улучшения)

> **⏱️ Общее время**: ~18 часов
> **Цель**: Advanced функции для профессиональной торговли

#### Breadcrumbs для контекста (⏱️ 30 мин)

- [ ] Добавлять breadcrumb перед каждым важным действием:

  ```python
  sentry_sdk.add_breadcrumb(
      category='trading',
      message=f'Scanning {game} market, level={level}',
      level='info',
      data={'user_id': user_id, 'balance': balance}
  )
  ```

- [ ] При падении бота — полный контекст в Sentry

#### Crash notifications через Telegram (⏱️ 20 мин)

- [ ] Интегрировать Sentry webhook → Telegram уведомление
- [ ] При критической ошибке: автоотправка в чат с трейсбеком

#### WebSocket улучшения (⏱️ 6 часов)

- [ ] Перевести `websocket_client.py` на reactive mode
- [ ] Real-time подписка на события DMarket (баланс, ордера)
- [ ] Push-уведомления без polling

#### Расширенная аналитика (⏱️ 8 часов)

- [ ] Реализовать сбор статистики продаж для выявления трендов
- [ ] Определение "справедливой цены" на основе истории
- [ ] Простой алгоритм предсказания падения цен (на основе RSI/MACD если применимо)

#### Архитектурная документация (⏱️ 4 часа)

- [ ] Добавить UML диаграммы (PlantUML) в `docs/ARCHITECTURE.md`
- [ ] Диаграммы последовательности для основных flow (arbitrage, alerts)

---

## 📚 Документация (Обязательно после P0-P1)

> **⏱️ Общее время**: ~3 часа

### Обновить docs/SECURITY.md (⏱️ 1 час)

- [ ] Раздел "Безопасная торговля"
- [ ] Чеклист перед первым запуском на реальных деньгах
- [ ] Процедура экстренной остановки

### Создать docs/DEBUG_WORKFLOW.md (⏱️ 1 час)

- [ ] Как запустить debug_suite.py
- [ ] Процесс запуска на реальных деньгах:
  1. `python scripts/debug_suite.py` (все тесты OK)
  2. 48-72 часа в DRY_RUN=True (мониторинг логов)
  3. Первый ордер на $1-2 вручную
  4. 24 часа мониторинга
  5. Включение полной торговли

### Добавить в README.md раздел "Production Checklist" (⏱️ 1 час)

- [ ] Обязательные шаги перед запуском на реальные деньги
- [ ] Что проверять ежедневно
- [ ] Как интерпретировать логи и уведомления

---

## 🔮 Долгосрочные планы (Phase 3+)

> **Примерные сроки**: Q2-Q3 2026

### База данных

- [ ] Миграция на PostgreSQL (полноценная поддержка в коде)
- [ ] Оптимизация SQL-запросов и индексов

### Веб-интерфейс (Опционально)

- [ ] Простой веб-дашборд (FastAPI + React/Vue) для мониторинга бота
- [ ] Управление конфигурацией через веб-UI
- [ ] Довести покрытие типами до 100% (MyPy strict mode)

### Продвинутые функции торговли

- [ ] **WebSocket арбитраж**:
  - Real-time подписка на изменения цен через WebSocket
  - Снайперский режим (мгновенная покупка при появлении выгодного оффера)
- [ ] **Machine Learning прогнозирование**:
  - Алгоритм предсказания падения цен (RSI/MACD)
  - Автоматическая настройка порогов профита на основе исторических данных

---

## ✅ Выполненные задачи

### Phase 1: Фундамент и Инфраструктура (Завершено: 19.11.2025)

#### Система логирования и мониторинга (Sentry)

- [x] Интеграция `sentry_sdk` для отслеживания ошибок (`src/utils/logging_utils.py`)
- [x] Настройка фильтрации PII (персональных данных) в логах
- [x] Создание `BotLogger` для структурированного JSON-логирования (`structlog`)
- [x] Добавление контекста (user_id, game, command) в логи

#### Пакетная обработка данных

- [x] Реализация `SimpleBatchProcessor` для обработки больших списков предметов (`src/utils/batch_processor.py`)
- [x] Асинхронная обработка чанков с `asyncio.gather`
- [x] Механизм `ProgressTracker` для отслеживания прогресса операций
- [x] Оптимизация API-вызовов через `chunked_api_calls`

#### Управление состоянием и восстановление

- [x] Реализация `StateManager` для сохранения состояния бота (`src/utils/state_manager.py`)
- [x] Автоматическое сохранение состояния при остановке (Graceful Shutdown)
- [x] Восстановление активных задач после перезапуска
- [x] Механизм Circuit Breaker для защиты от каскадных сбоев API

#### Ядро арбитража

- [x] Реализация `ArbitrageScanner` с поддержкой 5 уровней торговли (`src/dmarket/arbitrage_scanner.py`)
- [x] Интеграция с DMarket API v1.1.0 (Aggregated Prices)
- [x] Поддержка мульти-игрового сканирования (CS2, Dota 2, TF2, Rust)
- [x] Анализ ликвидности (`LiquidityAnalyzer`)

---

### Phase 2: Рефакторинг и Очистка кода (Завершено: 22.11.2025)

#### Очистка `src/telegram_bot/`

- [x] Удалить устаревшие файлы: `auto_arbitrage.py`, `enhanced_auto_arbitrage.py`, `enhanced_bot.py`
- [x] Рефакторинг и исправление `market_alerts_handler.py` (Linting, Logic fixes)
- [x] Рефакторинг и исправление `scanner_handler.py` (Linting, Logic fixes, Type safety)
- [x] Рефакторинг `target_handler.py` (Linting, Error handling, Type safety)
- [x] Рефакторинг `keyboards.py` (Formatting, Linting, Type safety)
- [x] Унифицировать структуру хендлеров
- [x] Исправить импорт из несуществующего `enhanced_auto_arbitrage` в `market_alerts.py`
- [x] Исправить синтаксическую ошибку в `state_manager.py` (пропущенная запятая)

#### Оптимизация импортов

- [x] Проверить и удалить циклические импорты (✓ не обнаружены)
- [x] Использовать абсолютные импорты везде
- [x] Автоматически отсортировать импорты через `ruff --fix` (✓ 18 файлов исправлено)

#### Type Hinting

- [x] Проверка MyPy выполнена (обнаружены проблемы в `performance.py`, `base.py` - не критично)

---

## 📊 Статистика выполнения

| Приоритет          | Задач всего | Выполнено | Осталось | Прогресс |
| ------------------ | ----------- | --------- | -------- | -------- |
| **P0** 🔴           | 2           | 2         | 0        | 100%     |
| **P1** 🟠           | 5           | 5         | 0        | 100%     |
| **P2** 🟡           | 9           | 3         | 6        | 33%      |
| **P3** 🟢           | 5           | 0         | 5        | 0%       |
| **Документация** 📚 | 3           | 0         | 3        | 0%       |
| **Phase 1** ✅      | 13          | 13        | 0        | 100%     |
| **Phase 2.1** ✅    | 9           | 9         | 0        | 100%     |

**Общий прогресс**: 32/46 задач (69.6%)

---

## 📝 Changelog

### [23.11.2025] - Phase 3: Production Infrastructure & Advanced Features ✅

**Выполнено:**

- **PM2 Production Configuration** (`ecosystem.config.js`):
  - Настройка кластеризации для высоконагруженных операций
  - Auto-restart при падениях
  - Мониторинг памяти (лимит 500MB)
  - Cron-restart каждый день в 3:00 AM
  - Логирование в `/logs` директорию
  - Переменные окружения для dev/prod

- **Debug Suite** (`scripts/debug_suite.py`):
  - Комплексная система тестирования перед деплоем
  - 6 критических тестов: API, БД, пользователи, цены, ордера, Telegram
  - Интеграция всех компонентов системы
  - Детальные отчёты с timestamps

- **Database Models Enhancement**:
  - `alert.py`: Модель PriceAlert с триггерами и условиями
  - `base.py`: SQLAlchemy base с SQLiteUUID
  - `log.py`: CommandLog и AnalyticsEvent для аудита
  - `market.py`: MarketData и MarketDataCache с JSON полями

- **Chart Generation System** (`chart_generator.py`):
  - Интеграция с QuickChart.io API
  - Графики прибыли, истории сканирования, распределения уровней
  - Асинхронная генерация Chart.js конфигураций
  - Поддержка различных типов визуализации

- **Interactive Dashboard** (`dashboard_handler.py`):
  - Полноценный dashboard для управления сканером
  - Статистика, управление сканами, история, графики
  - Inline-клавиатуры с callback handlers
  - State tracking для пользовательских сессий

- **Notification System Enhancements**:
  - **Digest System** (`notification_digest_handler.py`):
    - Группировка уведомлений по частоте (hourly/daily/weekly)
    - Конфигурируемые пороги и режимы группировки
    - NotificationDigestManager для управления дайджестами
  - **Filter System** (`notification_filters_handler.py`):
    - Фильтрация по играм, профиту, уровням арбитража
    - Гибкая настройка типов уведомлений
    - Метод `should_notify()` для проверки фильтров

- **Safety Mechanisms**:
  - **Price Sanity Checker** (`price_sanity_checker.py`):
    - Защита от аномальных цен
    - Сравнение с исторической средней (7 дней)
    - Порог отклонения 50%
    - Критические алерты при провале проверки
  - **Trading Notifier** (`trading_notifier.py`):
    - Wrapper для DMarketAPI с автоматическими уведомлениями
    - Уведомления о намерениях, успехах, ошибках
    - Интеграция с очередью уведомлений

- **Monitoring Infrastructure**:
  - **Prometheus Metrics** (`prometheus_metrics.py`):
    - Метрики команд бота, latency API, запросов к БД
    - Счётчики арбитражных возможностей и транзакций
    - ASGI app factory для метрик endpoint

- **Commands Enhancement**:
  - **Logs Command** (`logs_command.py`):
    - Просмотр последних 20 INTENT логов
    - JSON parsing с chunking для Telegram лимитов
  - **Resume Command** (`resume_command.py`):
    - Ручное возобновление после error-based pause
    - Проверка прав администратора
    - Интеграция с StateManager

- **Testing Infrastructure**:
  - Crash notification tests suite:
    - `test_crash_notif.py`: Упрощённые тесты
    - `test_crash_notifications.py`: Комплексные с интеграцией
    - `test_crash_notifications_simple.py`: Альтернативный вариант
  - Валидация отправки, форматирования, приоритетов
  - Тесты queue и direct sending режимов

**Функциональность:**

- ✅ Production-ready deployment с PM2
- ✅ Комплексная система тестирования перед запуском
- ✅ Интерактивный dashboard для управления
- ✅ Гибкая система уведомлений (дайджесты + фильтры)
- ✅ Защита от аномальных цен
- ✅ Мониторинг через Prometheus
- ✅ Расширенное логирование и управление

**Следующие шаги:**

1. 🟡 P2: Caching оптимизация (~4 часа)
2. 🟡 P2: Database оптимизация (~3 часа)
3. 🟡 P2: Integration тесты (~5 часов)
4. 📚 Обновить документацию (SECURITY.md, DEBUG_WORKFLOW.md, PRODUCTION.md)

### [23.11.2025] - P1: Автоматическая остановка при проблемах ✅

**Выполнено:**

- Расширен `StateManager` с механизмом отслеживания ошибок:
  - Счётчик `consecutive_errors` (максимум: 5)
  - Методы `record_error()`, `reset_error_counter()`, `pause_operations()`, `resume_operations()`
  - Свойства `is_paused`, `consecutive_errors`
  - Метод `trigger_emergency_shutdown()` для критической остановки
- Создана команда `/resume`:
  - Файл: `src/telegram_bot/commands/resume_command.py`
  - Возобновление операций после паузы
  - Проверка прав администратора
  - Сброс счётчика ошибок
- Добавлена функция `send_critical_shutdown_notification()` в `notifier.py`:
  - Критические уведомления с приоритетом CRITICAL
  - Отправка причины остановки и количества ошибок
  - Инструкции по возобновлению (/resume)
- Интеграция в `main.py`:
  - Инициализация `StateManager` с `max_consecutive_errors=5`
  - Метод `_handle_critical_shutdown()` для обработки критических событий
  - Автоматическая отправка уведомлений всем администраторам при остановке
  - Хранение `state_manager` в `bot_data` для доступа из команд
- Регистрация команды `/resume` в `register_all_handlers.py`

**Функциональность:**

- При 5 последовательных ошибках бот автоматически переходит в режим паузы
- Отправляется критическое уведомление администраторам с подробностями
- Операции можно возобновить вручную командой `/resume`
- Все ошибки логируются с уровнем CRITICAL
- Счётчик сбрасывается после успешной операции или команды `/resume`

**Следующие шаги:**

1. 🟠 P1: Crash notifications через Telegram (~45 мин)
2. 🟡 P2: Ежедневные отчёты (~45 мин)
3. 📚 Обновить документацию (SECURITY.md, DEBUG_WORKFLOW.md)

### [23.11.2025] - P1: Проверка "здравого смысла" цен ✅

**Выполнено:**

- Создан модуль `src/utils/price_sanity_checker.py`:
  - Класс `PriceSanityChecker` для проверки адекватности цен
  - Исключение `PriceSanityCheckFailed` для блокировки подозрительных покупок
  - Константы: `MAX_PRICE_MULTIPLIER = 1.5`, `HISTORY_DAYS = 7`, `MIN_HISTORY_SAMPLES = 3`
- Добавлен `TradingSafetyConfig` в `src/utils/config.py`:
  - `max_price_multiplier`: макс. множитель от средней цены
  - `price_history_days`: период анализа истории
  - `min_history_samples`: минимум сэмплов для расчета
  - `enable_price_sanity_check`: флаг включения/выключения
- Реализован метод `get_price_history()` в `DatabaseManager`
- Поддержка переменных окружения:
  - `MAX_PRICE_MULTIPLIER`
  - `PRICE_HISTORY_DAYS`
  - `MIN_HISTORY_SAMPLES`
  - `ENABLE_PRICE_SANITY_CHECK`

**Функциональность:**

- Автоматическая проверка цены перед каждой покупкой
- Сравнение с историей цен за 7 дней
- Логирование `PRICE_SANITY_CHECK_FAILED` (CRITICAL) при превышении лимита
- Отправка критических алертов в Telegram при проваленной проверке
- Автоматическая блокировка покупки при подозрительных ценах

**Следующие шаги:**

1. 🟠 P1: Автоматическая остановка при проблемах (~30 мин)
2. 🟠 P1: Crash notifications (~45 мин)
3. 📚 Обновить документацию (SECURITY.md, DEBUG_WORKFLOW.md)

### [23.11.2025] - Реструктуризация в Roadmap

**Изменения:**

- Создан новый файл `ROADMAP.md` с четкой приоритизацией
- Задачи распределены по приоритетам (P0/P1/P2/P3)
- Выполненные задачи перенесены в отдельный раздел "Выполненные задачи"
- Добавлены оценки времени для каждой задачи
- Добавлена статистика выполнения

**Следующие шаги:**

1. 🔴 P0: DRY_RUN режим + Debug Suite (~1.5 часа)
2. 🟠 P1: Telegram уведомления + санитарные проверки (~4 часа)
3. 📚 Обновить документацию (SECURITY.md, DEBUG_WORKFLOW.md)

### [22.11.2025] - Phase 2.7: Критические улучшения безопасности

**Добавлена новая фаза Phase 2.7** с приоритетом P0-P1 на основе экспертных рекомендаций senior-разработчика трейдинг-ботов (10+ лет опыта).

**Основание**: Анализ показал критические риски потери денег при реальной торговле.

**Ключевые задачи:**

- DRY_RUN режим по умолчанию
- Debug Suite скрипт с 6 тестами
- Telegram уведомления на каждый ордер
- Санитарные проверки цен
- Автоматическая остановка при проблемах

### [22.11.2025] - Завершение Phase 2.1

**Выполнено:**

- Исправлены критические ошибки в коде
- Оптимизированы импорты (18 файлов)
- Проверка качества кода (Ruff, MyPy)

### [19.11.2025] - Завершение Phase 1

**Выполнено:**

- Система логирования (Sentry + structlog)
- Пакетная обработка данных
- Управление состоянием бота
- Многоуровневый арбитраж (5 уровней)

---

**Версия роадмапы**: 1.0
**Последнее обновление**: 23 ноября 2025 г.
