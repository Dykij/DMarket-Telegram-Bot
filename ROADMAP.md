# 🗺️ ROADMAP: DMarket Telegram Bot - Исправление проблем

**Дата создания**: 23 ноября 2025 г.
**Статус проекта**: 🔴 **ТРЕБУЕТ ИСПРАВЛЕНИЙ** (6.4% тестов провалены)

**📊 Общий прогресс по исправлениям**: 0/8 задач выполнено (0%)

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (Приоритет P0)

> **⏱️ Общее время**: ~20 часов
> **🎯 Цель**: Восстановить работоспособность тестов и исправить блокирующие ошибки

### 1. 🔴 Массовые падения тестов (109/1700 провалено) - ⏱️ 12 часов

**Проблема**: 6.4% тестов (109 из 1700) не проходят

**Затронутые модули**:

- `tests/dmarket/test_auto_arbitrage.py` - весь файл
- `tests/dmarket/test_dmarket_api.py` - buy/sell операции (3 теста)
- `tests/integration/test_api_with_httpx_mock.py` - 24+ теста
- `tests/integration/test_arbitrage_edge_cases.py` - edge cases (6+ тестов)
- `tests/telegram_bot/test_auto_arbitrage.py` - весь файл
- `tests/test_targets.py` - SmartTargets (7 тестов) + интеграционные тесты (1 тест)

**Задачи**:

- [ ] Запустить тесты с полным traceback для определения причин
- [ ] Проверить изменения в `src/dmarket/dmarket_api.py` (API сигнатуры)
- [ ] Обновить моки в integration тестах под pytest-httpx
- [ ] Проверить совместимость pytest-httpx с текущей версией httpx
- [ ] Обновить фикстуры для новых API методов
- [ ] Исправить тесты с buy/sell операциями
- [ ] Проверить изменения в бизнес-логике ArbitrageScanner
- [ ] Обновить тесты SmartTargets после изменений в targets.py

**Детальный план**:

1. **Изоляция проблемы** (2 часа):

   ```bash
   # Запустить один упавший тест с traceback
   python -m pytest tests/dmarket/test_dmarket_api.py::TestBuyItem::test_buy_item_success -vv

   # Определить общую причину
   python -m pytest tests/integration/ -x --tb=short
   ```

2. **Анализ API изменений** (3 часа):
   - Сравнить старые и новые сигнатуры методов в `dmarket_api.py`
   - Проверить изменения в параметрах buy/sell методов
   - Обновить моки с новыми форматами ответов
   - Проверить документацию DMarket API v1.1.0

3. **Обновление моков и фикстур** (4 часа):
   - Обновить `conftest.py` с новыми моками
   - Исправить pytest-httpx mock responses
   - Синхронизировать форматы ответов с реальным API
   - Добавить недостающие поля в mock данных

4. **Исправление бизнес-логики** (2 часа):
   - Проверить изменения в ArbitrageScanner
   - Обновить тесты под новую логику SmartTargets
   - Исправить edge case тесты

5. **Регрессионное тестирование** (1 час):
   - Запустить все тесты после исправлений
   - Убедиться что покрытие не упало
   - Проверить отсутствие новых падений

**Критерий завершения**: < 5 упавших тестов (0.3% failure rate)

---

### 2. 🔴 SQLAlchemy конфликт в test_main.py - ⏱️ 4 часа

**Проблема**: `InvalidRequestError: Attribute name 'metadata' is reserved when using the Declarative API`

**Файл**: `tests/test_main.py`

**Блокировка**: Все тесты главного модуля не запускаются

**Задачи**:

- [ ] Проверить версию SQLAlchemy (`pip show sqlalchemy`)
- [ ] Изолировать проблемный импорт (какая именно модель вызывает ошибку)
- [ ] Проверить `src/models/base.py` на скрытые атрибуты metadata
- [ ] Проверить документацию SQLAlchemy 2.x о reserved attributes
- [ ] Рассмотреть downgrade SQLAlchemy 2.x → 1.4.x если необходимо
- [ ] Исправить определения моделей если проблема в коде
- [ ] Обновить импорты в тестах
- [ ] Проверить fixture инициализацию БД

**Детальный план**:

1. **Диагностика** (1 час):

   ```bash
   # Проверить версию
   pip show sqlalchemy

   # Попытаться импортировать модели по одной
   python -c "from src.models.user import User"
   python -c "from src.models.target import Target"
   python -c "from src.models.market import MarketData"

   # Запустить test_main.py с детальным traceback
   python -m pytest tests/test_main.py -vv --tb=long
   ```

2. **Анализ SQLAlchemy изменений** (1 час):
   - Прочитать migration guide SQLAlchemy 1.4 → 2.0
   - Проверить reserved attributes list
   - Изучить DeclarativeBase vs declarative_base()
   - Проверить правильность использования Mapped[T]

3. **Исправление** (1.5 часа):
   - Если проблема в модели - исправить определение
   - Если проблема в версии - downgrade или обновить код
   - Убрать неявные metadata определения
   - Обновить все модели на современный синтаксис SQLAlchemy 2.x

4. **Валидация** (30 минут):
   - Запустить test_main.py
   - Проверить что все модели импортируются
   - Убедиться что БД инициализируется корректно

**Критерий завершения**: test_main.py импортируется и запускается без ошибок

---

### 3. 🟡 Отсутствующие зависимости - ⏱️ 1 час

**Проблема**: pytest-httpx и apscheduler отсутствовали в requirements.txt

**Последствия**: Тесты не запускались, планировщик не работал

**Задачи**:

- [ ] Добавить pytest-httpx>=0.30.0 в requirements.txt
- [ ] Добавить apscheduler>=3.10.0 в requirements.txt
- [ ] Запустить `pipdeptree` для проверки других отсутствующих зависимостей
- [ ] Синхронизировать requirements.txt с pyproject.toml
- [ ] Обновить .env.example с новыми переменными окружения
- [ ] Проверить что все импорты в коде имеют соответствующие зависимости
- [ ] Создать requirements-dev.txt для dev-only зависимостей

**Детальный план**:

1. **Обновление requirements.txt** (15 минут):

   ```txt
   # Testing
   pytest-httpx>=0.30.0

   # Scheduling
   apscheduler>=3.10.0
   ```

2. **Аудит зависимостей** (30 минут):

   ```bash
   # Проверить все импорты
   pipdeptree -p dmarket-telegram-bot

   # Найти неустановленные пакеты
   python -c "import ast; import sys; [print(i.module) for f in sys.argv[1:] for i in ast.parse(open(f).read()).body if isinstance(i, ast.Import)]" src/**/*.py | sort | uniq
   ```

3. **Синхронизация** (15 минут):
   - Сравнить requirements.txt и pyproject.toml
   - Добавить недостающие dev зависимости
   - Обновить версии до актуальных

**Критерий завершения**: `pip install -r requirements.txt` устанавливает все необходимые пакеты

---

## 🟠 ВАЖНЫЕ ПРОБЛЕМЫ (Приоритет P1)

> **⏱️ Общее время**: ~3 часа
> **🎯 Цель**: Исправить проблемы качества кода

### 4. 🟡 MyPy файловая кодировка - ⏱️ 30 минут

**Проблема**: `src/utils/prometheus_metrics.py` показывает ошибку в MyPy, но Python парсит корректно

**Файл**: `src/utils/prometheus_metrics.py:154`

**Причина**: Windows использует cp1251 по умолчанию, файл в UTF-8

**Задачи**:

- [ ] Добавить `# -*- coding: utf-8 -*-` в начало файла
- [ ] Альтернатива: Исключить файл из MyPy проверки в pyproject.toml
- [ ] Проверить другие файлы на кодировку
- [ ] Настроить VS Code на UTF-8 по умолчанию
- [ ] Добавить pre-commit hook для проверки кодировки

**Исправление**:

```python
# -*- coding: utf-8 -*-
"""Prometheus metrics collection for DMarket bot."""

# ... остальной код
```

Или в `pyproject.toml`:

```toml
[tool.mypy]
exclude = ["src/utils/prometheus_metrics.py"]
```

**Критерий завершения**: MyPy не показывает ошибок или файл исключен из проверки

---

### 5. 🟢 Ruff предупреждения (4 оставшихся) - ⏱️ 1 час

**Проблема**: 4 предупреждения после auto-fix

**Детали**:

- 3× TC002: Type-checking imports not in TYPE_CHECKING блоке
- 1× SIM101: Можно объединить isinstance проверки

**Задачи**:

- [ ] Обернуть type-only импорты в `if TYPE_CHECKING:` блоки
- [ ] Объединить isinstance проверки как предлагает SIM101
- [ ] Опционально: Добавить в `pyproject.toml` → `ignore = ["TC002", "SIM101"]`
- [ ] Запустить Ruff повторно для проверки

**Пример исправления TC002**:

```python
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from src.dmarket.dmarket_api import DMarketAPI
```

**Пример исправления SIM101**:

```python
# До
if isinstance(x, int) or isinstance(x, float):
    ...

# После
if isinstance(x, (int, float)):
    ...
```

**Критерий завершения**: `ruff check src/ tests/` выдаёт 0 ошибок

---

### 6. 🟡 Защита от кириллицы в командах - ⏱️ 1.5 часа

**Проблема**: GitHub Copilot **3 раза** вставил русскую "с" вместо латинской "c"

**Примеры**:

- `сpython -m pytest` вместо `python -m pytest`
- `сStart-Sleep` вместо `Start-Sleep`

**Последствия**: Команды не выполняются, потеря времени на отладку

**Задачи**:

- [ ] Установить расширение VS Code "Highlight Bad Chars"
- [ ] Настроить шрифт Cascadia Code NF / JetBrains Mono NF
- [ ] Добавить pre-commit hook для проверки scripts/ на кириллицу
- [ ] Создать lint правило для обнаружения кириллицы в .sh/.bat/.ps1
- [ ] Обновить документацию docs/vs_code_cyrillic_protection.md

**VS Code settings.json**:

```json
{
  "editor.fontFamily": "'Cascadia Code NF', 'JetBrains Mono NF', monospace",
  "terminal.integrated.fontFamily": "'Cascadia Code NF'",
  "editor.renderWhitespace": "all",
  "editor.renderControlCharacters": true,
  "highlightBadChars.additionalUnicodeChars": ["с", "р", "о", "а", "е"]
}
```

**Pre-commit hook** (`.pre-commit-config.yaml`):

```yaml
- repo: local
  hooks:
    - id: check-cyrillic
      name: Check for Cyrillic in scripts
      entry: python scripts/check_cyrillic.py
      language: system
      files: \.(sh|bat|ps1)$
```

**Критерий завершения**: VS Code подсвечивает кириллицу, pre-commit блокирует коммиты

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ (P2)

> **⏱️ Общее время**: ~3 часа
> **🎯 Цель**: Улучшение документации и покрытия

### 7. 📚 Документация интеграционного тестирования - ⏱️ 2 часа

**Проблема**: Отсутствует руководство по integration тестам

**Задачи**:

- [ ] Создать `docs/INTEGRATION_TESTING_GUIDE.md`
- [ ] Примеры написания integration тестов
- [ ] Best practices для моков DMarket API
- [ ] Структура тестовых данных (fixtures/)
- [ ] Как тестировать edge cases (rate limits, downtime)
- [ ] Примеры использования pytest-httpx
- [ ] Рекомендации по организации тестов

**Структура документа**:

1. Введение в integration тесты
2. Настройка окружения
3. Создание моков с pytest-httpx
4. Примеры тестов для DMarket API
5. Тестирование edge cases
6. Best practices
7. Troubleshooting

**Критерий завершения**: Документ создан, примеры работают

---

### 8. 📊 Увеличение покрытия тестами - ⏱️ 10 часов (долгосрочно)

**Проблема**: Покрытие 25% (цель: 80-85%)

**Задачи**:

- [ ] Идентифицировать модули с низким покрытием
- [ ] Написать тесты для критических модулей (приоритет):
  - `src/dmarket/dmarket_api.py`
  - `src/dmarket/arbitrage_scanner.py`
  - `src/telegram_bot/handlers/`
- [ ] Добавить edge case тесты
- [ ] Проверить покрытие с `pytest --cov-report=html`
- [ ] Цель: довести до 80%+ coverage

**Приоритетные модули**:

1. DMarket API client (критично)
2. ArbitrageScanner (бизнес-логика)
3. TargetManager (торговля)
4. DatabaseManager (данные)
5. Notifier (уведомления)

**Критерий завершения**: Coverage >= 80%

---

## 📊 Статистика проблем

| Приоритет | Проблем всего | Критичность          | Время     | Статус       |
| --------- | ------------- | -------------------- | --------- | ------------ |
| **P0** 🔴  | 3             | Блокируют production | ~17 часов | 0% (0/3)     |
| **P1** 🟠  | 3             | Важные               | ~3 часа   | 0% (0/3)     |
| **P2** 🟢  | 2             | Низкий               | ~12 часов | 0% (0/2)     |
| **ИТОГО** | **8**         | -                    | **32 ч**  | **0% (0/8)** |

---

## 🎯 План на неделю (23.11 - 30.11.2025)

### День 1-2 (23-24.11): Критические исправления

**Цель**: Починить блокирующие проблемы

1. **P0-1**: SQLAlchemy конфликт (4 часа)
   - Изолировать проблему
   - Исправить модели или downgrade версию
   - Запустить test_main.py

2. **P0-2**: Массовые падения тестов - начало (8 часов)
   - Первые 50 тестов с traceback
   - Определить общую причину
   - Начать исправление API моков

### День 3-4 (25-26.11): Доработка тестов

**Цель**: Довести до конца исправление тестов

3. **P0-2**: Массовые падения тестов - завершение (4 часа)
   - Исправить остальные тесты
   - Обновить фикстуры
   - Регрессионное тестирование

4. **P0-3**: Отсутствующие зависимости (1 час)
   - Обновить requirements.txt
   - Аудит pipdeptree
   - Синхронизация с pyproject.toml

### День 5-6 (27-28.11): Качество кода

**Цель**: Убрать все warnings и улучшить DevEx

5. **P1-4**: MyPy кодировка (30 мин)
6. **P1-5**: Ruff предупреждения (1 час)
7. **P1-6**: Защита от кириллицы (1.5 часа)

### День 7 (29.11): Документация

**Цель**: Завершить оставшуюся документацию

8. **P2-7**: Integration testing guide (2 часа)

---

## ✅ Критерии готовности к production

Проект готов к production **ТОЛЬКО** если:

- ✅ **0 упавших тестов** (или < 0.3% = 5 тестов)
- ✅ **SQLAlchemy конфликт решен** (test_main.py работает)
- ✅ **Все зависимости в requirements.txt**
- ✅ **MyPy и Ruff без ошибок**
- ✅ **Покрытие тестами >= 80%**
- ✅ **Документация актуализирована**
- ✅ **Pre-commit hooks настроены**

---

## 📝 Changelog

### [23.11.2025] - Создание новой ROADMAP

**Обнаружено** при запуске debug suite:

- 109 упавших тестов из 1700 (6.4% failure rate)
- SQLAlchemy 'metadata' конфликт блокирует test_main.py
- 2 отсутствующие зависимости (pytest-httpx, apscheduler)
- 4 Ruff предупреждения
- MyPy кодировка файла
- 3 случая вставки кириллицы в команды

**Создана новая ROADMAP** с 8 задачами по приоритетам:

- P0 (Критично): 3 задачи - 17 часов
- P1 (Важно): 3 задачи - 3 часа
- P2 (Низкий): 2 задачи - 12 часов

**Оценка времени**: ~32 часа работы (1 неделя для 1 разработчика)

**Цель**: Восстановить работоспособность проекта и довести до production-ready состояния

---

**Версия ROADMAP**: 2.0
**Последнее обновление**: 23 ноября 2025 г.
**Фокус**: Исправление найденных проблем перед релизом
