# 📋 План обновления проекта DMarket Bot

**Текущий статус**: Phase 2 в процессе
**Дата обновления**: 22 ноября 2025 г.

---

## ✅ Phase 1: Фундамент и Инфраструктура (Завершено)

Эта фаза была посвящена созданию надежной основы для бота, включая обработку ошибок, логирование, управление состоянием и базовую логику арбитража.

### 1.1. Система логирования и мониторинга (Sentry)

- [x] Интеграция `sentry_sdk` для отслеживания ошибок (`src/utils/logging_utils.py`)
- [x] Настройка фильтрации PII (персональных данных) в логах
- [x] Создание `BotLogger` для структурированного JSON-логирования (`structlog`)
- [x] Добавление контекста (user_id, game, command) в логи

### 1.2. Пакетная обработка данных

- [x] Реализация `SimpleBatchProcessor` для обработки больших списков предметов (`src/utils/batch_processor.py`)
- [x] Асинхронная обработка чанков с `asyncio.gather`
- [x] Механизм `ProgressTracker` для отслеживания прогресса операций
- [x] Оптимизация API-вызовов через `chunked_api_calls`

### 1.3. Управление состоянием и восстановление

- [x] Реализация `StateManager` для сохранения состояния бота (`src/utils/state_manager.py`)
- [x] Автоматическое сохранение состояния при остановке (Graceful Shutdown)
- [x] Восстановление активных задач после перезапуска
- [x] Механизм Circuit Breaker для защиты от каскадных сбоев API

### 1.4. Ядро арбитража

- [x] Реализация `ArbitrageScanner` с поддержкой 5 уровней торговли (`src/dmarket/arbitrage_scanner.py`)
- [x] Интеграция с DMarket API v1.1.0 (Aggregated Prices)
- [x] Поддержка мульти-игрового сканирования (CS2, Dota 2, TF2, Rust)
- [x] Анализ ликвидности (`LiquidityAnalyzer`)

---

## 🚀 Phase 2: Оптимизация и Качество жизни (Текущая)

Цель этой фазы - улучшить пользовательский опыт, оптимизировать производительность и навести порядок в кодовой базе.

### 2.1. Рефакторинг и Очистка кода ✅ ЗАВЕРШЕНО

- [x] **Очистка `src/telegram_bot/`**:
  - [x] Удалить устаревшие файлы: `auto_arbitrage.py`, `enhanced_auto_arbitrage.py`, `enhanced_bot.py`
  - [x] Рефакторинг и исправление `market_alerts_handler.py` (Linting, Logic fixes)
  - [x] Рефакторинг и исправление `scanner_handler.py` (Linting, Logic fixes, Type safety)
  - [x] Рефакторинг `target_handler.py` (Linting, Error handling, Type safety)
  - [x] Рефакторинг `keyboards.py` (Formatting, Linting, Type safety)
  - [x] Унифицировать структуру хендлеров
  - [x] Исправить импорт из несуществующего `enhanced_auto_arbitrage` в `market_alerts.py`
  - [x] Исправить синтаксическую ошибку в `state_manager.py` (пропущенная запятая)
- [x] **Оптимизация импортов**:
  - [x] Проверить и удалить циклические импорты (✓ не обнаружены)
  - [x] Использовать абсолютные импорты везде
  - [x] Автоматически отсортировать импорты через `ruff --fix` (✓ 18 файлов исправлено)
- [x] **Type Hinting**:
  - [x] Проверка MyPy выполнена (обнаружены проблемы в `performance.py`, `base.py` - не критично)

### 2.2. Улучшение UX Telegram бота

- [ ] **Интерактивные дашборды**:
  - [ ] Реализовать меню с inline-кнопками для управления сканером
  - [ ] Добавить графики статистики (через `matplotlib` или `quickchart.io`)
- [ ] **Уведомления**:
  - [ ] Настройка гибких фильтров уведомлений (по игре, профиту, уровню)
  - [ ] Группировка уведомлений (дайджесты)

### 2.3. Оптимизация производительности

- [ ] **Caching оптимизация**:
  - [ ] Добавить in-memory cache с TTL для частых запросов (цены, история)
  - [ ] Реализовать query caching в `database.py` для популярных запросов
  - [ ] Заменить `json` на `orjson` для ускорения сериализации
- [ ] **Database оптимизация**:
  - [ ] Миграция на `aiosqlite` для асинхронных операций с SQLite
  - [ ] Добавить индексы для частых запросов
- [ ] **WebSocket улучшения**:
  - [ ] Перевести `websocket_client.py` на reactive mode
  - [ ] Real-time подписка на события DMarket (баланс, ордера)
  - [ ] Push-уведомления без polling

### 2.4. Расширенная аналитика

- [ ] **Анализ истории продаж**:
  - [ ] Реализовать сбор статистики продаж для выявления трендов
  - [ ] Определение "справедливой цены" на основе истории
- [ ] **Прогнозирование**:
  - [ ] Простой алгоритм предсказания падения цен (на основе RSI/MACD если применимо)

### 2.5. Улучшенные уведомления и мониторинг

- [ ] **Crash notifications**:
  - [ ] Интегрировать уведомления о крашах бота через Telegram (в `notifier.py`)
  - [ ] Автоматические алерты при критических ошибках
- [ ] **Интерактивные дашборды**:
  - [ ] Реализовать меню с inline-кнопками для управления сканером
  - [ ] Добавить графики статистики (через `matplotlib` или `quickchart.io`)
- [ ] **Настройка фильтров**:
  - [ ] Гибкие фильтры уведомлений (по игре, профиту, уровню)
  - [ ] Группировка уведомлений (дайджесты)

### 2.6. Качество кода и тестирование

### 2.6. Тестирование и документация

- [ ] **Integration тесты**:
  - [ ] Добавить integration тесты с `httpx-mock` для полных сценариев
  - [ ] Mock DMarket API для тестирования edge cases (downtime, rate limits)
  - [ ] Увеличить coverage до 90%+
- [ ] **Архитектурная документация**:
  - [ ] Добавить UML диаграммы (PlantUML) в `docs/ARCHITECTURE.md`
  - [ ] Диаграммы последовательности для основных flow (arbitrage, alerts)
- [ ] **CI/CD**:
  - [ ] Настройка GitHub Actions для авто-тестов и линтинга
  - [ ] Pre-commit hooks для MyPy + Ruff

---

## � Phase 2.7: Критические улучшения безопасности (ПРИОРИТЕТ P0-P1)

> **Источник**: Экспертные рекомендации senior-разработчика трейдинг-ботов (10+ лет опыта на продакшене).
>
> **Цель**: Защита от потери денег при реальной торговле на DMarket.

### 2.7.1. DRY_RUN режим и Debug Suite 🔴 КРИТИЧНО

- [ ] **DRY_RUN флаг (P0 - 30 минут)**:
  - [ ] Добавить `dry_run: bool = True` в `Config` (по умолчанию True!)
  - [ ] Добавить проверку в DMarket API перед реальными ордерами
  - [ ] Логировать все операции с меткой `[DRY-RUN]` или `[LIVE]`
  - [ ] Добавить переменную окружения `DRY_RUN` в `.env.example`

- [ ] **Debug Suite скрипт (P0 - 1 час)**:
  - [ ] Создать `scripts/debug_suite.py` с 6 тестами:
    - Подключение к DMarket API + проверка баланса
    - Подключение к базе данных
    - Проверка/создание пользователя в БД
    - Тест получения цены и расчёта профита на реальных данных
    - Симуляция ордера в DRY-RUN режиме
    - Отправка тестового уведомления в Telegram
  - [ ] Добавить в README инструкцию: запускать перед каждым деплоем
  - [ ] Интегрировать в CI/CD как обязательный шаг

### 2.7.2. Уведомления и мониторинг торговли 🟠 ВЫСОКИЙ ПРИОРИТЕТ

- [ ] **Telegram уведомления на каждый ордер (P1 - 1 час)**:
  - [ ] Уведомление перед покупкой (INTENT):
    ```
    [DRY-RUN] 🔵 НАМЕРЕНИЕ КУПИТЬ
    AK-47 | Redline (FT)
    💵 Цена: 12.50$
    📊 Продать: 13.89$
    📈 Профит: +1.39$ (+11.1%)
    🎯 Источник: arbitrage_scanner
    ```
  - [ ] Уведомление после покупки (SUCCESS/FAIL)
  - [ ] Уведомление после продажи
  - [ ] Добавить кнопки "Отменить" для ручного контроля

- [ ] **INTENT логирование (P1 - 30 минут)**:
  - [ ] Логировать намерение перед каждой покупкой/продажей:
    ```python
    logger.info("BUY_INTENT", extra={
        "item": name, "price_usd": price, "profit_usd": profit,
        "profit_percent": profit_pct, "source": "arbitrage",
        "dry_run": settings.dry_run, "timestamp": utcnow()
    })
    ```
  - [ ] Добавить команду `/logs` для просмотра последних 20 INTENT логов

- [ ] **Ежедневные отчёты (P2 - 45 минут)**:
  - [ ] Автоматическая отправка сводки раз в день (через APScheduler):
    ```
    📊 Дневной отчёт (22.11.2025)
    💼 Сделок: 15 (12 успешных, 3 отменены)
    💰 Профит: +45.32$ (+12.3%)
    ⚠️ Ошибок API: 2 (rate_limit: 1, timeout: 1)
    🔴 Критических: 0
    ```

### 2.7.3. Санитарные проверки и защита от багов 🟠 ВЫСОКИЙ ПРИОРИТЕТ

- [ ] **Проверка "здравого смысла" цен (P1 - 45 минут)**:
  - [ ] Добавить `MAX_PRICE_MULTIPLIER = 1.5` (макс 50% выше средней)
  - [ ] Перед покупкой сравнивать с историей цен за 7 дней
  - [ ] При превышении лимита:
    - Логировать `PRICE_SANITY_CHECK_FAILED` (CRITICAL)
    - Отправлять уведомление в Telegram с алертом 🚨
    - Блокировать покупку
    - Опционально: останавливать бота полностью

- [ ] **Автоматическая остановка при проблемах (P1 - 30 минут)**:
  - [ ] Счётчик `consecutive_errors`
  - [ ] При 5+ ошибках подряд:
    ```python
    await notifier.send_message("🚨 БОТ ОСТАНОВЛЕН: 5 ошибок подряд")
    await shutdown_gracefully()
    ```
  - [ ] Добавить команду `/resume` для ручного возобновления

- [ ] **Rate limit защита и мониторинг (P2 - 30 минут)**:
  - [ ] Отслеживать 429 ошибки
  - [ ] При превышении лимита: увеличивать delay экспоненциально
  - [ ] Уведомлять в Telegram о приближении к лимиту (90% использования)

### 2.7.4. Валидация API ответов (Pydantic) 🟡 СРЕДНИЙ ПРИОРИТЕТ

- [ ] **Pydantic схемы для DMarket API (P2 - 2 часа)**:
  - [ ] Создать `src/dmarket/schemas.py` с моделями:
    ```python
    class DMarketOffer(BaseModel):
        offer_id: str = Field(..., alias="offerId")
        price_usd: Decimal = Field(..., alias="price")
        item_name: str = Field(..., alias="title")

        class Config:
            populate_by_name = True  # Поддержка старых/новых полей
    ```
  - [ ] Валидировать все ответы через `.model_validate()`
  - [ ] Обрабатывать ValidationError → уведомление в Telegram об изменении API

### 2.7.5. Расширенный Sentry мониторинг 🟢 НИЗКИЙ ПРИОРИТЕТ

- [ ] **Breadcrumbs для контекста (P3 - 30 минут)**:
  - [ ] Добавлять breadcrumb перед каждым важным действием:
    ```python
    sentry_sdk.add_breadcrumb(
        category='trading',
        message=f'Scanning {game} market, level={level}',
        level='info',
        data={'user_id': user_id, 'balance': balance}
    )
    ```
  - [ ] При падении бота — полный контекст в Sentry

- [ ] **Crash notifications через Telegram (P3 - 20 минут)**:
  - [ ] Интегрировать Sentry webhook → Telegram уведомление
  - [ ] При критической ошибке: автоотправка в чат с трейсбеком

### 2.7.6. Документация и процессы 📚

- [ ] **Обновить docs/SECURITY.md**:
  - [ ] Раздел "Безопасная торговля"
  - [ ] Чеклист перед первым запуском на реальных деньгах
  - [ ] Процедура экстренной остановки

- [ ] **Создать docs/DEBUG_WORKFLOW.md**:
  - [ ] Как запустить debug_suite.py
  - [ ] Процесс запуска на реальных деньгах:
    1. `python scripts/debug_suite.py` (все тесты OK)
    2. 48-72 часа в DRY_RUN=True (мониторинг логов)
    3. Первый ордер на $1-2 вручную
    4. 24 часа мониторинга
    5. Включение полной торговли

- [ ] **Добавить в README.md раздел "Production Checklist"**:
  - [ ] Обязательные шаги перед запуском на реальные деньги
  - [ ] Что проверять ежедневно
  - [ ] Как интерпретировать логи и уведомления

---

## 📅 Phase 3: Масштабирование и Pro-функции (Планируемое)

### 3.1. База данных

- [ ] Миграция на PostgreSQL (полноценная поддержка в коде)
- [ ] Оптимизация SQL-запросов и индексов

### 3.2. Веб-интерфейс и другие функции (Опционально)

- [ ] Простой веб-дашборд (FastAPI + React/Vue) для мониторинга бота
- [ ] Управление конфигурацией через веб-UI
- [ ] Довести покрытие типами до 100% (MyPy strict mode)

### 3.3. Продвинутые функции торговли (Будущее)

- [ ] **WebSocket арбитраж (P3)**:
  - [ ] Real-time подписка на изменения цен через WebSocket
  - [ ] Снайперский режим (мгновенная покупка при появлении выгодного оффера)

- [ ] **Machine Learning прогнозирование (P3)**:
  - [ ] Простой алгоритм предсказания падения цен (RSI/MACD)
  - [ ] Автоматическая настройка порогов профита на основе исторических данных

---

## 📝 Журнал изменений (Changelog)

### [22.11.2025] - Phase 2.7: Критические улучшения безопасности 🔥

**Добавлена новая фаза Phase 2.7 - Критические улучшения безопасности (приоритет P0-P1):**

**Основание**: Экспертные рекомендации senior-разработчика трейдинг-ботов с 10+ годами опыта на продакшене.
Анализ показал критические риски потери денег при реальной торговле:
- Отсутствие DRY_RUN режима → бот может начать торговлю сразу на реальные деньги
- Нет отладочного скрипта → невозможно проверить все компоненты перед запуском
- Отсутствие уведомлений о каждом ордере → невозможно отследить действия бота
- Нет санитарных проверок цен → риск покупки по цене в 10x выше рынка
- Отсутствие валидации API ответов → падение при изменении структуры ответа DMarket

**Добавленные секции:**

1. **2.7.1. DRY_RUN режим и Debug Suite (P0 - критично)**:
   - Флаг DRY_RUN по умолчанию True для защиты от случайного запуска торговли
   - Скрипт `scripts/debug_suite.py` с 6 тестами для проверки всех компонентов
   - Обязательный запуск перед каждым деплоем

2. **2.7.2. Уведомления и мониторинг торговли (P1)**:
   - Telegram уведомления на каждый ордер (INTENT → SUCCESS/FAIL)
   - INTENT логирование для post-factum анализа
   - Ежедневные отчёты со статистикой

3. **2.7.3. Санитарные проверки и защита от багов (P1)**:
   - Проверка цен на "здравый смысл" (максимум 50% выше средней)
   - Автоматическая остановка при 5+ ошибках подряд
   - Rate limit мониторинг и защита

4. **2.7.4. Валидация API ответов (P2)**:
   - Pydantic схемы для всех ответов DMarket API
   - Защита от изменений в структуре API
   - Уведомления при ValidationError

5. **2.7.5. Расширенный Sentry мониторинг (P3)**:
   - Breadcrumbs для полного контекста при падении
   - Crash notifications через Telegram webhook

6. **2.7.6. Документация и процессы**:
   - Обновление docs/SECURITY.md (раздел "Безопасная торговля")
   - Создание docs/DEBUG_WORKFLOW.md (процесс запуска на реальных деньгах)
   - Production Checklist в README.md

**Приоритизация**:
- 🔴 P0 (критично): DRY_RUN флаг, Debug Suite - 1.5 часа
- 🟠 P1 (высокий): Уведомления, санитарные проверки - 3 часа
- 🟡 P2 (средний): Pydantic валидация - 2 часа
- 🟢 P3 (низкий): Sentry breadcrumbs - 1 час

**Следующие шаги**:
1. Начать с P0: добавить DRY_RUN флаг (30 мин) → создать debug_suite.py (1 час)
2. P1: Telegram уведомления + санитарные проверки (3 часа)
3. Тестирование: 48-72 часа в DRY_RUN → первый ордер на $1-2 → 24 часа мониторинга

**Цель**: Защита от потери денег через многоуровневую систему проверок и мониторинга.

### [22.11.2025] - Обновление плана Phase 2: Интеграция экспертных рекомендаций ✨

**Добавлены новые секции Phase 2:**

- **2.3. Оптимизация производительности**:
  - In-memory caching с TTL для частых запросов
  - Query caching в `database.py`
  - Миграция на `orjson` для быстрой JSON-сериализации
  - Переход на `aiosqlite` для асинхронных операций с SQLite
  - WebSocket reactive mode для real-time уведомлений

- **2.5. Улучшенные уведомления и мониторинг**:
  - Telegram crash notifications
  - Автоматические алерты при критических ошибках

- **2.6. Качество кода и тестирование**:
  - Integration тесты с `httpx-mock`
  - UML диаграммы (PlantUML) в документации
  - GitHub Actions CI/CD конфигурация

**Источник**: Экспертный анализ от senior Python-разработчика с 10-летним опытом.

### [22.11.2025] - Завершение Phase 2.1: Рефакторинг и Очистка кода ✅

**Исправлены критические ошибки:**

- Исправлена синтаксическая ошибка в `state_manager.py` (пропущенная запятая в logger.debug)
- Заменен импорт из несуществующего `enhanced_auto_arbitrage` на использование `ArbitrageScanner` в `market_alerts.py`
- Автоматически исправлены импорты в 18 файлах через `ruff check --fix`

**Оптимизация импортов:**

- Проверены и подтверждены отсутствие циклических импортов
- Все импорты отсортированы согласно стандарту (isort)
- Используются абсолютные импорты везде

**Проверка качества кода:**

- Ruff: 18 ошибок исправлено, осталась 1 незначительная (в тестовом файле)
- MyPy: Выполнена проверка типизации (обнаружены проблемы в `performance.py`, `base.py` - не критично)
- Циклические импорты: не обнаружены ✓

Статус Phase 2.1: ЗАВЕРШЕНА ✅

### [22.11.2025] - Рефакторинг keyboards.py

- Исправлено форматирование (длина строк, отступы) для соответствия PEP 8.
- Исправлены ошибки линтера (Ruff) и типизации (MyPy).
- Верифицирована работоспособность через unit-тесты.

### [22.11.2025] - Рефакторинг target_handler.py

- Исправлена инициализация `TargetManager` (использование `api_client` вместо `api`).
- Исправлена проверка `callback_data` на `None`.
- Добавлены проверки типов и улучшена обработка ошибок.
- Проверено соответствие структуре других хендлеров.

### [22.11.2025] - Рефакторинг scanner_handler.py

- Исправлена инициализация `ArbitrageScanner` (удален `cache_ttl` из конструктора).
- Исправлен вызов `analyze_market_depth` (конфликт аргументов).
- Добавлены проверки типов для `query.data`.
- Исправлены ошибки линтера (Ruff) и типизации (MyPy).

### [21.11.2025] - Исправление ошибок и рефакторинг

- Исправлены логические ошибки в `market_alerts_handler.py` (неверные вызовы методов менеджера).
- Исправлены ошибки линтинга (длина строк, импорты) в `market_alerts_handler.py`.
- Проверена работоспособность тестов для `market_alerts_handler.py`.

### [19.11.2025] - Рефакторинг плана

- Подтверждено выполнение Phase 1 (Sentry, Batching, State Management).
- Очищен и структурирован файл плана.
- Определены приоритеты для Phase 2.

### [Ранее]

- Реализован многоуровневый арбитраж.
- Добавлена поддержка DMarket API v1.1.0.
