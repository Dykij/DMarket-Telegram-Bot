# 🤖 План управления ботом через AI/ML

## Оглавление

1. [Текущее состояние ML системы](#текущее-состояние-ml-системы)
2. [План улучшений нейросети](#план-улучшений-нейросети)
3. [Архитектура AI-управляемого бота](#архитектура-ai-управляемого-бота)
4. [Этапы внедрения](#этапы-внедрения)
5. [Компоненты автономного управления](#компоненты-автономного-управления)
6. [Система принятия решений](#система-принятия-решений)
7. [Безопасность и ограничения](#безопасность-и-ограничения)
8. [Мониторинг и отладка](#мониторинг-и-отладка)

---

## Текущее состояние ML системы

### Существующие модули

| Модуль | Назначение | Статус |
|--------|-----------|--------|
| `EnhancedPricePredictor` | Прогнозирование цен (RF + XGBoost + GB + Ridge) | ✅ Готов |
| `AdaptivePricePredictor` | Адаптивный прогнозатор (GB + Ridge) | ✅ Готов |
| `AdaptiveTradeClassifier` | Классификация торговых сигналов | ✅ Готов |
| `SmartRecommendations` | Рекомендации по покупке/продаже | ✅ Готов |
| `AnomalyDetector` | Обнаружение аномалий (IsolationForest) | ✅ Готов |
| `DiscountThresholdPredictor` | ML выбор порога скидки | ✅ Новый |
| `LlamaIntegration` | Интеграция Llama 3.1 8B | ✅ Готов |
| `BalanceAdaptiveStrategy` | Стратегия по балансу | ✅ Готов |
| `RealPriceCollector` | Сбор реальных цен с API | ✅ Готов |

### Текущие возможности

```
                    ┌─────────────────────────────────────────┐
                    │         DMarket Telegram Bot            │
                    └─────────────────────────────────────────┘
                                       │
           ┌───────────────────────────┼───────────────────────────┐
           │                           │                           │
           ▼                           ▼                           ▼
    ┌─────────────┐           ┌─────────────┐           ┌─────────────┐
    │   Scanner   │           │   Predictor │           │  Classifier │
    │  (Arbitrage)│           │   (Prices)  │           │  (Signals)  │
    └─────────────┘           └─────────────┘           └─────────────┘
           │                           │                           │
           └───────────────────────────┼───────────────────────────┘
                                       │
                                       ▼
                           ┌─────────────────────┐
                           │   User Decision     │
                           │   (Manual Action)   │
                           └─────────────────────┘
```

---

## План улучшений нейросети

### Фаза 1: Улучшение существующих моделей (1-2 недели)

#### 1.1 Унифицированный AI Coordinator
```python
# src/ml/ai_coordinator.py
class AICoordinator:
    """Координатор всех AI модулей."""
    
    def __init__(self):
        self.price_predictor = EnhancedPricePredictor()
        self.trade_classifier = AdaptiveTradeClassifier()
        self.discount_predictor = DiscountThresholdPredictor()
        self.anomaly_detector = AnomalyDetector()
        self.recommendations = SmartRecommendations()
        self.llama = LlamaIntegration()
    
    async def make_decision(self, item, market_data) -> TradeDecision:
        """Принять решение о торговле с использованием всех моделей."""
        # 1. Прогноз цены
        price_pred = self.price_predictor.predict(item)
        
        # 2. Проверка аномалий
        anomaly = self.anomaly_detector.check_price_anomaly(...)
        
        # 3. Классификация сигнала
        signal = self.trade_classifier.classify(...)
        
        # 4. ML порог скидки
        threshold = self.discount_predictor.predict(...)
        
        # 5. Объединение решений
        return self._aggregate_decisions(...)
```

#### 1.2 Добавить Deep Learning модель (LSTM)
```python
# Для временных рядов цен
class PriceLSTMPredictor:
    """LSTM модель для прогнозирования цен."""
    
    def __init__(self):
        self.model = self._build_lstm()
    
    def _build_lstm(self):
        # Используем PyTorch или TensorFlow
        # Input: [batch, sequence_length, features]
        # Output: predicted_price
        pass
```

#### 1.3 Reinforcement Learning для торговых решений
```python
# Deep Q-Network для принятия решений
class TradingDQNAgent:
    """DQN агент для обучения торговой стратегии."""
    
    # States: market features, portfolio, balance
    # Actions: buy, sell, hold, skip
    # Rewards: profit/loss после сделки
```

### Фаза 2: Автономное управление (2-4 недели)

#### 2.1 Создать BotBrain - центральный модуль управления
```python
# src/ml/bot_brain.py
class BotBrain:
    """Мозг бота - автономное принятие решений."""
    
    async def run_autonomous_cycle(self):
        """Один цикл автономной работы."""
        while True:
            # 1. Сканировать рынок
            opportunities = await self.scan_market()
            
            # 2. Оценить каждую возможность
            evaluated = await self.evaluate_opportunities(opportunities)
            
            # 3. Выбрать лучшие
            selected = self.select_best(evaluated)
            
            # 4. Выполнить (или запросить подтверждение)
            await self.execute_or_confirm(selected)
            
            # 5. Обучиться на результатах
            await self.learn_from_outcomes()
            
            await asyncio.sleep(self.scan_interval)
```

#### 2.2 Система подтверждений
```python
class ConfirmationSystem:
    """Система подтверждений для важных решений."""
    
    # Уровни автономности:
    # 1. MANUAL - все решения требуют подтверждения
    # 2. SEMI_AUTO - только крупные сделки требуют подтверждения
    # 3. AUTO - бот работает полностью автономно (с лимитами)
    
    async def should_execute(self, decision: TradeDecision) -> bool:
        if self.autonomy_level == AutonomyLevel.AUTO:
            return decision.amount <= self.max_auto_amount
        elif self.autonomy_level == AutonomyLevel.SEMI_AUTO:
            return decision.confidence >= 0.8 and decision.amount <= 10.0
        return False  # MANUAL - всегда спрашивать
```

---

## Архитектура AI-управляемого бота

```
                          ┌─────────────────────────────────┐
                          │         User Interface          │
                          │     (Telegram Bot Commands)     │
                          └───────────────┬─────────────────┘
                                          │
                          ┌───────────────▼─────────────────┐
                          │          BOT BRAIN              │
                          │   (Central AI Coordinator)      │
                          └───────────────┬─────────────────┘
                                          │
        ┌─────────────────────────────────┼─────────────────────────────────┐
        │                                 │                                 │
        ▼                                 ▼                                 ▼
┌───────────────┐               ┌───────────────┐               ┌───────────────┐
│   PERCEPTION  │               │   DECISION    │               │   EXECUTION   │
│    Module     │               │    Module     │               │    Module     │
├───────────────┤               ├───────────────┤               ├───────────────┤
│ • Market Scan │               │ • Price Pred  │               │ • Buy Orders  │
│ • Price Feed  │               │ • Signal Class│               │ • Sell Orders │
│ • Anomaly Det │               │ • Risk Assess │               │ • Target Mgmt │
│ • Real Prices │               │ • ML Threshold│               │ • Notificatn  │
└───────────────┘               └───────────────┘               └───────────────┘
        │                                 │                                 │
        └─────────────────────────────────┼─────────────────────────────────┘
                                          │
                          ┌───────────────▼─────────────────┐
                          │         LEARNING Module         │
                          │   (Continuous Improvement)      │
                          ├─────────────────────────────────┤
                          │ • Trade Outcome Tracking        │
                          │ • Model Retraining              │
                          │ • Strategy Optimization         │
                          │ • A/B Testing Strategies        │
                          └─────────────────────────────────┘
```

---

## Этапы внедрения

### Этап 1: Унификация ML (Неделя 1) ✅ ВЫПОЛНЕНО
- [x] Создать `AICoordinator` - единую точку входа для всех ML модулей
- [x] Интегрировать `DiscountThresholdPredictor` с `ArbitrageScanner`
- [x] Добавить `train_from_real_api()` во все модели
- [x] Унифицировать формат прогнозов

### Этап 2: BotBrain - мозг бота (Неделя 2) ✅ ВЫПОЛНЕНО
- [x] Создать `BotBrain` класс (`src/ml/bot_brain.py`)
- [x] Реализовать цикл автономной работы (`run_cycle()`, `run_autonomous()`)
- [x] Добавить систему приоритетов для решений
- [x] Интегрировать Llama для объяснения решений (через AICoordinator)

### Этап 3: Система управления (Неделя 3) ✅ ВЫПОЛНЕНО
- [x] Добавить уровни автономности (MANUAL/SEMI_AUTO/AUTO)
- [x] Реализовать лимиты и ограничения (`SafetyLimits`, `AutonomyConfig`)
- [x] Создать систему алертов и уведомлений (`Alert`, `AlertLevel`)
- [x] Добавить Telegram команды для управления (`ai_brain_handler.py`)

### Этап 4: Continuous Learning (Неделя 4) ⏳ В ПРОЦЕССЕ
- [x] Отслеживание результатов сделок (`add_trade_outcome()`)
- [ ] Автоматическое переобучение моделей
- [ ] A/B тестирование стратегий
- [ ] Оптимизация гиперпараметров

---

## Реализованные компоненты

### Telegram команды для управления AI

| Команда | Описание |
|---------|----------|
| `/ai_brain` | Показать статус BotBrain с интерактивными кнопками |
| `/ai_mode [mode]` | Установить уровень автономности (manual/semi/auto) |
| `/ai_start [interval]` | Запустить автономный режим |
| `/ai_stop` | Остановить автономный режим |
| `/ai_pause` | Приостановить работу |
| `/ai_resume` | Возобновить работу |
| `/ai_limits` | Показать текущие лимиты безопасности |
| `/ai_pending` | Показать ожидающие решения |
| `/ai_confirm [id]` | Подтвердить решение |
| `/ai_reject [id]` | Отклонить решение |
| `/ai_cycle` | Запустить один цикл вручную |
| `/ai_alerts` | Показать последние алерты |
| `/ai_emergency` | Аварийная остановка |

---

## Компоненты автономного управления

### 1. Perception (Восприятие)
```python
class PerceptionModule:
    """Модуль восприятия рынка."""
    
    async def get_market_state(self) -> MarketState:
        return MarketState(
            items=await self.scanner.scan_all_games(),
            prices=await self.collector.get_real_prices(),
            anomalies=self.anomaly_detector.check_all(),
            market_condition=self.analyze_condition(),
        )
```

### 2. Decision (Принятие решений)
```python
class DecisionModule:
    """Модуль принятия решений."""
    
    async def decide(self, market_state: MarketState) -> list[TradeDecision]:
        decisions = []
        
        for item in market_state.items:
            # Прогноз цены
            prediction = self.predictor.predict(item)
            
            # Классификация сигнала
            signal = self.classifier.classify(item, prediction)
            
            # Проверка порога (ML)
            threshold = self.discount_predictor.predict(item.game)
            
            # Оценка риска
            risk = self.assess_risk(item, prediction)
            
            # Генерация решения
            if self.should_act(signal, threshold, risk):
                decisions.append(TradeDecision(
                    action=signal.action,
                    item=item,
                    confidence=signal.confidence,
                    expected_profit=prediction.expected_profit,
                ))
        
        return self.rank_and_filter(decisions)
```

### 3. Execution (Исполнение)
```python
class ExecutionModule:
    """Модуль исполнения решений."""
    
    async def execute(self, decision: TradeDecision) -> TradeResult:
        # Проверка лимитов
        if not self.check_limits(decision):
            return TradeResult(success=False, reason="Limit exceeded")
        
        # Проверка подтверждения (если требуется)
        if self.needs_confirmation(decision):
            await self.request_confirmation(decision)
            return TradeResult(pending=True)
        
        # Выполнение
        if decision.action == Action.BUY:
            return await self.api.buy_item(decision.item)
        elif decision.action == Action.SELL:
            return await self.api.sell_item(decision.item)
        elif decision.action == Action.CREATE_TARGET:
            return await self.api.create_target(decision.item)
```

### 4. Learning (Обучение)
```python
class LearningModule:
    """Модуль непрерывного обучения."""
    
    async def learn_from_outcome(self, decision: TradeDecision, result: TradeResult):
        # Добавить в обучающие данные
        self.training_data.add(
            features=decision.features,
            outcome=result.profit_percent,
            was_profitable=result.profit > 0,
        )
        
        # Переобучить если накопилось достаточно данных
        if self.training_data.new_samples >= 100:
            await self.retrain_models()
```

---

## Система принятия решений

### Алгоритм принятия решений

```
┌─────────────────────────────────────────────────────────────────┐
│                    DECISION ALGORITHM                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. SCAN: Получить список предметов с рынка                    │
│     │                                                           │
│     ▼                                                           │
│  2. FILTER: Отфильтровать по ML-порогу скидки                  │
│     │        threshold = discount_predictor.predict(game)       │
│     ▼                                                           │
│  3. PREDICT: Прогноз цены для каждого предмета                 │
│     │        prediction = price_predictor.predict(item)         │
│     ▼                                                           │
│  4. CLASSIFY: Классифицировать сигнал                          │
│     │        signal = trade_classifier.classify(item, pred)     │
│     ▼                                                           │
│  5. CHECK ANOMALY: Проверить на аномалии                       │
│     │        if anomaly_detector.is_anomaly(item): SKIP         │
│     ▼                                                           │
│  6. ASSESS RISK: Оценить риск                                  │
│     │        risk = risk_assessor.assess(item, signal)          │
│     ▼                                                           │
│  7. BALANCE CHECK: Проверить соответствие балансу              │
│     │        if not balance_strategy.allows(item): SKIP         │
│     ▼                                                           │
│  8. LLAMA REVIEW: (опционально) LLM анализ                     │
│     │        if confidence < 0.8: llama.review(decision)        │
│     ▼                                                           │
│  9. EXECUTE OR CONFIRM: Выполнить или запросить подтверждение  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Веса моделей в ансамбле

| Модель | Вес | Задача |
|--------|-----|--------|
| EnhancedPricePredictor | 0.30 | Прогноз цены |
| TradeClassifier | 0.25 | Сигнал BUY/SELL/HOLD |
| DiscountThresholdPredictor | 0.20 | Оптимальный порог |
| AnomalyDetector | 0.15 | Фильтрация аномалий |
| BalanceAdapter | 0.10 | Адаптация к балансу |

### Формула итогового решения

```python
def calculate_decision_score(item, models_output) -> float:
    """Рассчитать итоговый скор для принятия решения."""
    
    # Веса моделей
    weights = {
        'price_prediction': 0.30,
        'signal_classification': 0.25,
        'discount_threshold': 0.20,
        'anomaly_score': 0.15,
        'balance_fit': 0.10,
    }
    
    # Нормализованные скоры от каждой модели (0-1)
    scores = {
        'price_prediction': models_output.price_pred.confidence,
        'signal_classification': models_output.signal.probability,
        'discount_threshold': 1.0 if item.discount > models_output.threshold else 0.0,
        'anomaly_score': 1.0 - models_output.anomaly.score,  # Инверсия
        'balance_fit': models_output.balance_check.score,
    }
    
    # Взвешенная сумма
    final_score = sum(w * scores[k] for k, w in weights.items())
    
    return final_score
```

---

## Безопасность и ограничения

### Уровни автономности

| Уровень | Описание | Лимиты |
|---------|----------|--------|
| 🔴 MANUAL | Все решения требуют подтверждения | Нет автоматических сделок |
| 🟡 SEMI_AUTO | Авто для мелких, подтверждение для крупных | < $10 авто, > $10 подтверждение |
| 🟢 AUTO | Полностью автономно в рамках лимитов | Max $50/сделка, $200/день |

### Обязательные ограничения (DRY_RUN всегда доступен)

```python
class SafetyLimits:
    """Лимиты безопасности для автономного режима."""
    
    # Максимальная сумма одной сделки
    MAX_SINGLE_TRADE_USD: float = 50.0
    
    # Максимальный дневной объем
    MAX_DAILY_VOLUME_USD: float = 200.0
    
    # Максимальный % баланса на позицию
    MAX_POSITION_PERCENT: float = 30.0
    
    # Минимальная уверенность для автоисполнения
    MIN_CONFIDENCE_AUTO: float = 0.80
    
    # Максимальное количество сделок в час
    MAX_TRADES_PER_HOUR: int = 10
    
    # Cooldown после убыточной сделки (минуты)
    LOSS_COOLDOWN_MINUTES: int = 30
    
    # DRY_RUN режим по умолчанию
    DEFAULT_DRY_RUN: bool = True
```

### Аварийное отключение

```python
class EmergencyStop:
    """Аварийная остановка бота."""
    
    # Триггеры для остановки:
    TRIGGERS = [
        "daily_loss > 20%",      # Дневной убыток > 20%
        "consecutive_losses > 5", # 5+ убыточных сделок подряд
        "api_errors > 10",        # 10+ ошибок API
        "anomaly_score > 0.9",    # Критическая аномалия
        "user_command",           # Команда пользователя /stop
    ]
    
    async def check_and_stop(self):
        for trigger in self.TRIGGERS:
            if self.evaluate(trigger):
                await self.emergency_stop(trigger)
                await self.notify_user(f"⚠️ Бот остановлен: {trigger}")
```

---

## Мониторинг и отладка

### Telegram команды для управления AI

```
/ai_status        - Статус AI модулей
/ai_mode [mode]   - Переключить режим (manual/semi/auto)
/ai_limits        - Текущие лимиты
/ai_stats         - Статистика решений
/ai_explain [id]  - Объяснение решения (через Llama)
/ai_retrain       - Принудительное переобучение
/ai_stop          - Аварийная остановка
/ai_resume        - Возобновить работу
```

### Dashboard метрики

```
╔═══════════════════════════════════════════════════════════╗
║                    AI BOT DASHBOARD                        ║
╠═══════════════════════════════════════════════════════════╣
║                                                            ║
║  📊 Mode: SEMI_AUTO          🟢 Status: ACTIVE             ║
║                                                            ║
║  ═══════════════════════════════════════════════════════  ║
║                                                            ║
║  📈 Today's Performance:                                   ║
║     Trades: 15 | Win Rate: 73% | Profit: +$12.50          ║
║                                                            ║
║  🤖 ML Models:                                             ║
║     PricePredictor:    ✅ Trained (acc: 78%)              ║
║     TradeClassifier:   ✅ Trained (f1: 0.82)              ║
║     DiscountThreshold: ✅ Trained (samples: 250)          ║
║     AnomalyDetector:   ✅ Active (alerts: 3)              ║
║                                                            ║
║  ⚡ Last Actions:                                          ║
║     14:23 BUY  AK-47 Redline   $8.50  ✅ +$0.85           ║
║     14:15 SKIP M4A4 Howl       $450   (above limit)       ║
║     14:02 BUY  AWP Asiimov     $12.00 ✅ +$1.20           ║
║                                                            ║
║  🔒 Safety:                                                ║
║     Daily Limit: $45/$200 | Single: $12.00 max            ║
║     Consecutive Losses: 0 | Anomalies: 0 critical         ║
║                                                            ║
╚═══════════════════════════════════════════════════════════╝
```

---

## Следующие шаги

1. **Немедленно**: Интегрировать `DiscountThresholdPredictor` в сканер
2. **Неделя 1**: Создать `AICoordinator` для унификации моделей
3. **Неделя 2**: Реализовать `BotBrain` с базовой автономностью
4. **Неделя 3**: Добавить Telegram команды для управления AI
5. **Неделя 4**: Включить continuous learning

---

## Приложение: Код интеграции

### Пример использования AICoordinator

```python
from src.ml.ai_coordinator import AICoordinator

async def main():
    # Инициализация
    ai = AICoordinator()
    
    # Обучение на реальных данных
    await ai.train_all_models_from_api(
        games=["csgo", "dota2"],
        items_per_game=500,
    )
    
    # Запуск автономного режима
    await ai.run_autonomous(
        mode=AutonomyLevel.SEMI_AUTO,
        scan_interval=60,  # секунд
    )
```

### Пример Telegram handler

```python
@bot.command("ai_mode")
async def set_ai_mode(update, context):
    mode = context.args[0] if context.args else "manual"
    
    if mode not in ["manual", "semi", "auto"]:
        await update.message.reply_text("❌ Unknown mode. Use: manual, semi, auto")
        return
    
    ai_coordinator.set_mode(mode)
    await update.message.reply_text(f"✅ AI mode set to: {mode.upper()}")
```

---

*Документ создан: Январь 2026*  
*Версия: 1.0*
