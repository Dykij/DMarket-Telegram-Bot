# 🧹 Очистка тестовых issues в Sentry

**Дата**: 23 ноября 2025 г.
**Версия**: 1.0

---

## 📋 Обзор

После тестирования интеграции Sentry в вашем проекте, необходимо очистить тестовые issues перед запуском в production. Этот гайд объясняет, как безопасно удалить тестовые данные.

---

## 🎯 Зачем нужна очистка?

**Проблемы с тестовыми issues:**
- 📊 Искажают статистику ошибок
- 🔔 Могут вызывать ложные алерты
- 🗂️ Засоряют dashboard
- 📈 Влияют на анализ трендов
- 💰 Могут расходовать квоту событий

**Рекомендация:** Всегда очищайте тестовые данные перед production!

---

## 🚀 Быстрая очистка

### Метод 1: Автоматический скрипт (Рекомендуется)

#### Шаг 1: Настройка переменных окружения

Добавьте в `.env`:

```env
# Sentry Cleanup Configuration
SENTRY_AUTH_TOKEN=your_auth_token_here
SENTRY_ORGANIZATION=your-organization-slug
SENTRY_PROJECT=your-project-slug
```

**Получение Auth Token:**
1. Перейдите: https://sentry.io/settings/account/api/auth-tokens/
2. Нажмите **Create New Token**
3. Название: `Cleanup Script`
4. Разрешения: `project:read`, `project:write`, `event:admin`
5. Скопируйте токен

#### Шаг 2: Предпросмотр (Dry Run)

```bash
# Показать что будет удалено (без изменений)
python scripts/sentry_cleanup.py --test-only

# Показать старые issues (>7 дней)
python scripts/sentry_cleanup.py --old 7
```

**Пример вывода:**
```
🔍 Поиск: 'Test Error'...
   ✅ Найдено: 5 issues

   📌 Issue: Test Error
      ID: 12345
      Status: unresolved
      [DRY RUN] Будет: RESOLVE

====================================
📊 Итого:
   Найдено: 15 issues
   Обработано: 15 issues

⚠️  DRY RUN режим - изменения не применены
   Запустите без --dry-run для применения
```

#### Шаг 3: Выполнить очистку

**Опция A: Пометить как resolved (Безопаснее)**
```bash
python scripts/sentry_cleanup.py --test-only --execute
```

**Опция B: Полное удаление (Осторожно!)**
```bash
python scripts/sentry_cleanup.py --test-only --delete --execute
```

**Опция C: Удалить старые resolved issues**
```bash
python scripts/sentry_cleanup.py --old 7 --delete --execute
```

---

### Метод 2: Ручная очистка через Web UI

#### Шаг 1: Фильтрация тестовых issues

1. Откройте Sentry Dashboard
2. Перейдите в **Issues**
3. В поиске введите:
   ```
   Test Error OR Test Critical OR Test Trading
   ```
4. Выберите все найденные issues (checkbox вверху)

#### Шаг 2: Массовые действия

**Опция A: Resolve**
- Нажмите **Resolve** в верхнем меню
- Подтвердите действие

**Опция B: Delete**
- Нажмите **Delete** в выпадающем меню
- ⚠️ **Внимание:** Удаление необратимо!
- Подтвердите действие

#### Шаг 3: Очистка resolved issues

1. Фильтр: `is:resolved`
2. Выберите issues старше 7 дней
3. Удалите их

---

## 🔍 Типы тестовых issues

### Автоматически созданные тестовой командой `/test_sentry`

| Название Issue             | Тип теста   | Можно удалить? |
| -------------------------- | ----------- | -------------- |
| `Test Error`               | error       | ✅ Да           |
| `Test Critical Error`      | critical    | ✅ Да           |
| `Test Trading Error`       | trading     | ✅ Да           |
| `Test Auth Error`          | auth        | ✅ Да           |
| `Test Database Error`      | database    | ✅ Да           |
| `Test Rate Limit Error`    | api         | ✅ Да           |
| `Test Unhandled Exception` | critical    | ✅ Да           |
| `Test User Context`        | context     | ✅ Да           |
| `Test Breadcrumbs`         | breadcrumb  | ✅ Да           |
| `Test Performance`         | performance | ✅ Да           |

### Случайные тестовые запуски

| Шаблон             | Примеры             | Можно удалить? |
| ------------------ | ------------------- | -------------- |
| `test` (lowercase) | `test error`        | ✅ Да           |
| `TEST` (uppercase) | `TEST ERROR`        | ✅ Да           |
| Содержит `[TEST]`  | `[TEST] Real Error` | ⚠️ Осторожно    |
| Содержит `debug`   | `debug mode error`  | ⚠️ Проверить    |

---

## 📊 Использование скрипта очистки

### Основные команды

```bash
# Показать помощь
python scripts/sentry_cleanup.py --help

# Dry run (показать что будет сделано)
python scripts/sentry_cleanup.py --test-only

# Пометить тестовые как resolved
python scripts/sentry_cleanup.py --test-only --execute

# Удалить все тестовые issues
python scripts/sentry_cleanup.py --all --delete --execute

# Удалить issues старше 30 дней
python scripts/sentry_cleanup.py --old 30 --delete --execute
```

### Параметры скрипта

| Параметр      | Описание                                          |
| ------------- | ------------------------------------------------- |
| `--all`       | Очистить все тестовые issues                      |
| `--test-only` | Очистить только issues с "Test" в названии        |
| `--old DAYS`  | Очистить issues старше указанного количества дней |
| `--delete`    | Удалить issues (по умолчанию - только resolve)    |
| `--execute`   | Выполнить изменения (отключить dry-run)           |
| `--dry-run`   | Показать что будет сделано (по умолчанию включен) |

### Примеры использования

**Пример 1: Безопасная очистка перед production**
```bash
# Шаг 1: Предпросмотр
python scripts/sentry_cleanup.py --test-only

# Шаг 2: Пометить как resolved
python scripts/sentry_cleanup.py --test-only --execute

# Шаг 3: Через неделю удалить
python scripts/sentry_cleanup.py --old 7 --delete --execute
```

**Пример 2: Полная очистка (осторожно!)**
```bash
# Сразу удалить все тестовые
python scripts/sentry_cleanup.py --all --delete --execute
```

**Пример 3: Регулярное обслуживание**
```bash
# Удалить старые resolved issues (рекомендуется запускать еженедельно)
python scripts/sentry_cleanup.py --old 14 --delete --execute
```

---

## 🔐 Безопасность

### Защита Auth Token

**✅ Правильно:**
```env
# .env (НЕ коммитить в git!)
SENTRY_AUTH_TOKEN=sntrys_xxxxxxxxxxxxx
```

**❌ Неправильно:**
```python
# НЕ ДЕЛАЙТЕ ТАК!
auth_token = "sntrys_xxxxxxxxxxxxx"  # Хардкод в коде
```

### Добавьте в .gitignore

```gitignore
# Sentry tokens
.env
.env.local
sentry_auth_token.txt
```

### Ротация токенов

Рекомендуется обновлять Auth Token:
- ✅ Каждые 3-6 месяцев
- ✅ После увольнения сотрудника с доступом
- ✅ При подозрении на компрометацию

---

## ⚠️ Предостережения

### Что НЕ удалять

❌ **Не удаляйте:**
- Issues из production окружения
- Нерешенные реальные баги
- Issues с активными обсуждениями
- Issues связанные с PR/commits

✅ **Можно удалять:**
- Issues с "Test" в названии из development
- Resolved issues старше 30 дней
- Duplicate issues
- Spam/false positive issues

### Checklist перед удалением

- [ ] Проверили, что это тестовые данные
- [ ] Сделали dry-run (`--dry-run`)
- [ ] Убедились в правильности фильтра
- [ ] Backup не требуется (удаление необратимо)
- [ ] Уведомили команду (если удаление массовое)

---

## 🎯 Best Practices

### 1. Регулярное обслуживание

**Еженедельно:**
```bash
# Resolve тестовые issues
python scripts/sentry_cleanup.py --test-only --execute
```

**Ежемесячно:**
```bash
# Удалить старые resolved issues
python scripts/sentry_cleanup.py --old 30 --delete --execute
```

### 2. Автоматизация через Cron/Scheduled Task

**Linux Cron:**
```bash
# Добавить в crontab
0 2 * * 0 cd /path/to/bot && python scripts/sentry_cleanup.py --old 30 --delete --execute
```

**Windows Task Scheduler:**
```powershell
# PowerShell скрипт для планировщика
cd D:\Dmarket\DMarket-Telegram-Bot\DMarket-Telegram-Bot
.venv\Scripts\python.exe scripts\sentry_cleanup.py --old 30 --delete --execute
```

### 3. Окружения (Environments)

Используйте разные проекты Sentry для:
- **development** - можно часто очищать
- **staging** - очищать раз в неделю
- **production** - НЕ ОЧИЩАТЬ автоматически!

---

## 📊 Мониторинг эффективности очистки

### Метрики до очистки

```bash
# Запустить dry-run и сохранить результат
python scripts/sentry_cleanup.py --all > cleanup_report_before.txt
```

### Проверка после очистки

1. Откройте Sentry Dashboard
2. Проверьте:
   - Количество unresolved issues уменьшилось
   - Графики ошибок стали читабельнее
   - Quota использования снизилась

---

## 🆘 Troubleshooting

### Ошибка: "Unauthorized"

**Проблема:** Неверный Auth Token

**Решение:**
1. Проверьте `SENTRY_AUTH_TOKEN` в `.env`
2. Убедитесь, что токен не истек
3. Проверьте разрешения токена: `project:write`, `event:admin`

### Ошибка: "Project not found"

**Проблема:** Неверный slug проекта

**Решение:**
1. Проверьте `SENTRY_ORGANIZATION` и `SENTRY_PROJECT`
2. Slug можно найти в URL: `https://sentry.io/organizations/{org}/projects/{project}/`

### Скрипт находит 0 issues

**Проблема:** Нет тестовых issues или неверный фильтр

**Решение:**
1. Проверьте Issues в web UI
2. Выполните `/test_sentry all` для создания тестовых
3. Подождите 1-2 минуты для индексации

---

## 📚 Дополнительные ресурсы

- [Sentry API Documentation](https://docs.sentry.io/api/)
- [Managing Issues](https://docs.sentry.io/product/issues/)
- [Проект: SENTRY_TESTING_GUIDE.md](SENTRY_TESTING_GUIDE.md)
- [Проект: SENTRY_ALERTS_SETUP.md](SENTRY_ALERTS_SETUP.md)

---

## ✅ Финальный Checklist

Перед запуском в production:

- [ ] Выполнили `/test_sentry all` и проверили работу
- [ ] Проверили все breadcrumbs и контекст
- [ ] Запустили dry-run очистки
- [ ] Очистили тестовые issues
- [ ] Настроили алерты (см. SENTRY_ALERTS_SETUP.md)
- [ ] Установили `SENTRY_ENVIRONMENT=production`
- [ ] Снизили `SENTRY_TRACES_SAMPLE_RATE` до 0.1 или ниже
- [ ] Удалили или закомментировали тестовую команду `/test_sentry` (опционально)
- [ ] Проверили квоту событий в Sentry

---

**Готово! Ваш Sentry чист и готов к production! 🎉**
