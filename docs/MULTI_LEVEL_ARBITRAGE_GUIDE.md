"""Руководство по использованию многоуровневого арбитража DMarket Bot.

Этот документ описывает, как использовать новую систему многоуровневого
сканирования арбитражных возможностей и таргетов (buy orders).
"""

## 🚀 Быстрый старт

### 1. Запуск бота

```bash
# Установите переменные окружения
export DMARKET_PUBLIC_KEY="your_public_key"
export DMARKET_SECRET_KEY="your_secret_key"
export TELEGRAM_BOT_TOKEN="your_bot_token"

# Запустите бота
python run_bot.py
```

### 2. Основные команды

- `/start` - Главное меню
- `/arbitrage` - Меню арбитража
- `/targets` - Управление таргетами (buy orders)
- `/scanner` - Многоуровневое сканирование

---

## 📊 Многоуровневое сканирование

### Уровни арбитража

Система предлагает 5 уровней для разных стратегий торговли:

#### 🚀 Разгон баланса (Boost)
- **Цены**: $0.50 - $3.00
- **Мин. прибыль**: 1.5%
- **Рекомендуемый баланс**: $10
- **Риск**: Низкий
- **Сделок в день**: 20-30
- **Для кого**: Начинающие трейдеры с малым капиталом

**Пример использования:**
```python
from src.dmarket.arbitrage_scanner import ArbitrageScanner
from src.dmarket.dmarket_api import DMarketAPI

api = DMarketAPI(public_key, secret_key)
scanner = ArbitrageScanner(api)

# Сканируем уровень boost для CS:GO
results = await scanner.scan_level("boost", game="csgo", max_results=20)

for item in results:
    print(f"{item['title']}: ${item['buy_price']} -> ${item['sell_price']}")
    print(f"Прибыль: ${item['profit']} ({item['profit_percent']:.1f}%)")
```

#### ⭐ Стандарт (Standard)
- **Цены**: $3.00 - $10.00
- **Мин. прибыль**: 3%
- **Рекомендуемый баланс**: $50
- **Риск**: Низкий
- **Сделок в день**: 10-15
- **Для кого**: Трейдеры с небольшим опытом

#### 💰 Средний (Medium)
- **Цены**: $10.00 - $30.00
- **Мин. прибыль**: 5%
- **Рекомендуемый баланс**: $150
- **Риск**: Средний
- **Сделок в день**: 5-10
- **Для кого**: Опытные трейдеры

#### 💎 Продвинутый (Advanced)
- **Цены**: $30.00 - $100.00
- **Мин. прибыль**: 7%
- **Рекомендуемый баланс**: $500
- **Риск**: Средний-Высокий
- **Сделок в день**: 3-7
- **Для кого**: Профессиональные трейдеры

#### 🏆 Профессиональный (Pro)
- **Цены**: $100.00 - $1000.00
- **Мин. прибыль**: 10%
- **Рекомендуемый баланс**: $2000
- **Риск**: Высокий
- **Сделок в день**: 1-3
- **Для кого**: Эксперты с большим капиталом

---

## 🎯 Система таргетов (Buy Orders)

### Что такое таргеты?

Таргеты - это автоматические заявки на покупку предметов по желаемой цене.
Когда кто-то выставляет предмет по вашей цене или ниже, система автоматически
совершает покупку.

### Создание таргета

**Через Telegram бота:**
1. `/targets` - Открыть меню таргетов
2. "📝 Создать таргет"
3. Выбрать игру
4. Ввести название предмета
5. Указать желаемую цену
6. Подтвердить создание

**Программно:**
```python
from src.dmarket.targets import TargetManager
from src.dmarket.dmarket_api import DMarketAPI

api = DMarketAPI(public_key, secret_key)
target_manager = TargetManager(api)

# Создать таргет на AK-47 Redline по цене $12
target = await target_manager.create_target(
    game="a8db",  # CS:GO
    title="AK-47 | Redline (Field-Tested)",
    price=12.00,
    amount=1
)

print(f"Таргет создан: {target['TargetId']}")
```

### Умные таргеты

Система может автоматически создавать таргеты на популярные предметы
по цене ниже рыночной на 5-10%.

```python
# Создать умные таргеты для топ-20 предметов CS:GO
smart_targets = await target_manager.create_smart_targets(
    game="a8db",
    items=top_items,
    price_reduction_percent=7.0
)
```

### Мониторинг таргетов

```python
# Получить активные таргеты
active = await target_manager.get_user_targets(game="a8db", status="Active")

# Получить закрытые таргеты (купленные)
closed = await target_manager.get_closed_targets(limit=20)

# Статистика
stats = await target_manager.get_target_statistics()
print(f"Всего таргетов: {stats['total_targets']}")
print(f"Выполнено: {stats['completed_targets']}")
print(f"Средняя цена: ${stats['avg_price']:.2f}")
```

---

## 🔄 Workflow: Полный цикл арбитража

### Стратегия 1: Быстрый разгон баланса

1. **Сканирование** (уровень Boost):
   ```python
   results = await scanner.scan_level("boost", "csgo", max_results=50)
   ```

2. **Фильтрация** лучших возможностей:
   ```python
   best = [r for r in results if r['profit_percent'] >= 2.5]
   ```

3. **Покупка** предметов вручную или автоматически

4. **Продажа** по рекомендованной цене

5. **Реинвестирование** прибыли в следующий уровень

### Стратегия 2: Таргеты + Сканирование

1. **Создать умные таргеты** на популярные предметы

2. **Периодически сканировать** рынок на среднем уровне:
   ```python
   results = await scanner.scan_level("medium", "csgo")
   ```

3. **Покупать** выгодные предметы из сканирования

4. **Ждать** исполнения таргетов

5. **Продавать** всё с прибылью 5-10%

### Стратегия 3: Профессиональный арбитраж

1. **Обзор рынка**:
   ```python
   overview = await scanner.get_market_overview("csgo")
   ```

2. **Выбрать лучший уровень** на данный момент

3. **Глубокое сканирование** выбранного уровня

4. **Анализ** каждой возможности вручную

5. **Крупные сделки** с высокой прибылью

---

## 📈 Примеры использования API

### Сканирование всех уровней

```python
scanner = ArbitrageScanner(api)

# Сканировать все уровни параллельно
all_results = await scanner.scan_all_levels(
    game="csgo",
    max_results_per_level=20
)

for level, opportunities in all_results.items():
    print(f"\n{level.upper()}: {len(opportunities)} возможностей")
    for opp in opportunities[:3]:  # Топ-3 для каждого уровня
        print(f"  - {opp['title']}: {opp['profit_percent']:.1f}% прибыли")
```

### Поиск лучших возможностей

```python
# Найти топ-10 возможностей среди уровней standard-advanced
best = await scanner.find_best_opportunities(
    game="csgo",
    top_n=10,
    min_level="standard",
    max_level="advanced"
)

for i, opp in enumerate(best, 1):
    print(f"{i}. {opp['title']}")
    print(f"   ${opp['buy_price']:.2f} -> ${opp['sell_price']:.2f}")
    print(f"   Прибыль: ${opp['profit']:.2f} ({opp['profit_percent']:.1f}%)")
```

### Статистика уровней

```python
stats = scanner.get_level_stats()

for level, info in stats.items():
    print(f"\n{info['name']}")
    print(f"  Цены: {info['price_range']}")
    print(f"  Мин. прибыль: {info['min_profit']}")
    print(f"  Баланс: {info['recommended_balance']}")
    print(f"  Риск: {info['risk_level']}")
```

---

## ⚙️ Конфигурация

### Параметры сканера

```python
scanner = ArbitrageScanner(
    api=api_client,
    cache_ttl=300  # Кэш на 5 минут
)
```

### Настройка таргетов

```python
target_manager = TargetManager(
    api=api_client,
    default_game="a8db"  # CS:GO по умолчанию
)
```

### Переменные окружения

```bash
# Обязательные
DMARKET_PUBLIC_KEY=your_key
DMARKET_SECRET_KEY=your_secret
TELEGRAM_BOT_TOKEN=your_token

# Опциональные
DMARKET_API_URL=https://api.dmarket.com
CACHE_TTL=300
MAX_RETRIES=3
RATE_LIMIT_PER_MINUTE=30
```

---

## 🎮 Поддерживаемые игры

| Код   | Игра             | Game ID |
| ----- | ---------------- | ------- |
| csgo  | Counter-Strike 2 | a8db    |
| dota2 | Dota 2           | 9a92    |
| tf2   | Team Fortress 2  | tf2     |
| rust  | Rust             | rust    |

---

## 🔔 Уведомления

Бот может отправлять уведомления о:
- Новых арбитражных возможностях
- Исполнении таргетов
- Изменении цен на отслеживаемые предметы
- Ошибках API

Настройка в меню: `Настройки → Уведомления`

---

## 🐛 Устранение неполадок

### Ошибка: "Не удалось создать API клиент"

**Решение:** Проверьте переменные окружения `DMARKET_PUBLIC_KEY` и `DMARKET_SECRET_KEY`

### Ошибка: "Rate limit exceeded"

**Решение:** DMarket ограничивает ~30 запросов в минуту. Подождите минуту или включите кэширование.

### Пустые результаты сканирования

**Причины:**
- Нет выгодных предметов на данный момент
- Слишком строгие фильтры минимальной прибыли
- Рынок малоактивен

**Решение:** Попробуйте другой уровень или игру, подождите обновления рынка.

---

## 📚 Дополнительные ресурсы

- [Полная документация API DMarket](docs/DMARKET_API_FULL_SPEC.md)
- [План внедрения](docs/IMPLEMENTATION_PLAN.md)
- [Руководство по фильтрам](docs/game_filters_guide.md)
- [Тестирование](docs/testing_guide.md)

---

**Версия**: 2.0
**Дата обновления**: 12 ноября 2025 г.
