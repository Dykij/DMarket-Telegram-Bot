# 🤖 Руководство по перезапуску бота

## Проблема: Бот не отвечает после очистки чата в Telegram

Когда вы очищаете историю чата с ботом в Telegram, бот продолжает работать, но может накопить старые необработанные обновления (updates), которые мешают получать новые сообщения.

## Решение

### 🎯 Метод 1: Автоматический (РЕКОМЕНДУЕТСЯ)

Просто запустите скрипт перезапуска:

```bash
python restart_bot.py
```

Этот скрипт:
1. ✅ Очистит все старые обновления
2. ✅ Автоматически запустит бота
3. ✅ Готов к приему новых сообщений

### 🔧 Метод 2: Ручной (пошагово)

#### Шаг 1: Очистите pending updates
```bash
python clear_bot_updates.py
```

Вы увидите:
```
🔍 Checking for pending updates...
📬 Found 5 pending updates
🧹 Clearing updates up to ID: 143804884...
✅ All old updates cleared!
🚀 Bot is ready to receive new messages
```

#### Шаг 2: Запустите бота
```bash
python -m src.main
```

#### Шаг 3: Проверьте в Telegram
Отправьте `/start` боту - он должен ответить.

## 🚨 Если бот все еще не отвечает

### 1. Проверьте, запущен ли бот
```bash
# Windows
tasklist | findstr python

# Linux/Mac
ps aux | grep python
```

### 2. Проверьте токен бота
```bash
python test_bot_connection.py
```

Должен показать:
```
✅ Bot connection successful!
📱 Bot username: @YourBot
```

### 3. Проверьте логи
```bash
# Последние 50 строк
Get-Content logs\dmarket_bot.log -Tail 50

# Или поиск ошибок
Get-Content logs\dmarket_bot.log | Select-String "error|exception" -Context 2
```

## 💡 Полезные скрипты

| Скрипт                       | Описание                            |
| ---------------------------- | ----------------------------------- |
| `restart_bot.py`             | Автоматическая очистка и перезапуск |
| `clear_bot_updates.py`       | Только очистка updates              |
| `test_bot_connection.py`     | Проверка подключения к Telegram API |
| `test_updates_processing.py` | Тест обработки updates              |

## 📋 Частые вопросы

### Q: Почему бот перестает отвечать после очистки чата?
A: Telegram хранит историю updates (обновлений) для бота. Когда вы очищаете чат, бот все еще пытается обработать старые updates, которые уже неактуальны.

### Q: Нужно ли перезапускать бота каждый раз после очистки?
A: Да, рекомендуется. Используйте `restart_bot.py` для автоматизации.

### Q: Можно ли избежать этой проблемы?
A: Да, не очищайте чат полностью. Вместо этого просто удаляйте отдельные сообщения или используйте команду `/start` для сброса состояния.

### Q: Как часто нужно очищать updates?
A: Telegram автоматически удаляет updates старше 24 часов. Очистка нужна только если:
- Бот был выключен надолго
- Вы очистили чат в Telegram
- Бот перестал отвечать

## 🔄 Автоматическая очистка (расширенное)

Для production можно настроить автоматическую очистку при запуске:

```python
# В src/main.py добавить перед запуском бота:
async def clear_pending_updates_on_start():
    """Clear all pending updates before starting bot."""
    updates = await application.bot.get_updates()
    if updates:
        last_id = updates[-1].update_id
        await application.bot.get_updates(offset=last_id + 1, timeout=1)
        logger.info(f"Cleared {len(updates)} pending updates")
```

## 📞 Поддержка

Если проблема не решается:
1. Проверьте `.env` файл (правильный ли токен?)
2. Убедитесь что бот не забанен в Telegram
3. Проверьте интернет-соединение
4. Посмотрите логи: `logs/dmarket_bot.log`

---

**Создано:** 01.01.2026
**Версия бота:** 1.0.0
**Python:** 3.11+
