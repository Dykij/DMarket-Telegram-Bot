# 🎯 Direct Buy Trading System

Эта система переводит бота на полностью автоматическую прямую торговлю без использования таргетов.

## 📋 Компоненты системы

### 1. **Whitelist (Белый список)** - `src/dmarket/whitelist_config.py`
Высоколиквидные предметы для каждой игры:
- **CS2**: Кейсы (Recoil, Kilowatt), популярные скины
- **Rust**: Базовые строительные предметы
- **Dota 2**: Immortal Treasure, легендарные предметы
- **TF2**: Mann Co. Keys, билеты, металл

**Преимущества**:
- Снижение порога профита на 2% для whitelist предметов
- Гарантированная быстрая продажа
- Минимальный риск

### 2. **Blacklist (Черный список)** - `src/dmarket/blacklist_filters.py`
Фильтры для отсеивания "мусорных" предметов:
- ❌ Сувенирные наборы и пакеты
- ❌ Наклейки и граффити
- ❌ Музыкальные наборы
- ❌ Battle-Scarred скины с низким профитом (<20%)
- ❌ Переплата за дешевые наклейки

### 3. **Inventory Manager** - `src/dmarket/inventory_manager.py`
Автоматическое управление инвентарем:
- 🚀 Мгновенное выставление купленных предметов
- 📉 Undercutting (снижение цены на $0.01 ниже конкурентов)
- 🛡️ Защита от продажи в минус (минимум +2% профита)
- 📊 Статистика работы

### 4. **Direct Buy Logic** - интеграция в `arbitrage_scanner.py`
Логика мгновенной покупки:
```python
async def evaluate_and_buy(self, item: dict):
    # 1. Проверка Blacklist
    if self.blacklist_filter.is_blacklisted(item):
        return

    # 2. Проверка ликвидности
    if not self.liquidity_filter.is_liquid(item):
        return

    # 3. Проверка Whitelist (приоритет)
    is_whitelist = self.whitelist_checker.is_whitelisted(item, game)
    required_margin = self.whitelist_checker.get_adjusted_profit_margin(
        base_margin=8.0,
        is_whitelist=is_whitelist
    )

    # 4. Расчет реального профита (с учетом комиссии 7%)
    net_profit_percent = ((steam_price * 0.93) / buy_price - 1) * 100

    if net_profit_percent >= required_margin:
        # 5. Мгновенная покупка
        success = await self.api.buy_item(item['itemId'], buy_price)

        if success:
            # 6. Silent Mode уведомление
            await send_profit_alert(item)

            # 7. Inventory Manager автоматически выставит на продажу
```

## 🚀 Как это работает

### Жизненный цикл сделки:

1. **Сканирование** (каждые 2-3 минуты)
   - Parallel Scanner проверяет CS2, Rust, Dota 2, TF2
   - Находит предметы с профитом

2. **Фильтрация**
   ```
   Предмет → Blacklist? → Ликвидность? → Whitelist? → Расчет профита
   ```

3. **Покупка** (если профит ≥ порогу)
   - Direct Buy через API
   - Баланс тратится мгновенно (не замораживается)

4. **Выставление на продажу** (автоматически через 30 сек)
   - Inventory Manager находит новый предмет
   - Получает минимальную рыночную цену
   - Выставляет на $0.01 дешевле конкурента

5. **Undercutting** (каждые 30 минут)
   - Проверяет активные лоты
   - Если кто-то выставил дешевле → снижаем цену
   - Защита: не опускаемся ниже цены покупки +2%

6. **Продажа**
   - Предмет продается быстро (в топе списка)
   - Profit зачисляется на баланс

## ⚙️ Настройка

### Файл `.env`:
```bash
# Торговля
DRY_RUN=true              # Тестовый режим (рекомендуется на старте)
MIN_PROFIT_PERCENT=8      # Минимальный профит (уже с вычетом комиссий)
MAX_ITEM_PRICE=30         # Не покупаем дороже $30
CHECK_INTERVAL=120        # Интервал сканирования (секунды)

# Whitelist
WHITELIST_ENABLED=true    # Включить белый список
WHITELIST_PROFIT_BOOST=2.0 # Снижение порога профита для whitelist (%)

# Undercutting
UNDERCUT_ENABLED=true     # Включить автоматическое снижение цен
UNDERCUT_STEP=1           # Снижать на 1 цент ($0.01)
MIN_PROFIT_MARGIN=1.02    # Минимум +2% от цены покупки

# Уведомления
SILENT_MODE=true          # Тихий режим (23:00-08:00 без звука)
TELEGRAM_CHAT_ID=your_id  # Ваш Telegram ID
```

### Пример использования в коде:

```python
from src.dmarket.dmarket_api import DMarketAPI
from src.dmarket.arbitrage_scanner import ArbitrageScanner
from src.dmarket.inventory_manager import InventoryManager
from src.dmarket.whitelist_config import WhitelistChecker
from src.dmarket.blacklist_filters import ItemQualityFilter

# Инициализация
api = DMarketAPI(public_key, secret_key, dry_run=True)
scanner = ArbitrageScanner(api_client=api)
inventory_mgr = InventoryManager(api_client=api)

# Whitelist & Blacklist
whitelist = WhitelistChecker(enable_priority_boost=True)
quality_filter = ItemQualityFilter()

# Добавляем фильтры в сканер
scanner.whitelist_checker = whitelist
scanner.quality_filter = quality_filter

# Запуск в фоне
asyncio.create_task(scanner.continuous_scan())
asyncio.create_task(inventory_mgr.refresh_inventory_loop())
```

## 📊 Преимущества Direct Buy

| Метод           | Таргеты (старый)         | Direct Buy (новый)             |
| --------------- | ------------------------ | ------------------------------ |
| **Скорость**    | Ждем пока кто-то продаст | Покупаем мгновенно             |
| **Баланс**      | Заморожен в ордерах      | Тратится только при покупке    |
| **Конкуренция** | Можем опоздать           | Первыми хватаем выгодные лоты  |
| **Оборот**      | Медленный (дни)          | Быстрый (часы)                 |
| **Риск**        | Средний                  | Низкий (whitelist + blacklist) |

## 🛡️ Безопасность

### Многоуровневая защита:

1. **DRY_RUN режим** по умолчанию
   - Симулирует сделки без реальных покупок
   - Логирует все действия

2. **Blacklist**
   - Отсекает мусорные предметы
   - Защита от манипуляций ценой

3. **Whitelist**
   - Приоритет ликвидным предметам
   - Снижает риск застрять

4. **Profit Floor**
   - Никогда не продаем дешевле покупки +2%
   - Защита от демпинга

5. **Max Item Price**
   - Лимит на максимальную цену ($30 по умолчанию)
   - Защита от больших потерь

## 📈 Метрики эффективности

Inventory Manager собирает статистику:
```python
stats = inventory_mgr.get_statistics()
# {
#     "total_undercuts": 42,      # Сколько раз снижали цену
#     "total_listed": 18,          # Выставлено предметов
#     "failed_listings": 2,        # Неудачных попыток
#     "active_relist_attempts": 3  # Активных попыток перевыставления
# }
```

## 🔧 Troubleshooting

### Бот не покупает предметы:
1. Проверь `DRY_RUN=false` в `.env`
2. Убедись что баланс Available > 0 (не Locked)
3. Проверь логи на ошибки 401 Unauthorized

### Предметы не выставляются на продажу:
1. Проверь что Inventory Manager запущен
2. Убедись что предметы в статусе `at_inventory`
3. Проверь логи на ошибки API

### Undercutting не работает:
1. Проверь `UNDERCUT_ENABLED=true`
2. Убедись что есть активные лоты
3. Проверь что цена не достигла порога `MIN_PROFIT_MARGIN`

## 📚 Дополнительные ресурсы

- `QUICK_START_NOTIFICATIONS.md` - Настройка уведомлений
- `TRADING_IMPROVEMENTS.md` - История улучшений системы
- `docs/ARBITRAGE.md` - Полная документация по арбитражу
- `docs/API_COVERAGE_MATRIX.md` - Покрытие DMarket API методов

## ✅ Чеклист перед запуском

- [ ] ✅ Проверен файл `.env` (ключи, настройки)
- [ ] ✅ `DRY_RUN=true` для первых тестов
- [ ] ✅ Баланс Available > 0 на DMarket
- [ ] ✅ `TELEGRAM_CHAT_ID` указан корректно
- [ ] ✅ Whitelist и Blacklist настроены
- [ ] ✅ Inventory Manager запущен в фоне
- [ ] ✅ Silent Mode настроен (если нужен)

## 🚀 Запуск

```bash
# 1. Установка зависимостей (если еще не)
pip install h2  # HTTP/2 support для ускорения API

# 2. Проверка конфигурации
python -c "from src.dmarket.whitelist_config import WHITELIST_ITEMS; print(WHITELIST_ITEMS)"

# 3. Запуск в тестовом режиме (DRY_RUN=true)
python -m src.main

# 4. Проверка логов через 5 минут
# Должны быть сообщения "🎯 Whitelist priority" и "📉 Undercutting"

# 5. Переход на боевой режим (когда убедились что все работает)
# В .env: DRY_RUN=false
# Перезапуск: python -m src.main
```

## 💡 Pro Tips

1. **TF2 Keys** - самые стабильные для арбитража (цена почти не меняется)
2. **Кейсы CS2** - высокая оборачиваемость, низкий риск
3. **Rust базовые предметы** - продаются мгновенно
4. **Избегайте StatTrak ножей** - низкая ликвидность, застрянете надолго
5. **Следите за Steam распродажами** - во время них профит падает

---

**Версия**: 1.0.0
**Последнее обновление**: 03 января 2026
**Автор**: DMarket Arbitrage Bot Team
**Лицензия**: MIT
