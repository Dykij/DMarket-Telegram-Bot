# Быстрая настройка .env

## ⚠️ ВАЖНО: Бот не запустится без этих настроек!

Файл `.env` уже создан из `.env.example`. Теперь нужно заполнить обязательные поля.

## 🔑 Обязательные параметры

### 1. TELEGRAM_BOT_TOKEN

**Где получить:**
1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду `/newbot` или `/mybots`
4. Следуйте инструкциям
5. Скопируйте токен вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

**Как добавить в .env:**
```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

---

### 2. DMARKET_PUBLIC_KEY и DMARKET_SECRET_KEY

**Где получить:**
1. Перейдите на https://dmarket.com/
2. Войдите в аккаунт
3. Откройте https://dmarket.com/account/api-settings
4. Нажмите **"Create API Key"** (если еще не создан)
5. Скопируйте **Public Key** и **Secret Key**

**Как добавить в .env:**
```env
DMARKET_PUBLIC_KEY=ваш_public_key_здесь
DMARKET_SECRET_KEY=ваш_secret_key_здесь
```

⚠️ **ВАЖНО:** НЕ публикуйте эти ключи в интернете!

---

## 🛡️ Режим безопасности (DRY_RUN)

По умолчанию бот работает в **безопасном режиме** (`DRY_RUN=true`):
- ✅ Все функции доступны
- ✅ Можно тестировать команды
- ❌ Реальные сделки НЕ выполняются
- ❌ Деньги НЕ тратятся

**Текущая настройка в .env:**
```env
DRY_RUN=true
```

Чтобы **включить реальные сделки** (только после тестирования!):
```env
DRY_RUN=false
```

---

## ✅ Проверка настройки

После заполнения `.env` проверьте:

```bash
# Запуск бота
python -m src.main
```

**Ожидаемый результат:**
```
2026-01-02 14:30:00 - __main__ - INFO - Loading configuration...
2026-01-02 14:30:00 - src.utils.config - INFO - ✅ DRY_RUN=true - Bot is in safe mode
2026-01-02 14:30:00 - __main__ - INFO - Configuration loaded successfully
2026-01-02 14:30:01 - __main__ - INFO - Bot started successfully
```

---

## 🚀 Быстрая настройка для тестирования

Если хотите **только протестировать код** без реального бота:

1. Откройте `.env`
2. Установите режим тестирования:
   ```env
   TESTING=true
   ```
3. В этом режиме API ключи не требуются

**Но помните:** В режиме `TESTING=true` бот не подключится к Telegram!

---

## 📝 Пример заполненного .env

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# DMarket API Configuration
DMARKET_PUBLIC_KEY=abc123def456ghi789
DMARKET_SECRET_KEY=xyz987wvu654tsr321
DMARKET_API_URL=https://api.dmarket.com

# Safety Mode (рекомендуется для начала)
DRY_RUN=true

# Database (по умолчанию SQLite)
DATABASE_URL=sqlite:///bot_database.db

# Redis (опционально, для продакшена)
# REDIS_URL=redis://localhost:6379/0

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Environment
ENVIRONMENT=development
DEBUG=false
```

---

## 🔧 Дополнительные параметры (опционально)

### Redis (для production)
```env
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
```

### Sentry (мониторинг ошибок)
```env
SENTRY_DSN=https://your-sentry-dsn-here
SENTRY_ENVIRONMENT=production
```

### PostgreSQL (вместо SQLite)
```env
DATABASE_URL=postgresql://user:password@localhost:5432/dmarket_bot
```

---

## ❓ Частые проблемы

### Проблема: "TELEGRAM_BOT_TOKEN is required"

**Решение:**
1. Откройте файл `.env` в текстовом редакторе
2. Найдите строку `TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here`
3. Замените `your_telegram_bot_token_here` на реальный токен от @BotFather
4. Сохраните файл
5. Перезапустите бота

### Проблема: "DMARKET_PUBLIC_KEY is required"

**Решение:**
1. Перейдите на https://dmarket.com/account/api-settings
2. Создайте API ключ (если еще не создан)
3. Скопируйте Public Key и Secret Key
4. Вставьте их в `.env`:
   ```env
   DMARKET_PUBLIC_KEY=ваш_ключ
   DMARKET_SECRET_KEY=ваш_секретный_ключ
   ```
5. Сохраните файл
6. Перезапустите бота

### Проблема: Бот запускается, но не отвечает в Telegram

**Причины:**
- Неверный TELEGRAM_BOT_TOKEN
- Бот не запущен (@BotFather → /mybots → выберите бота → проверьте что он активен)

**Решение:**
1. Проверьте токен у @BotFather командой `/mybots`
2. Убедитесь что бот не заблокирован
3. Перезапустите бота

---

## 📚 Дополнительная документация

- **Полная настройка:** `ENV_SETUP_GUIDE.md`
- **Быстрый старт:** `docs/QUICK_START.md`
- **Безопасность:** `docs/SECURITY.md`

---

## ✅ Готово!

После заполнения `.env` ваш бот готов к запуску:

```bash
python -m src.main
```

Или используйте команду `/simple` в Telegram для доступа к упрощенному меню! 🚀
