# 📊 GitHub Actions Monitoring & Improvement Guide

## Обзор

Скрипт `scripts/github_actions_monitor.py` автоматически анализирует результаты GitHub Actions workflows и предоставляет конкретные рекомендации по улучшению проекта.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install httpx rich
```

### Базовый запуск

```bash
python scripts/github_actions_monitor.py
```

### С GitHub Token (рекомендуется)

```bash
# Установите токен
export GITHUB_TOKEN=your_github_token_here  # Linux/Mac
$env:GITHUB_TOKEN="your_github_token_here"  # Windows PowerShell

# Запустите скрипт
python scripts/github_actions_monitor.py
```

## 📋 Что анализируется

### 1. Общая статистика
- Количество запусков workflows
- Success rate (процент успешных запусков)
- Среднее время выполнения
- Количество failed/cancelled runs

### 2. Статистика по workflow
- Success rate каждого workflow
- Частота запусков
- Типичные ошибки

### 3. Детали ошибок
- Список failed jobs
- Конкретные шаги, где произошли ошибки
- Ссылки на логи для детального анализа

## 💡 Генерируемые рекомендации

### Автоматические проверки:

1. **Success Rate**
   - 🟢 > 95% - Отлично
   - 🟡 80-95% - Хорошо, но можно улучшить
   - 🔴 < 80% - Требуется внимание

2. **Время выполнения**
   - ✅ < 10 минут - Оптимально
   - ⚠️ 10-15 минут - Рекомендуется оптимизация
   - 🔴 > 15 минут - Требуется оптимизация

3. **Стабильность**
   - Отслеживание нестабильных workflows
   - Выявление частых ошибок
   - Рекомендации по исправлению

## 📊 Примеры отчетов

### Console Output

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ GitHub Actions - Мониторинг и Анализ   ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

📊 Общая Статистика
┏━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━┓
┃ Метрика       ┃ Значение     ┃
┡━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━┩
│ Всего запусков│ 50           │
│ Успешных      │ 45 (90.0%)   │
│ Провалено     │ 5            │
│ Отменено      │ 0            │
│ Среднее время │ 8.5 мин      │
└───────────────┴──────────────┘
```

### Markdown Report

Скрипт автоматически генерирует детальный отчет в `build/reports/`:

```markdown
# 📊 GitHub Actions - Отчет и План Улучшения

## 📈 Текущая Статистика
- Success Rate: 90%
- Среднее время: 8.5 минут

## 💡 Рекомендации
🟡 ВНИМАНИЕ: Success rate ниже 95%
⏱️ ОПТИМИЗАЦИЯ: Можно улучшить с помощью кэширования

## 🚀 План Действий
- [ ] Исправить failing tests
- [ ] Оптимизировать время выполнения
```

## 🔧 Настройка

### GitHub Token

Для избежания rate limiting создайте Personal Access Token:

1. Перейдите на: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Выберите права:
   - ✅ `repo` - Full control of private repositories
   - ✅ `workflow` - Update GitHub Action workflows
4. Скопируйте токен
5. Установите переменную окружения:

```bash
# Linux/Mac
echo 'export GITHUB_TOKEN=your_token' >> ~/.bashrc
source ~/.bashrc

# Windows PowerShell
[System.Environment]::SetEnvironmentVariable('GITHUB_TOKEN', 'your_token', 'User')
```

### Автоматический мониторинг

#### Ежедневный запуск (cron)

**Linux/Mac:**
```bash
# Добавьте в crontab
crontab -e

# Запуск каждый день в 9:00
0 9 * * * cd /path/to/project && python scripts/github_actions_monitor.py
```

**Windows (Task Scheduler):**
1. Откройте Task Scheduler
2. Create Basic Task
3. Trigger: Daily, 9:00 AM
4. Action: Start a program
5. Program: `python`
6. Arguments: `scripts/github_actions_monitor.py`
7. Start in: `D:\BotDmarket-master`

#### GitHub Actions Workflow

Создайте `.github/workflows/monitor.yml`:

```yaml
name: Self-Monitor

on:
  schedule:
    - cron: '0 9 * * *'  # Каждый день в 9:00 UTC
  workflow_dispatch:     # Ручной запуск

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install httpx rich

      - name: Run monitor
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/github_actions_monitor.py

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: monitoring-report
          path: build/reports/*.md
```

## 📈 Интерпретация результатов

### Success Rate

| Диапазон | Оценка    | Действия                      |
| -------- | --------- | ----------------------------- |
| 95-100%  | 🟢 Отлично | Поддерживайте текущий уровень |
| 80-95%   | 🟡 Хорошо  | Устраните нестабильные тесты  |
| < 80%    | 🔴 Плохо   | Срочно требуется исправление  |

### Среднее время

| Время     | Оценка     | Рекомендации          |
| --------- | ---------- | --------------------- |
| < 5 мин   | 🟢 Отлично  | Оптимально            |
| 5-10 мин  | ✅ Хорошо   | Нормально             |
| 10-15 мин | 🟡 Средне   | Добавьте кэширование  |
| > 15 мин  | 🔴 Медленно | Требуется оптимизация |

### Оптимизация workflows

1. **Кэширование зависимостей**
   ```yaml
   - uses: actions/cache@v4
     with:
       path: ~/.cache/pip
       key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
   ```

2. **Параллелизация тестов**
   ```bash
   pytest -n auto  # Использует все CPU
   ```

3. **Разделение на stages**
   - Quick tests (lint, format)
   - Unit tests
   - Integration tests

## 🎯 Практические примеры

### Пример 1: Анализ после неудачного deploy

```bash
# Запустите монитор
python scripts/github_actions_monitor.py

# Проверьте отчет
cat build/reports/github_actions_report_*.md

# Найдите причину ошибки
# Исправьте проблему
# Проверьте улучшение
```

### Пример 2: Еженедельный review

```bash
# Запустите в начале недели
python scripts/github_actions_monitor.py

# Сравните с прошлой неделей
# Отслеживайте тренды
# Планируйте улучшения
```

### Пример 3: Перед релизом

```bash
# Убедитесь в стабильности
python scripts/github_actions_monitor.py

# Success rate должен быть > 95%
# Все critical jobs должны проходить
# Время выполнения оптимально
```

## 🔍 Troubleshooting

### Ошибка: Rate limit exceeded

**Проблема**: Слишком много запросов к GitHub API

**Решение**:
```bash
# Используйте GitHub Token
export GITHUB_TOKEN=your_token
```

### Ошибка: Repository not found

**Проблема**: Неверное имя репозитория

**Решение**: Проверьте настройки в скрипте:
```python
REPO_OWNER = "Dykij"
REPO_NAME = "DMarket-Telegram-Bot"
```

### Ошибка: No module named 'httpx'

**Проблема**: Не установлены зависимости

**Решение**:
```bash
pip install httpx rich
```

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [GitHub REST API](https://docs.github.com/en/rest)
- [Workflow Optimization Guide](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows)

## 🤝 Contributing

Предложения по улучшению монитора приветствуются! Создайте Issue или Pull Request.

---

**Автор**: DMarket Bot Team
**Версия**: 1.0
**Последнее обновление**: 13 ноября 2025 г.
