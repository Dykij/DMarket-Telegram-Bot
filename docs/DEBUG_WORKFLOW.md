# 🐛 Руководство по отладке и запуску на реальных деньгах

**Дата**: 17 декабря 2025 г.
**Версия**: 3.0
**Назначение**: Пошаговое руководство по отладке бота и безопасному переходу на реальную торговлю

---

## 📋 Оглавление

- [Как запустить debug_suite.py](#как-запустить-debug_suitepy)
- [5 шагов процесса запуска на реальных деньгах](#5-шагов-процесса-запуска-на-реальных-деньгах)
- [Интерпретация логов](#интерпретация-логов)
- [Общие проблемы и их решение](#общие-проблемы-и-их-решение)
- [Инструменты отладки](#инструменты-отладки)

---

## 🔧 Как запустить debug_suite.py

### Что такое debug_suite.py?

`debug_suite.py` - это комплексный набор диагностических тестов, который проверяет все критические компоненты бота перед запуском на реальных деньгах. Он включает:

- ✅ Проверку подключения к API DMarket
- ✅ Валидацию конфигурации
- ✅ Тестирование базы данных
- ✅ Проверку Redis (если настроен)
- ✅ Симуляцию торговых операций
- ✅ Проверку системы уведомлений

### Предварительные требования

```bash
# 1. Убедитесь, что виртуальное окружение активировано
# Windows:
.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

# 2. Убедитесь, что все зависимости установлены
pip install -r requirements.txt

# 3. Проверьте наличие .env файла
ls .env  # Linux/macOS
dir .env  # Windows
```

### Запуск debug_suite.py

#### Метод 1: Базовый запуск

```bash
# Запустить все проверки
python scripts/debug_suite.py
```

**Вывод будет выглядеть так:**

```
🔍 DMarket Bot Debug Suite v1.0
=====================================

📋 1/10: Проверка конфигурации...
✅ Конфигурация валидна
   - TELEGRAM_BOT_TOKEN: ****6789 (найден)
   - DMARKET_PUBLIC_KEY: ****xyz9 (найден)
   - DMARKET_SECRET_KEY: **** (найден)
   - DRY_RUN: true

📡 2/10: Проверка подключения к DMarket API...
✅ Подключение успешно
   - Баланс: $125.50
   - API доступен: 200 OK

🗄️  3/10: Проверка базы данных...
✅ База данных доступна
   - Версия: 001_initial_migration
   - Пользователей: 15
   - Активных таргетов: 8

📊 4/10: Проверка Redis кэша...
⚠️  Redis не настроен (опционально)

💼 5/10: Симуляция торговой операции...
✅ Торговая симуляция прошла успешно
   - Найдено возможностей: 12
   - Топ прибыль: 8.5% ($1.25)
   - DRY_RUN режим: активен

🔔 6/10: Проверка системы уведомлений...
✅ Уведомления работают
   - Telegram API: доступен
   - Тестовое сообщение отправлено

🧪 7/10: Запуск integration тестов...
✅ Все тесты пройдены (45/45)
   - Покрытие: 87%

🔍 8/10: Проверка rate limiting...
✅ Rate limiter работает корректно
   - Лимит: 30 req/min
   - Текущая нагрузка: 0 req/min

📈 9/10: Проверка системы мониторинга...
✅ Sentry настроен (опционально)
   - DSN: https://***@sentry.io/***

🛡️  10/10: Финальная проверка безопасности...
✅ Все проверки безопасности пройдены
   - DRY_RUN: ✅ true (безопасно)
   - MAX_TRADE_VALUE: $50.00
   - DAILY_TRADE_LIMIT: $500.00

=====================================
🎉 Результаты: 9/10 тестов пройдено

⚠️  Предупреждения:
   - Redis не настроен (рекомендуется для production)

✅ БОТ ГОТОВ К ТЕСТИРОВАНИЮ В DRY_RUN РЕЖИМЕ
❌ НЕ ПЕРЕКЛЮЧАЙТЕСЬ НА РЕАЛЬНУЮ ТОРГОВЛЮ БЕЗ 48-72 ЧАСОВ ТЕСТИРОВАНИЯ!
```

#### Метод 2: Запуск с детальным логированием

```bash
# Включить подробные логи
python scripts/debug_suite.py --verbose

# Сохранить отчет в файл
python scripts/debug_suite.py --output debug_report.txt

# Только критические проверки (быстрый режим)
python scripts/debug_suite.py --quick
```

#### Метод 3: Запуск через pytest

```bash
# Запустить как pytest тест
pytest scripts/debug_suite.py -v

# С покрытием
pytest scripts/debug_suite.py --cov=src
```

### Интерпретация результатов debug_suite.py

#### ✅ Все тесты пройдены (10/10)

**Действия:**
- Бот готов к использованию в **DRY_RUN** режиме
- Можете начинать 48-72 часовое тестирование
- НЕ переключайтесь на реальную торговлю сразу!

#### ⚠️ Предупреждения (warnings)

**Что проверить:**
- Redis не настроен - рекомендуется для кэширования, но не критично
- Sentry не настроен - рекомендуется для мониторинга ошибок
- Баланс ниже порога - пополните счет

**Действия:**
- Проверьте конфигурацию `.env`
- Настройте опциональные компоненты (при необходимости)
- Продолжайте тестирование с осторожностью

#### ❌ Ошибки (errors)

**Критические ошибки - НЕ ЗАПУСКАЙТЕ БОТ!**
- DMarket API недоступен
- База данных не подключается
- Telegram API недоступен
- Отсутствуют обязательные переменные окружения

**Действия:**
1. Проверьте логи: `logs/dmarket_bot.log`
2. Проверьте `.env` файл
3. Проверьте сетевое подключение
4. Обратитесь к [troubleshooting guide](#общие-проблемы-и-их-решение)

### Автоматический запуск debug_suite.py

Рекомендуется запускать debug_suite.py:

#### 1. Перед каждым запуском бота

```bash
# Добавьте в скрипт запуска
#!/bin/bash
echo "Запуск диагностики..."
python scripts/debug_suite.py --quick

if [ $? -eq 0 ]; then
    echo "✅ Диагностика пройдена, запуск бота..."
    python -m src.main
else
    echo "❌ Диагностика провалена! НЕ ЗАПУСКАЙТЕ БОТ!"
    exit 1
fi
```

#### 2. По расписанию (cron)

```bash
# Запускать каждые 6 часов
0 */6 * * * cd /path/to/bot && .venv/bin/python scripts/debug_suite.py --output /var/log/bot_health.log
```

#### 3. В CI/CD pipeline

```yaml
# .github/workflows/health-check.yml
name: Health Check
on:
  schedule:
    - cron: '0 */6 * * *'  # Каждые 6 часов

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run debug suite
        run: python scripts/debug_suite.py
```

---

## 🚀 5 шагов процесса запуска на реальных деньгах

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Следуйте этим шагам **СТРОГО** по порядку. Не пропускайте ни один шаг!

### Шаг 1: Тестирование в DRY_RUN режиме (48-72 часа)

**Цель**: Убедиться, что бот работает стабильно без рисков для баланса.

**Чек-лист:**

```bash
# 1.1. Проверьте DRY_RUN режим
grep "DRY_RUN=true" .env
# Должно вывести: DRY_RUN=true

# 1.2. Запустите debug_suite.py
python scripts/debug_suite.py
# Убедитесь: ✅ DRY_RUN: true (безопасно)

# 1.3. Запустите бота
python -m src.main

# 1.4. Проверьте логи каждые 6 часов
tail -f logs/dmarket_bot.log | grep "DRY-RUN"
# Все операции должны быть помечены [DRY-RUN]
```

**Что мониторить:**

| Интервал     | Что проверять               | Ожидаемый результат              |
| ------------ | --------------------------- | -------------------------------- |
| **6 часов**  | Логи без критических ошибок | Нет ERROR/CRITICAL               |
| **12 часов** | Арбитражные находки         | Находит 5+ возможностей          |
| **24 часа**  | Симуляция сделок            | Все сделки помечены [DRY-RUN]    |
| **48 часов** | Стабильность                | Нет утечек памяти, нет зависаний |
| **72 часа**  | Готовность к production     | Все метрики в норме              |

**Красные флаги (остановить тестирование):**
- 🚨 Критические ошибки в логах
- 🚨 Бот зависает/крашится
- 🚨 API DMarket возвращает 429/500 ошибки
- 🚨 Утечки памяти (RAM растет постоянно)

**Лог-файлы для проверки:**
```bash
logs/
├── dmarket_bot.log          # Основные логи
├── trading_simulation.log   # Симуляция сделок
└── errors.log               # Только ошибки
```

---

### Шаг 2: Настройка безопасных лимитов

**Цель**: Установить защитные барьеры перед включением реальной торговли.

**Обязательные лимиты в .env:**

```env
# === ЗАЩИТНЫЕ ЛИМИТЫ (ОБЯЗАТЕЛЬНО!) ===

# Режим торговли (ПОКА ОСТАВИТЬ true!)
DRY_RUN=true

# Торговые лимиты
MAX_TRADE_VALUE=25.00          # Начните с $25 макс за сделку
DAILY_TRADE_LIMIT=150.00       # Макс $150 в день
MIN_PROFIT_PERCENT=5.0         # Мин 5% прибыли

# Защита от убытков
STOP_LOSS_PERCENT=10.0         # Остановить если цена упала на 10%
MAX_CONSECUTIVE_LOSSES=5       # Остановить после 5 убытков подряд

# Контроль баланса
MIN_BALANCE_THRESHOLD=10.00    # Остановить если баланс < $10
BALANCE_CHECK_INTERVAL=300     # Проверять каждые 5 минут

# Ограничение одновременных сделок
MAX_CONCURRENT_TRADES=3        # Не более 3 одновременно

# Таймауты и повторы
API_TIMEOUT=30                 # Таймаут API запросов (секунды)
MAX_RETRIES=3                  # Макс попыток повтора при ошибке
RETRY_DELAY=5                  # Задержка между повторами (секунды)
```

**Прогрессивная стратегия лимитов:**

```
День 1-3:   MAX_TRADE_VALUE=$10,  DAILY_LIMIT=$50
День 4-7:   MAX_TRADE_VALUE=$25,  DAILY_LIMIT=$150
День 8-14:  MAX_TRADE_VALUE=$50,  DAILY_LIMIT=$300
День 15+:   Постепенное увеличение на основе ROI
```

**Валидация лимитов:**

```bash
# Запустите валидацию конфигурации
python scripts/validate_config.py

# Вывод:
# ✅ Все лимиты установлены корректно
# ✅ DRY_RUN: true (безопасный режим)
# ✅ MAX_TRADE_VALUE: $25.00 (в пределах нормы)
# ✅ DAILY_TRADE_LIMIT: $150.00 (консервативно)
```

---

### Шаг 3: Настройка системы мониторинга

**Цель**: Настроить алерты для экстренного реагирования.

**3.1. Sentry для отслеживания ошибок**

```bash
# Установка Sentry
pip install sentry-sdk

# В .env добавьте:
# SENTRY_DSN=https://your-key@sentry.io/your-project
```

**Конфигурация Sentry** (`src/main.py`):

```python
import sentry_sdk

sentry_sdk.init(
    dsn=os.getenv("SENTRY_DSN"),
    environment="production",
    traces_sample_rate=1.0,

    # Алерты на критические ошибки
    before_send=lambda event, hint: event if event['level'] == 'error' else None,
)
```

**3.2. Telegram алерты**

```python
# src/utils/alerts.py
async def send_critical_alert(message: str):
    """Отправить критический алерт администраторам."""
    admin_ids = os.getenv("ADMIN_TELEGRAM_IDS", "").split(",")

    for admin_id in admin_ids:
        await bot.send_message(
            chat_id=admin_id,
            text=f"🚨 КРИТИЧЕСКИЙ АЛЕРТ\n\n{message}",
            parse_mode="HTML"
        )
```

**Алерты которые ОБЯЗАТЕЛЬНО настроить:**

| Событие                       | Уровень    | Действие                |
| ----------------------------- | ---------- | ----------------------- |
| Баланс < MIN_BALANCE          | 🔴 CRITICAL | Остановить торговлю     |
| 5 убыточных сделок подряд     | 🔴 CRITICAL | Остановить торговлю     |
| API DMarket недоступен        | 🟡 WARNING  | Пауза на 10 минут       |
| Превышен дневной лимит        | 🟢 INFO     | Прекратить новые сделки |
| Необычно высокая цена (>200%) | 🟡 WARNING  | Пропустить сделку       |

**3.3. Email уведомления (опционально)**

```env
# .env
EMAIL_ALERTS_ENABLED=true
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
ALERT_EMAILS=admin1@example.com,admin2@example.com
```

**Тестирование системы алертов:**

```bash
# Запустить тест алертов
python scripts/test_alerts.py

# Вывод:
# ✅ Telegram алерт отправлен: @your_username
# ✅ Email алерт отправлен: admin@example.com
# ✅ Sentry тест: Event ID 12345abcde
```

---

### Шаг 4: Резервное копирование и план восстановления

**Цель**: Подготовиться к любым сбоям и иметь план восстановления.

**4.1. Автоматический бэкап базы данных**

```bash
#!/bin/bash
# scripts/backup_database.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backups"

# Создать директорию если не существует
mkdir -p $BACKUP_DIR

# PostgreSQL бэкап
pg_dump dmarket_bot_prod > "$BACKUP_DIR/db_backup_$DATE.sql"

# Сжать
gzip "$BACKUP_DIR/db_backup_$DATE.sql"

# Удалить старые (>30 дней)
find $BACKUP_DIR -name "db_backup_*.sql.gz" -mtime +30 -delete

echo "✅ Бэкап завершен: db_backup_$DATE.sql.gz"
```

**Настройка cron для автоматического бэкапа:**

```bash
# Бэкап каждый день в 3:00 AM
0 3 * * * /path/to/scripts/backup_database.sh >> /var/log/backup.log 2>&1
```

**4.2. Бэкап конфигурации и логов**

```bash
#!/bin/bash
# scripts/backup_configs.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backups/configs"

mkdir -p $BACKUP_DIR

# Бэкап .env (БЕЗОПАСНО!)
tar -czf "$BACKUP_DIR/config_$DATE.tar.gz" \
    .env \
    config/ \
    logs/ \
    --exclude='logs/*.log'  # Исключить активные логи

echo "✅ Конфигурация сохранена: config_$DATE.tar.gz"
```

**4.3. План восстановления при сбое**

**Сценарий 1: База данных повреждена**

```bash
# 1. Остановить бота
systemctl stop dmarket-bot  # или docker-compose down

# 2. Восстановить последний бэкап
gunzip /backups/db_backup_20251119_030000.sql.gz
psql dmarket_bot_prod < /backups/db_backup_20251119_030000.sql

# 3. Проверить целостность
python scripts/validate_database.py

# 4. Перезапустить бота
systemctl start dmarket-bot
```

**Сценарий 2: Потеря конфигурации**

```bash
# 1. Восстановить из бэкапа
tar -xzf /backups/configs/config_20251119.tar.gz

# 2. Проверить валидность
python scripts/validate_config.py

# 3. Перезапустить
systemctl restart dmarket-bot
```

**Сценарий 3: Сервер полностью недоступен**

**План действий:**
1. **Экстренная остановка через API DMarket** (отменить все таргеты вручную)
2. **Развернуть резервный сервер** с последнего бэкапа
3. **Восстановить базу данных** и конфигурацию
4. **Запустить в DRY_RUN** для проверки
5. **Постепенное возобновление** торговли

**4.4. Контакты экстренной поддержки**

```env
# .env
EMERGENCY_CONTACT_PHONE=+1234567890
EMERGENCY_CONTACT_EMAIL=emergency@your-domain.com
EMERGENCY_CONTACT_TELEGRAM=@your_emergency_account
```

---

### Шаг 5: Первый запуск на реальных деньгах

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Это точка невозврата! После этого шага бот будет тратить реальные деньги!

**ФИНАЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ:**

```bash
# 5.1. Финальная проверка DRY_RUN режима
python scripts/debug_suite.py --production-check

# Вывод должен быть:
# 🔍 Production Readiness Check
# ================================
# ✅ 48+ часов тестирования в DRY_RUN
# ✅ Все лимиты установлены
# ✅ Система мониторинга настроена
# ✅ Бэкапы настроены
# ✅ База данных здорова
# ✅ API DMarket доступен
#
# ⚠️  ВНИМАНИЕ: Бот готов к запуску на РЕАЛЬНЫХ ДЕНЬГАХ!
#
# Для продолжения введите: YES_I_AM_READY
```

**5.2. Запись начального состояния**

```bash
# Записать текущий баланс
python scripts/record_initial_balance.py

# Вывод:
# 📊 Начальный баланс записан:
# - USD: $125.50
# - Дата: 2025-11-19 15:30:00
# - Файл: data/initial_balance.json
```

**5.3. Переключение на реальную торговлю**

```bash
# Отредактировать .env
nano .env

# Изменить (ВНИМАТЕЛЬНО!):
# DRY_RUN=false  # ⚠️ РЕАЛЬНАЯ ТОРГОВЛЯ!

# Сохранить и выйти (Ctrl+X, Y, Enter)
```

**5.4. Перезапуск с реальной торговлей**

```bash
# Перезапустить бота
systemctl restart dmarket-bot
# или
docker-compose restart bot

# Проверить логи НЕМЕДЛЕННО
tail -f logs/dmarket_bot.log

# ОБЯЗАТЕЛЬНО проверьте:
# - Логи показывают [LIVE] вместо [DRY-RUN]
# - Первые 5 минут НЕТ критических ошибок
# - Балансовые операции отображаются корректно
```

**Первая торговая сессия (первые 2 часа):**

```
Минута 0-5:   Мониторить логи каждую минуту
Минута 5-30:  Проверять каждые 5 минут
Минута 30-60: Проверять каждые 10 минут
Час 1-2:      Проверять каждые 15 минут
```

**Первая сделка - что проверить:**

```bash
# После первой сделки (НЕМЕДЛЕННО):
# 1. Проверить баланс
python scripts/check_balance.py

# 2. Проверить логи
grep "LIVE.*purchase" logs/dmarket_bot.log

# 3. Проверить в DMarket аккаунте
# - Зайти на dmarket.com
# - Проверить инвентарь
# - Проверить историю транзакций

# 4. Сверить с логами бота
# Баланс в DMarket = Баланс в боте?
```

**5.5. Первый день мониторинга**

**Каждые 30 минут проверять:**
- [ ] Логи не показывают критических ошибок
- [ ] Баланс изменяется корректно (сверяется с DMarket)
- [ ] Сделки совершаются в рамках лимитов
- [ ] Нет зацикливания (покупка одного и того же предмета)

**Вечером первого дня:**
```bash
# Запустить ежедневный отчет
python scripts/generate_daily_report.py

# Проверить:
# - Общая прибыль/убыток
# - Количество сделок
# - Средняя прибыльность
# - Ошибки/предупреждения

# Если убыток > 10%, ОСТАНОВИТЬ торговлю!
```

**Красные флаги первого дня (ОСТАНОВИТЬ НЕМЕДЛЕННО):**
- 🚨 Убыток более $20 или 10% баланса
- 🚨 5+ неудачных сделок подряд
- 🚨 Бот покупает одни и те же предметы
- 🚨 Необычно высокие цены покупки (>200% рынка)
- 🚨 API ошибки 429/500
- 🚨 Баланс не сверяется с DMarket (разница >$1)

---

## 📋 Интерпретация логов

### Структура лог-файлов

```
logs/
├── dmarket_bot.log          # Все логи (ротация каждые 10MB)
├── trading.log              # Только торговые операции
├── errors.log               # Только ошибки (ERROR и выше)
└── debug.log                # Детальные debug логи (если DEBUG=true)
```

### Формат лог-записей

**Стандартный формат:**

```
2025-11-19 15:30:45,123 | INFO | src.dmarket.arbitrage_scanner | [user:12345] [level:standard] Найдено арбитражных возможностей: 12
```

**Расшифровка:**
- `2025-11-19 15:30:45,123` - Timestamp (дата и время с миллисекундами)
- `INFO` - Уровень логирования (DEBUG/INFO/WARNING/ERROR/CRITICAL)
- `src.dmarket.arbitrage_scanner` - Модуль/компонент
- `[user:12345]` - Контекст (ID пользователя)
- `[level:standard]` - Дополнительный контекст (уровень сканирования)
- `Найдено арбитражных возможностей: 12` - Сообщение

### Уровни логирования

| Уровень      | Значение                        | Действие                   |
| ------------ | ------------------------------- | -------------------------- |
| **DEBUG**    | Детальная отладочная информация | Игнорировать в production  |
| **INFO**     | Обычные операции                | Мониторить для статистики  |
| **WARNING**  | Предупреждения                  | Обратить внимание          |
| **ERROR**    | Ошибки (не критичные)           | Проверить причину          |
| **CRITICAL** | Критические ошибки              | Немедленное вмешательство! |

### Примеры логов и их интерпретация

#### ✅ Нормальные операции (INFO)

```log
2025-11-19 15:30:00 | INFO | src.main | Бот запущен в DRY_RUN режиме
```
**Интерпретация**: Бот успешно запустился в безопасном режиме. Всё ОК.

```log
2025-11-19 15:30:15 | INFO | src.dmarket.api | [DRY-RUN] Получен баланс: $125.50
```
**Интерпретация**: API работает, баланс получен. Операция симулирована (DRY-RUN).

```log
2025-11-19 15:31:20 | INFO | src.dmarket.arbitrage_scanner | Найдено 8 возможностей (топ прибыль: 7.5%)
```
**Интерпретация**: Сканер нашел арбитражные возможности. Топ прибыль 7.5% - хороший показатель.

```log
2025-11-19 15:32:00 | INFO | src.dmarket.targets | [DRY-RUN] Таргет создан: AK-47 | Redline (FT) @ $8.00
```
**Интерпретация**: Таргет создан успешно (симуляция). Название и цена записаны.

#### ⚠️ Предупреждения (WARNING)

```log
2025-11-19 15:33:15 | WARNING | src.dmarket.api | Rate limit близок к превышению: 28/30 запросов
```
**Интерпретация**: Приближаемся к лимиту API (30 req/min). Бот автоматически замедлится.
**Действие**: Наблюдать. Если повторяется часто - уменьшить частоту сканирования.

```log
2025-11-19 15:34:00 | WARNING | src.dmarket.arbitrage | Прибыль меньше минимума: 3.2% < 5.0%
```
**Интерпретация**: Найденная сделка имеет прибыль ниже установленного минимума (5%).
**Действие**: Нормально. Бот пропустит эту сделку.

```log
2025-11-19 15:35:20 | WARNING | src.utils.cache | Redis недоступен, используется in-memory кэш
```
**Интерпретация**: Redis не настроен или недоступен. Используется менее эффективный кэш.
**Действие**: Настроить Redis для лучшей производительности (опционально).

#### 🚨 Ошибки (ERROR)

```log
2025-11-19 15:36:45 | ERROR | src.dmarket.api | Ошибка API DMarket: 429 Too Many Requests
```
**Интерпретация**: Превышен лимит запросов к API DMarket (30 req/min).
**Действие**:
- Проверить `retry_after` в ответе API
- Бот автоматически подождет перед повтором
- Если повторяется - уменьшить частоту запросов

```log
2025-11-19 15:37:00 | ERROR | src.telegram_bot.handlers | Не удалось отправить сообщение: Chat not found
```
**Интерпретация**: Пользователь заблокировал бота или удалил чат.
**Действие**: Нормально. Бот пометит пользователя как неактивного.

```log
2025-11-19 15:38:15 | ERROR | src.dmarket.api | Ошибка покупки: Insufficient funds (balance: $5.00, required: $10.00)
```
**Интерпретация**: Недостаточно средств для покупки.
**Действие**:
- Пополнить баланс DMarket
- Или уменьшить MAX_TRADE_VALUE

#### 🔴 Критические ошибки (CRITICAL)

```log
2025-11-19 15:39:00 | CRITICAL | src.main | База данных недоступна: Connection refused
```
**Интерпретация**: Потеряно подключение к базе данных!
**Действие**:
1. **НЕМЕДЛЕННО** остановить бота
2. Проверить PostgreSQL: `systemctl status postgresql`
3. Проверить подключение: `psql -h localhost -U user -d dmarket_bot`
4. Восстановить из бэкапа если нужно

```log
2025-11-19 15:40:00 | CRITICAL | src.dmarket.trading | [LIVE] 5 убыточных сделок подряд! ОСТАНОВКА ТОРГОВЛИ!
```
**Интерпретация**: Сработал стоп-лосс защита после 5 убытков.
**Действие**:
1. Торговля остановлена автоматически
2. Проверить причину убытков
3. Проанализировать последние сделки
4. Скорректировать стратегию
5. НЕ ВОЗОБНОВЛЯТЬ без анализа!

```log
2025-11-19 15:41:00 | CRITICAL | src.dmarket.api | [LIVE] Необычная цена покупки: $500 (рынок: $50) - ОТМЕНЕНО
```
**Интерпретация**: Обнаружена аномально высокая цена (в 10 раз выше рынка). Сделка отменена.
**Действие**:
1. Проверить логику ценообразования
2. Проверить данные DMarket API
3. Возможен баг или манипуляция рынком

### Специальные маркеры в логах

#### [DRY-RUN] - Симуляция

```log
2025-11-19 15:30:00 | INFO | src.dmarket.api | [DRY-RUN] Покупка симулирована: AK-47 @ $10.00
```
**Значение**: Операция НЕ выполнена реально. Это тестовый режим.

#### [LIVE] - Реальная торговля

```log
2025-11-19 15:30:00 | INFO | src.dmarket.api | [LIVE] Покупка выполнена: AK-47 @ $10.00
```
**Значение**: Операция выполнена РЕАЛЬНО! Деньги списаны!

#### [user:12345] - Контекст пользователя

```log
2025-11-19 15:30:00 | INFO | src.telegram_bot | [user:123456789] Команда /balance
```
**Значение**: ID пользователя Telegram, выполнившего действие.

#### [request_id:abc123] - ID запроса

```log
2025-11-19 15:30:00 | INFO | src.dmarket.api | [request_id:abc123] GET /marketplace-api/v1/items
```
**Значение**: Уникальный ID запроса для отслеживания цепочки операций.

### Поиск в логах

#### Найти все ошибки за последний час

```bash
# Linux/macOS
grep "ERROR\|CRITICAL" logs/dmarket_bot.log | tail -100

# Windows PowerShell
Select-String -Path logs\dmarket_bot.log -Pattern "ERROR|CRITICAL" | Select-Object -Last 100
```

#### Найти все операции конкретного пользователя

```bash
grep "user:123456789" logs/dmarket_bot.log
```

#### Найти все реальные сделки (LIVE)

```bash
grep "\[LIVE\].*purchase\|sell" logs/trading.log
```

#### Найти все операции с определенным предметом

```bash
grep "AK-47 | Redline" logs/dmarket_bot.log
```

#### Статистика ошибок по типам

```bash
grep "ERROR" logs/dmarket_bot.log | awk '{print $6}' | sort | uniq -c | sort -rn
```

**Пример вывода:**
```
45 src.dmarket.api
12 src.telegram_bot.handlers
5 src.utils.database
```

### Анализ производительности из логов

#### Время выполнения операций

```bash
# Найти долгие операции (>5 секунд)
grep "execution_time" logs/dmarket_bot.log | awk '$NF > 5000'
```

#### Частота сканирования

```bash
# Сколько сканирований в час
grep "arbitrage_scan_completed" logs/dmarket_bot.log | grep "$(date +%Y-%m-%d)" | wc -l
```

---

## 🐛 Общие проблемы и их решение

### Проблема 1: DMarket API возвращает 401 Unauthorized

**Симптомы:**
```log
ERROR | src.dmarket.api | Ошибка API: 401 Unauthorized
```

**Причины:**
- Неверные API ключи
- Ключи истекли
- Неправильная подпись HMAC

**Решение:**
```bash
# 1. Проверить ключи в .env
cat .env | grep "DMARKET"

# 2. Проверить генерацию подписи
python scripts/test_api_signature.py

# 3. Сгенерировать новые ключи на dmarket.com
# 4. Обновить .env и перезапустить
```

---

### Проблема 2: Бот не находит арбитражные возможности

**Симптомы:**
```log
INFO | src.dmarket.arbitrage_scanner | Найдено возможностей: 0
```

**Причины:**
- Слишком высокий MIN_PROFIT_PERCENT
- Неверные фильтры
- Рынок неактивен

**Решение:**
```bash
# 1. Проверить настройки
grep "MIN_PROFIT_PERCENT" .env

# 2. Временно снизить до 3%
# MIN_PROFIT_PERCENT=3.0

# 3. Проверить фильтры игр
python scripts/test_game_filters.py

# 4. Проверить активность рынка вручную на dmarket.com
```

---

### Проблема 3: Утечка памяти (RAM постоянно растет)

**Симптомы:**
```bash
# RAM использование постоянно увеличивается
# 1 час:  500MB
# 6 часов: 2GB
# 24 часа: 8GB
```

**Причины:**
- Неочищенный кэш
- Незакрытые соединения
- Накопление логов в памяти

**Решение:**
```bash
# 1. Включить ограничение кэша
# В .env:
# CACHE_MAX_SIZE=1000
# CACHE_TTL=300

# 2. Проверить закрытие соединений
python scripts/check_memory_leaks.py

# 3. Перезапускать бота раз в 24 часа (cron)
0 3 * * * systemctl restart dmarket-bot
```

---

### Проблема 4: Таргеты не исполняются

**Симптомы:**
```log
INFO | src.dmarket.targets | Таргетов создано: 10
INFO | src.dmarket.targets | Исполнено за 24ч: 0
```

**Причины:**
- Цена таргета слишком низкая
- Нет предложений на рынке
- Конкуренция с другими покупателями

**Решение:**
```bash
# 1. Проверить текущие цены на рынке
python scripts/check_market_prices.py --title "AK-47 | Redline (FT)"

# 2. Увеличить цену таргета на 2-3%
# 3. Использовать "умные таргеты" (автоматический расчет цены)
```

---

## 🔧 Инструменты отладки

### 1. Interactive Debugger (pdb)

```python
# Вставить в код где нужна остановка
import pdb; pdb.set_trace()

# Или для async
import asyncio
breakpoint()
```

**Команды pdb:**
- `n` (next) - следующая строка
- `s` (step) - войти в функцию
- `c` (continue) - продолжить выполнение
- `p variable` - вывести значение переменной
- `l` (list) - показать код вокруг
- `q` (quit) - выйти

### 2. Логирование debug информации

```bash
# Включить DEBUG логирование
export LOG_LEVEL=DEBUG

# Перезапустить бота
systemctl restart dmarket-bot

# Смотреть debug логи
tail -f logs/debug.log
```

### 3. Профилирование производительности

```bash
# Запустить с профилированием
python -m cProfile -o profile.stats -m src.main

# Анализировать результаты
python -m pstats profile.stats
> sort cumulative
> stats 20  # Топ 20 медленных функций
```

### 4. Memory Profiler

```bash
# Установить
pip install memory-profiler

# Запустить
python -m memory_profiler src/main.py

# Найти утечки
mprof run src/main.py
mprof plot
```

### 5. HTTP Request Inspector

```bash
# Логировать все HTTP запросы
export HTTPX_LOG_LEVEL=DEBUG

# Смотреть запросы
tail -f logs/http_requests.log
```

---

## 📚 Дополнительные ресурсы

- **[SECURITY.md](SECURITY.md)** - Полное руководство по безопасности
- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт для новичков
- **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - Решение проблем
- **[API_REFERENCE.md](api_reference.md)** - Справка по API методам

---

## 🆘 Получение помощи

Если проблема не решается:

1. **Проверьте Issues на GitHub**: https://github.com/your-repo/issues
2. **Создайте новый Issue** с:
   - Подробным описанием проблемы
   - Релевантными логами
   - Шагами воспроизведения
3. **Обратитесь в Telegram**: @your_support_bot
4. **Email**: support@your-domain.com

**Формат Issue:**

```markdown
**Проблема**: Краткое описание

**Шаги воспроизведения**:
1. Шаг 1
2. Шаг 2

**Логи**:
```
<paste logs here>
```

**Окружение**:
- OS: Ubuntu 22.04
- Python: 3.11.5
- Bot Version: 1.0.0
```

---

**Версия документа**: 1.0
**Последнее обновление**: 17 декабря 2025 г.

---

**⚠️ ПОМНИТЕ**: Никогда не запускайте бота на реальных деньгах без тщательного тестирования!

**Удачной торговли! 🚀💰**
