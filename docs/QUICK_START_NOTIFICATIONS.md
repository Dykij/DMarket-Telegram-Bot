# 🎯 Быстрый старт после улучшений

## ✅ Что было исправлено

1. **Ошибка 401 Unauthorized** - исправлена ✅
2. **Черный список** - добавлен фильтр мусорных предметов ✅
3. **Фильтры ликвидности** - отсекаются неликвидные скины ✅
4. **Silent Mode** - уже реализован в `notifications.py` ✅
5. **HTTP/2** - установлен для ускорения запросов ✅

---

## 🚀 Что нужно сделать СЕЙЧАС

### Шаг 1: Получить Chat ID для уведомлений

Бот не может отправлять уведомления, пока вы не укажете `ADMIN_CHAT_ID`.

**Вариант А: Автоматически**
```bash
python get_chat_id.py
# Затем отправьте /start боту в Telegram
```

**Вариант Б: Вручную**
1. Отправьте /start боту @userinfobot в Telegram
2. Он пришлет ваш Chat ID (например: 458765683)
3. Скопируйте это число

### Шаг 2: Обновить .env файл

Откройте `.env` и найдите строку:
```bash
ADMIN_CHAT_ID=
```

Замените на:
```bash
ADMIN_CHAT_ID=458765683  # ВАШ РЕАЛЬНЫЙ ID
```

### Шаг 3: Перезапустить бота

```bash
python -m src.main
```

---

## 📊 Проверка работы уведомлений

После запуска бота проверьте логи:

```bash
# В логах должно появиться
INFO - Notification system initialized with chat_id=458765683
INFO - ✅ Уведомление запланировано: AK-47 | Vulcan (rust), профит: $1.25
```

Если бот находит предметы (6-10 штук каждые 3 минуты), вы должны получить уведомления в Telegram.

---

## 🔍 Если уведомления НЕ приходят

### Проблема 1: "Notification system not available"

**Решение**: Проверьте что файл `src/telegram_bot/utils/notifications.py` существует.

```bash
# Проверка
ls src/telegram_bot/utils/notifications.py
```

### Проблема 2: "ADMIN_CHAT_ID пустой"

**Решение**: Заполните `ADMIN_CHAT_ID` в `.env` (см. Шаг 2 выше).

### Проблема 3: Уведомления приходят ТОЛЬКО о "мусоре"

**Решение**: Черный список работает! Проверьте логи:

```bash
grep "Пропускаем уведомление (blacklist)" logs/dmarket_bot.log
```

Если видите много таких записей - фильтры работают корректно.

---

## 🎯 Тестирование системы

### Тест 1: Проверка черного списка

```python
from src.dmarket.blacklist_filters import ItemBlacklistFilter

filter = ItemBlacklistFilter()

# Этот предмет должен быть отклонен
test_item = {"title": "Souvenir Package CS:GO"}
assert filter.is_blacklisted(test_item) == True

# Этот предмет должен пройти
test_item2 = {"title": "AK-47 | Vulcan (Field-Tested)"}
assert filter.is_blacklisted(test_item2) == False

print("✅ Черный список работает!")
```

### Тест 2: Проверка фильтра ликвидности

```python
from src.dmarket.blacklist_filters import ItemLiquidityFilter

filter = ItemLiquidityFilter()

# Низкая ликвидность - отклонить
test_item = {
    "title": "Редкий нож",
    "statistics": {"sales24h": 1, "avg_sales_per_day": 0.1}
}
assert filter.is_liquid(test_item) == False

# Высокая ликвидность - принять
test_item2 = {
    "title": "AK-47 | Redline",
    "statistics": {"sales24h": 10, "avg_sales_per_day": 5.0},
    "price": {"amount": 1000},
    "suggestedPrice": {"amount": 1200}
}
assert filter.is_liquid(test_item2) == True

print("✅ Фильтр ликвидности работает!")
```

---

## 🌙 Silent Mode (Тихий режим)

Уже реализован в `notifications.py`!

**Как работает**:
- С 23:00 до 08:00 - уведомления приходят БЕЗ звука
- Полное отключение: установите `SILENT_MODE=true` в `.env`

**Настройка тихих часов** (в `notifications.py`):
```python
# Изменить часы тихого режима
is_silent_night = current_hour >= 22 or current_hour < 9  # С 22:00 до 09:00
```

---

## 📈 Мониторинг в реальном времени

Откройте терминал и следите за логами:

```bash
tail -f logs/dmarket_bot.log | grep "Уведомление"
```

Вы должны видеть:
```
✅ Уведомление запланировано: AK-47 | Vulcan (rust), профит: $1.25
⏭ Пропускаем уведомление (blacklist): Souvenir Package ...
⏭ Пропускаем дубликат уведомления: ...
```

---

## 🎯 Следующие улучшения (Ready to implement)

После того как уведомления заработают, вы можете запросить:

### 1. Прямые покупки (Direct Buy)
Бот будет мгновенно покупать выгодные предметы вместо создания таргетов.

### 2. Автоматическое перевыставление (Undercutting Bot)
Бот будет автоматически снижать цену на ваших лотах, если конкуренты выставили дешевле.

### 3. Кнопка "Купить сейчас" в уведомлениях
Добавить inline кнопку для покупки предмета прямо из Telegram.

---

## 🆘 Troubleshooting

### Ошибка: "ModuleNotFoundError: No module named 'nacl'"

```bash
pip install pynacl
```

### Ошибка: "h2 not found"

```bash
pip install h2
```

### Бот не запускается после изменений

```bash
# Проверка синтаксиса
python -m py_compile src/dmarket/blacklist_filters.py
python -m py_compile src/dmarket/arbitrage_scanner.py

# Если ошибок нет - проверьте зависимости
pip install -r requirements.txt
```

---

## ✅ Checklist готовности

- [ ] `ADMIN_CHAT_ID` заполнен в `.env`
- [ ] `NOTIFICATIONS_ENABLED=true` в `.env`
- [ ] `SILENT_MODE=false` в `.env` (если хотите получать звуковые уведомления)
- [ ] Бот запущен: `python -m src.main`
- [ ] Вы отправили `/start` боту в Telegram
- [ ] В логах видно: `Notification system initialized`
- [ ] Бот находит возможности (6-10 штук каждые 3 минуты)
- [ ] Уведомления приходят в Telegram

---

**Готово!** 🎉

Если все пункты checklist выполнены, но уведомления не приходят - напишите скриншот логов за последние 5 минут.

**Файлы для проверки**:
- `logs/dmarket_bot.log` (последние 100 строк)
- `.env` (только строка ADMIN_CHAT_ID, без токенов!)
