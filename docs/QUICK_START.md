# 🚀 Быстрый старт: Запуск DMarket Telegram Bot

**Дата**: 17 декабря 2025 г.
**Версия**: 5.0
**Последнее обновление**: Полная актуализация документации

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Режим безопасности DRY_RUN

**ПРЕЖДЕ ЧЕМ НАЧАТЬ**, убедитесь, что понимаете разницу между режимами:

### 🔵 DRY_RUN=true (По умолчанию - БЕЗОПАСНО)

- ✅ Бот **НЕ** делает реальные сделки
- ✅ Все операции покупки/продажи **симулируются**
- ✅ Ваш баланс на DMarket **не изменяется**
- ✅ Идеально для тестирования и обучения
- ✅ Логи отмечены `[DRY-RUN]`

### 🔴 DRY_RUN=false (ОПАСНО - Реальные деньги!)

- ❌ Бот **ДЕЛАЕТ реальные сделки**
- ❌ Покупки и продажи **списывают/начисляют** реальные деньги
- ❌ Ошибки могут привести к **финансовым потерям**
- ⚠️ Используйте только после **48-72 часов тестирования** в DRY_RUN=true
- ⚠️ Логи отмечены `[LIVE]` с предупреждениями

**РЕКОМЕНДАЦИЯ**: Не переключайтесь на `DRY_RUN=false` до тех пор, пока:
1. Не протестируете бота минимум 48-72 часа в безопасном режиме
2. Не убедитесь, что бот работает корректно и стабильно
3. Не изучите все логи и не поймете поведение бота
4. Не прочитаете [docs/SECURITY.md](SECURITY.md)

---

## 📋 Предварительные требования

- ✅ Python 3.11 или выше (3.12+ рекомендуется)
- ✅ pip (менеджер пакетов Python)
- ✅ Git для клонирования репозитория
- ✅ Telegram Bot Token ([получить у @BotFather](https://t.me/BotFather))
- ✅ DMarket API ключи (опционально, [получить здесь](https://dmarket.com/profile/api))
- ✅ PostgreSQL (рекомендуется для production) или SQLite для разработки

---

### Шаг 1: Клонирование репозитория

```bash
# Клонировать репозиторий
git clone https://github.com/your-username/dmarket-telegram-bot.git
cd dmarket-telegram-bot
```

---

### Шаг 2: Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv .venv

# Активировать виртуальное окружение
# На Windows:
.venv\Scripts\activate
# На Linux/macOS:
source .venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

---

### Шаг 3: Настройка окружения

1. **Создать .env файл из шаблона**:

```bash
cp .env.example .env
```

2. **Отредактировать .env с вашими ключами**:

```env
# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# DMarket API
DMARKET_PUBLIC_KEY=your_public_key_here
DMARKET_SECRET_KEY=your_secret_key_here
DMARKET_API_URL=https://api.dmarket.com

# База данных
# Для разработки используйте SQLite:
DATABASE_URL=sqlite:///bot_database.db
# Для production используйте PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/dmarket_bot

# Redis (опционально, для кэширования)
REDIS_URL=redis://localhost:6379

# Логирование
LOG_LEVEL=INFO

# ⚠️ КРИТИЧЕСКИ ВАЖНО: Режим безопасности торговли ⚠️
DRY_RUN=true
# DRY_RUN=true  - Бот НЕ будет совершать реальные сделки (безопасный режим)
# DRY_RUN=false - Бот БУДЕТ совершать реальные сделки на ваш баланс!
#
# РЕКОМЕНДАЦИЯ: Всегда начинайте с DRY_RUN=true и тестируйте бота
# минимум 48-72 часа перед переключением на реальную торговлю!

# Безопасность (опционально)
# ALLOWED_USERS=123456789,987654321
# ADMIN_USERS=123456789
```

---

### Шаг 4: Проверка конфигурации

**ВАЖНО**: Перед первым запуском проверьте конфигурацию:

```bash
# Валидировать конфигурацию
python scripts/validate_config.py
```

Этот скрипт проверит:

- ✅ Наличие всех обязательных переменных окружения
- ✅ Правильность формата API ключей и токенов
- ✅ Доступность файлов и директорий
- ✅ Правильность URL баз данных
- ✅ Корректность уровня логирования

Если все проверки пройдены, вы увидите:

```
✅ Все проверки конфигурации пройдены успешно!
```

---

### Шаг 5: Инициализация базы данных

```bash
# Инициализировать БД с помощью Alembic
python scripts/init_db.py
```

Этот скрипт:

- ✅ Создаст все необходимые таблицы в БД
- ✅ Применит все миграции Alembic
- ✅ Проверит текущую версию схемы БД

Вы увидите:

```
✅ Миграции успешно применены!
📊 Текущая версия БД: 001_initial_migration
```

---

### Шаг 6: Проверка подключений

**ВАЖНО**: Перед запуском бота проверьте подключения ко всем сервисам:

```bash
# Проверить доступность всех сервисов
python scripts/health_check.py
```

Этот скрипт проверит:

- ✅ Telegram API (доступность бота)
- ✅ DMarket API (подключение и баланс)
- ✅ База данных (подключение и схема)
- ✅ Redis (опционально, если настроен)

Пример успешного вывода:

```
🏥 Проверка состояния сервисов...

✅ Telegram API: OK
   Bot: @YourBotName

✅ DMarket API: OK
   Баланс: $100.50

✅ База данных: OK
   Версия: 001_initial_migration

✅ Redis: OK (опционально)

🎉 Все сервисы работают нормально!
```

---

### Шаг 7: Запуск бота

```bash
# Запустить бота
python -m src.main

# Или с конфигурационным файлом
python -m src.main --config config/local.yaml

# С отладкой
python -m src.main --debug

# С кастомным уровнем логирования
python -m src.main --log-level DEBUG
```

Вы увидите:

```
INFO - Initializing DMarket Telegram Bot...
INFO - Configuration loaded successfully
INFO - Database initialized
INFO - DMarket API connection established (balance: $100.50)
INFO - Bot started successfully
INFO - Press Ctrl+C to stop
```

---

## 🎯 Основные команды бота

### Для пользователей

1. `/start` - Запустить бота и увидеть главное меню
2. `/balance` - Проверить баланс DMarket
3. `/arbitrage` - Открыть меню поиска арбитражных возможностей
4. `/targets` - Открыть меню управления таргетами
5. `/help` - Получить справку

### Использование арбитража

```
/arbitrage
→ Выбрать уровень (Разгон, Стандарт, Средний, Продвинутый, Профессионал)
→ Просмотреть найденные возможности
→ Купить вручную или запустить автоторговлю
```

### Использование таргетов

```
/targets
→ Создать таргет (указать предмет и цену)
→ Просмотреть активные таргеты
→ Удалить ненужные таргеты
→ Использовать "Умные таргеты" для автоматического создания
```

---

## 🔧 Примеры использования API

### Пример 1: Поиск арбитражных возможностей

```python
from src.dmarket.dmarket_api import DMarketAPI
from src.dmarket.arbitrage import find_arbitrage_opportunities

# Инициализация API клиента
api = DMarketAPI(
    public_key="your_public_key",
    secret_key="your_secret_key"
)

# Поиск возможностей
async def search_arbitrage():
    opportunities = await find_arbitrage_opportunities(
        api_client=api,
        mode='medium',  # standard, medium, high, boost, pro
        game='csgo',
        max_items=100,
        min_profit_percent=5.0
    )

    for opp in opportunities:
        print(f"Предмет: {opp['item_name']}")
        print(f"Купить за: ${opp['buy_price']:.2f}")
        print(f"Продать за: ${opp['sell_price']:.2f}")
        print(f"Прибыль: ${opp['profit']:.2f} ({opp['profit_percent']:.1f}%)")
        print("---")

# Запуск
import asyncio
asyncio.run(search_arbitrage())
```

### Пример 2: Создание таргетов

```python
from src.dmarket.targets import TargetManager
from src.dmarket.dmarket_api import DMarketAPI

api = DMarketAPI(public_key="...", secret_key="...")
target_manager = TargetManager(api)

async def create_targets():
    # Создание одного таргета
    result = await target_manager.create_target(
        game='csgo',
        title='AK-47 | Redline (Field-Tested)',
        price=8.00,  # USD
        amount=1
    )
    print(f"Таргет создан: {result}")

    # Создание умных таргетов (автоматически)
    smart_targets = await target_manager.create_smart_targets(
        game='csgo',
        items=[
            {'title': 'AK-47 | Redline (Field-Tested)'},
            {'title': 'AWP | Asiimov (Field-Tested)'},
        ],
        price_reduction_percent=5.0  # На 5% ниже текущей цены
    )
    print(f"Создано умных таргетов: {len(smart_targets)}")

asyncio.run(create_targets())
```

### Пример 3: Автоматическая торговля

```python
from src.dmarket.auto_trader import AutoTrader
from src.dmarket.dmarket_api import DMarketAPI
from src.dmarket.targets import TargetManager
from src.dmarket.arbitrage_scanner import ArbitrageScanner

api = DMarketAPI(public_key="...", secret_key="...")
targets = TargetManager(api)
scanner = ArbitrageScanner(api)

trader = AutoTrader(
    api_client=api,
    target_manager=targets,
    scanner=scanner
)

async def start_trading():
    # Настройка лимитов
    trader.settings.max_trade_value = 50.0  # Макс $50 на сделку
    trader.settings.daily_limit = 500.0     # Макс $500 в день
    trader.settings.min_profit_percent = 5.0

    # Запуск автоторговли
    success = await trader.start(
        user_id=123456789,
        level='standard',
        game='csgo'
    )

    if success:
        print("Автоторговля запущена!")

        # Мониторинг статуса
        while trader.active:
            status = trader.get_status()
            print(f"Сделок совершено: {status['transactions_count']}")
            print(f"Общая прибыль: ${status['total_profit']:.2f}")
            await asyncio.sleep(60)  # Проверка каждую минуту

asyncio.run(start_trading())
```

---

## 📊 Структура уровней арбитража

### Уровень 1: Разгон баланса 🎯

- **Диапазон цен**: $0.50 - $3.00
- **Минимальная прибыль**: 1.5% - 3%
- **Рекомендуется для**: Начинающих, малый стартовый капитал
- **Риск**: Низкий
- **Скорость оборота**: Очень высокая

### Уровень 2: Стандарт 📈

- **Диапазон цен**: $3.00 - $10.00
- **Минимальная прибыль**: 3% - 7%
- **Рекомендуется для**: Оптимальный старт
- **Риск**: Низкий-средний
- **Скорость оборота**: Высокая

### Уровень 3: Средний 💼

- **Диапазон цен**: $10.00 - $30.00
- **Минимальная прибыль**: 5% - 10%
- **Рекомендуется для**: Опытных пользователей
- **Риск**: Средний
- **Скорость оборота**: Средняя

### Уровень 4: Продвинутый 🚀

- **Диапазон цен**: $30.00 - $100.00
- **Минимальная прибыль**: 7% - 15%
- **Рекомендуется для**: Профессионалов
- **Риск**: Средний-высокий
- **Скорость оборота**: Низкая-средняя

### Уровень 5: Профессионал 👑

- **Диапазон цен**: $100.00+
- **Минимальная прибыль**: 10%+
- **Рекомендуется для**: Экспертов с большим капиталом
- **Риск**: Высокий
- **Скорость оборота**: Низкая

---

## 🎯 Как работают таргеты (Buy Orders)

### Что это?

**Таргет** - это заявка на покупку предмета по определенной цене. Когда продавец выставляет предмет, соответствующий вашему таргету, происходит автоматическая покупка.

### Преимущества

✅ **Покупка по вашей цене** - не переплачиваете
✅ **Автоматическое исполнение** - не нужно постоянно мониторить рынок
✅ **Приоритет** - ваша заявка выполняется первой
✅ **Гибкость** - можно указать специфические атрибуты (float, паттерн)

### Пример использования

```
1. Анализируете рынок и видите:
   AK-47 | Redline (FT) - текущая цена $8.50

2. Создаете таргет на $8.00 (на 5.9% ниже)

3. Когда кто-то выставит предмет за $8.00 или ниже,
   он автоматически купится на ваш аккаунт

4. Выставляете купленный предмет на продажу за $9.00

5. Прибыль: $9.00 * 0.93 (комиссия 7%) - $8.00 = $0.37 (4.6%)
```

### Стратегии использования таргетов

#### 1. Агрессивная стратегия

- Устанавливать цену на 3-5% ниже рынка
- Высокая вероятность исполнения
- Меньшая прибыль, но больше сделок

#### 2. Консервативная стратегия

- Устанавливать цену на 8-10% ниже рынка
- Низкая вероятность исполнения
- Высокая прибыль при исполнении

#### 3. Сбалансированная стратегия

- Устанавливать цену на 5-7% ниже рынка
- Средняя вероятность исполнения
- Оптимальная прибыль

---

## 🔄 Workflow автоматической торговли

```
1. Сканирование рынка
   ↓
2. Поиск арбитражных возможностей
   ↓
3. Фильтрация по критериям (прибыль, риск, ликвидность)
   ↓
4. Проверка баланса и лимитов
   ↓
5. Покупка предмета
   ↓
6. Выставление на продажу
   ↓
7. Мониторинг продажи
   ↓
8. Повтор цикла
```

---

## ⚙️ Настройки торговли

### Через Telegram бота

```
/settings
→ Торговля
  → Максимальная сумма сделки: $50
  → Дневной лимит: $500
  → Минимальная прибыль: 5%
  → Стратегия: Сбалансированная
  → Автоперезапуск: Включен
```

### Через код

```python
trader.settings.max_trade_value = 50.0
trader.settings.daily_limit = 500.0
trader.settings.min_profit_percent = 5.0
trader.settings.strategy = 'balanced'  # 'conservative', 'balanced', 'aggressive'
trader.settings.auto_restart = True
trader.settings.stop_loss_percent = 10.0  # Стоп-лосс при падении цены на 10%
```

---

## 📈 Мониторинг и уведомления

### Типы уведомлений

1. **Новая возможность найдена**:

   ```
   🔍 Найдена возможность!
   AK-47 | Redline (FT)
   💵 Купить: $8.50
   💰 Продать: $9.20
   📈 Прибыль: $0.52 (6.1%)
   ```

2. **Сделка совершена**:

   ```
   ✅ Покупка выполнена!
   AK-47 | Redline (FT)
   💵 Цена: $8.50
   📊 Выставлено на продажу за: $9.20
   ```

3. **Таргет исполнен**:

   ```
   🎯 Таргет исполнен!
   AK-47 | Redline (FT)
   💰 Куплено за: $8.00
   📊 Ваш таргет: $8.00
   ```

4. **Ошибка**:

   ```
   ❌ Ошибка при покупке
   AK-47 | Redline (FT)
   Причина: Недостаточно средств
   ```

5. **Ежедневный отчет**:

   ```
   📊 Ежедневный отчет
   📅 12.11.2025

   💼 Сделок: 15
   💰 Прибыль: $12.50
   📈 Успешных: 13 (86.7%)
   ❌ Неудачных: 2 (13.3%)

   🎯 Таргетов исполнено: 5
   💵 Средняя прибыль: $0.83
   ```

---

## 🛡️ Безопасность и лучшие практики

### Рекомендации

1. **Начните с малого**:
   - Первые сделки на $1-5
   - Изучите поведение рынка
   - Постепенно увеличивайте лимиты

2. **Установите лимиты**:
   - Максимальная сумма сделки
   - Дневной лимит торговли
   - Стоп-лосс

3. **Диверсифицируйте**:
   - Торгуйте разными предметами
   - Используйте несколько уровней
   - Комбинируйте стратегии

4. **Мониторьте результаты**:
   - Анализируйте статистику
   - Корректируйте стратегию
   - Изучайте неудачные сделки

5. **Защитите аккаунт**:
   - Используйте 2FA
   - Не делитесь API ключами
   - Регулярно проверяйте активность

---

## ⚠️ ВАЖНО: Проблема с кириллическими символами

### Частая ошибка при копировании команд

При копировании команд из документации или GitHub Copilot можете случайно вставить **русские буквы** вместо английских:

**❌ Неправильно:**
```bash
руtеst  # русские "р", "у", "е"
рip install  # русская "р"
руthоn -m src.main  # русские "р", "у", "о"
```

**✅ Правильно:**
```bash
pytest  # все буквы английские
pip install  # все буквы английские
python -m src.main  # все буквы английские
```

### Как избежать:

1. **Переключите клавиатуру на английский** перед вставкой:
   ```
   Win + Пробел → EN
   ```

2. **Проверяйте команды перед Enter:**
   - Выделите команду (Ctrl+A)
   - Увеличьте текст (Ctrl + колесо мыши)
   - Убедитесь, что все символы английские

3. **Используйте шрифт с четким различием:**
   - Установите **Cascadia Code** или **Fira Code**
   - Русская "с" и английская "c" будут выглядеть по-разному

---

## 🆘 Решение проблем

### Проблема: "Недостаточно средств"

**Решение**:

1. Проверьте баланс: `/balance`
2. Пополните счет DMarket
3. Уменьшите лимиты торговли

### Проблема: "Rate limit exceeded"

**Решение**:

1. Подождите 1-2 минуты
2. Уменьшите частоту сканирования
3. Увеличьте интервал между запросами в настройках

### Проблема: "Таргеты не исполняются"

**Решение**:

1. Проверьте цену таргета (возможно, слишком низкая)
2. Увеличьте цену на 1-2%
3. Используйте "Умные таргеты" для автоматического расчета

### Проблема: "Автоторговля остановилась"

**Решение**:

1. Проверьте логи: `/logs`
2. Проверьте баланс
3. Проверьте лимиты
4. Перезапустите: `/arbitrage_start`

---

## 📚 Дополнительные ресурсы

- **Документация DMarket API**: `docs/DMARKET_API_FULL_SPEC.md`
- **План реализации**: `docs/IMPLEMENTATION_PLAN.md`
- **Руководство по тестированию**: `docs/testing_guide.md`
- **Руководство по развертыванию**: `docs/deployment.md`

---

## 💡 Советы для максимальной прибыли

1. **Торгуйте популярными скинами**:
   - AK-47 | Redline
   - AWP | Asiimov
   - M4A4 | Howl
   - Glock | Fade

2. **Используйте таргеты на ночь**:
   - Устанавливайте таргеты перед сном
   - Утром проверяйте исполненные заявки

3. **Следите за обновлениями игр**:
   - Новые кейсы = новые возможности
   - Популярные скины растут в цене

4. **Комбинируйте стратегии**:
   - 50% - таргеты
   - 30% - автоторговля
   - 20% - ручные сделки

5. **Анализируйте конкурентов**:
   - Смотрите активные таргеты других
   - Изучайте историю продаж
   - Находите недооцененные предметы

---

## 🔧 Устранение неполадок

### Проблема 1: "Ошибка валидации конфигурации"

**Симптомы**:
```text
❌ TELEGRAM_BOT_TOKEN not set or empty
```

**Решение**:
1. Убедитесь, что файл `.env` существует в корне проекта
2. Проверьте, что все обязательные переменные установлены
3. Убедитесь, что переменные не содержат лишних пробелов
4. Перезапустите терминал после изменения `.env`

```bash
# Проверить содержимое .env (Linux/macOS)
cat .env

# Проверить содержимое .env (Windows)
type .env

# Проверить переменные окружения
python -c "import os; print(os.getenv('TELEGRAM_BOT_TOKEN'))"
```

---

### Проблема 2: "Ошибка подключения к базе данных"

**Симптомы**:
```text
❌ Database connection failed
```

**Решение для SQLite**:
```bash
# Проверить путь к БД
python -c "from src.utils.config import Config; print(Config.load().database.url)"

# Создать директорию для БД, если не существует
mkdir -p data

# Повторно инициализировать БД
python scripts/init_db.py
```

**Решение для PostgreSQL**:
```bash
# Проверить доступность PostgreSQL
psql -h localhost -U your_user -l

# Создать базу данных
createdb dmarket_bot

# Проверить строку подключения
python -c "from src.utils.config import Config; print(Config.load().database.url)"
```

---

### Проблема 3: "DMarket API ошибка 401 Unauthorized"

**Симптомы**:
```text
❌ DMarket API: Authentication failed (401)
```

**Решение**:
1. Проверьте правильность API ключей:
   ```bash
   python scripts/validate_config.py
   ```

2. Убедитесь, что ключи скопированы полностью (без пробелов)

3. Проверьте срок действия ключей на [DMarket](https://dmarket.com/profile/api)

4. Сгенерируйте новые ключи при необходимости

---

### Проблема 4: "Telegram API ошибка: Invalid token"

**Симптомы**:
```text
❌ Telegram API: Invalid bot token
```

**Решение**:
1. Получите новый токен от [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен

2. Обновите `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=123456789:ABCDefGHIjklMNOpqrsTUVwxyz
   ```

3. Перезапустите бота

---

### Проблема 5: "Миграции Alembic не применяются"

**Симптомы**:
```text
❌ Alembic migration failed
```

**Решение**:
```bash
# Проверить текущую версию БД
alembic current

# Показать историю миграций
alembic history

# Откатить к предыдущей версии
alembic downgrade -1

# Применить все миграции
alembic upgrade head

# В случае конфликта, откатить полностью и применить заново
alembic downgrade base
alembic upgrade head
```

---

### Проблема 6: "Rate limit exceeded"

**Симптомы**:
```text
❌ DMarket API: Rate limit exceeded (429)
```

**Решение**:
- DMarket ограничивает ~30 запросов в минуту
- Подождите минуту перед повторной попыткой
- Проверьте настройки rate limiting в коде:
  ```python
  # src/utils/rate_limiter.py
  max_calls=30,
  time_period=60  # seconds
  ```

---

### Проблема 7: "Импорты не найдены"

**Симптомы**:
```text
ModuleNotFoundError: No module named 'src'
```

**Решение**:
```bash
# Убедитесь, что виртуальное окружение активировано
which python  # Linux/macOS
where python  # Windows

# Переустановите зависимости
pip install -r requirements.txt

# Установите проект в режиме разработки
pip install -e .
```

---

### Проблема 8: "Логи не создаются"

**Симптомы**:
- Файлы логов отсутствуют в папке `logs/`

**Решение**:
```bash
# Создать директорию для логов
mkdir -p logs

# Проверить права доступа (Linux/macOS)
chmod 755 logs

# Проверить настройки логирования
python -c "from src.utils.config import Config; print(Config.load().logging)"
```

---

### Проблема 9: "Тесты не проходят"

**Симптомы**:
```text
FAILED tests/test_main.py::test_initialize_success
```

**Решение**:
```bash
# Обновить зависимости для тестирования
pip install pytest pytest-asyncio pytest-cov

# Запустить тесты с подробным выводом
pytest -v

# Запустить конкретный тест
pytest tests/test_main.py::test_initialize_success -v

# Проверить покрытие
pytest --cov=src --cov-report=html
```

---

### Проблема 10: "Docker контейнер не запускается"

**Симптомы**:
```text
Error: Cannot start container
```

**Решение**:
```bash
# Проверить логи контейнера
docker-compose logs bot

# Пересобрать образ
docker-compose build --no-cache

# Проверить docker-compose.yml
docker-compose config

# Запустить в режиме debug
docker-compose up
```

---

## 📚 Дополнительные ресурсы

- **Полная документация API DMarket**: [docs/DMARKET_API_FULL_SPEC.md](DMARKET_API_FULL_SPEC.md)
- **Руководство по многоуровневому арбитражу**: [docs/MULTI_LEVEL_ARBITRAGE_GUIDE.md](MULTI_LEVEL_ARBITRAGE_GUIDE.md)
- **Руководство по тестированию**: [docs/testing_guide.md](testing_guide.md)
- **Руководство по развертыванию**: [docs/deployment.md](deployment.md)
- **Архитектура проекта**: [docs/ARCHITECTURE.md](ARCHITECTURE.md)
- **Безопасность**: [docs/SECURITY.md](SECURITY.md)

---

## 💡 Полезные команды

### Разработка

```bash
# Форматировать код
ruff format src/ tests/

# Проверить код
ruff check src/ tests/

# Проверить типы
mypy src/

# Запустить все проверки качества
ruff check src/ tests/ && mypy src/ && pytest
```

### База данных

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Описание изменений"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1

# Посмотреть статус
alembic current
```

### Docker

```bash
# Собрать образ
docker-compose build

# Запустить сервисы
docker-compose up -d

# Остановить сервисы
docker-compose down

# Просмотреть логи
docker-compose logs -f bot
```

---

## 🎓 Следующие шаги

После успешного запуска бота:

1. **Изучите документацию**:
   - [Руководство по многоуровневому арбитражу](MULTI_LEVEL_ARBITRAGE_GUIDE.md)
   - [Руководство по фильтрам игр](game_filters_guide.md)
   - [Анализ продаж](sales_analysis_guide.md)

2. **Настройте уведомления**:
   - Используйте команду `/notify` в боте
   - Настройте алерты на интересующие предметы

3. **Экспериментируйте с уровнями арбитража**:
   - Начните с уровня "Разгон" для малого капитала
   - Постепенно переходите на более высокие уровни

4. **Мониторьте производительность**:
   - Просматривайте логи регулярно
   - Анализируйте статистику сделок
   - Оптимизируйте стратегии

5. **Присоединяйтесь к сообществу**:
   - Создавайте Issues для багов
   - Предлагайте улучшения в Discussions
   - Помогайте другим пользователям

---

## ✅ Чеклист перед запуском

- [ ] Python 3.11+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан и заполнен
- [ ] Конфигурация проверена (`python scripts/validate_config.py`)
- [ ] База данных инициализирована (`python scripts/init_db.py`)
- [ ] Health check пройден (`python scripts/health_check.py`)
- [ ] Telegram Bot Token получен от @BotFather
- [ ] DMarket API ключи получены и добавлены
- [ ] Логи директория создана и доступна для записи
- [ ] Тесты проходят (`pytest`)

После выполнения всех пунктов вы готовы к запуску бота! 🚀

---

## 🆘 Получение помощи

Если у вас возникли проблемы, которые не описаны в этом руководстве:

1. **Проверьте Issues на GitHub**: Возможно, проблема уже обсуждалась
2. **Создайте новый Issue**: Подробно опишите проблему и шаги воспроизведения
3. **Обратитесь в Discussions**: Задайте вопрос сообществу
4. **Проверьте логи**: Файлы в папке `logs/` могут содержать полезную информацию

**Формат сообщения об ошибке**:
```markdown
**Описание проблемы**: Краткое описание

**Шаги воспроизведения**:
1. Шаг 1
2. Шаг 2
3. Шаг 3

**Ожидаемое поведение**: Что должно было произойти

**Фактическое поведение**: Что произошло на самом деле

**Окружение**:
- ОС: Windows 10 / Ubuntu 22.04 / macOS 13
- Python версия: 3.11.5
- Версия бота: 2.0

**Логи** (если есть):
```
<paste logs here>
```
```

---

**Удачной торговли! 🚀💰**

При возникновении вопросов обращайтесь в поддержку или создавайте Issue на GitHub.
