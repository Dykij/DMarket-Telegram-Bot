# Руководство по автоматической остановке бота

**Версия**: 1.0
**Дата**: 23 ноября 2025 г.

---

## 📋 Обзор

Система автоматической остановки предназначена для защиты от каскадных сбоев и потери денег при многократных ошибках подряд. Бот автоматически останавливается после **5 последовательных ошибок** и отправляет критическое уведомление администраторам.

---

## 🔧 Как это работает

### 1. Отслеживание ошибок

`StateManager` отслеживает количество последовательных ошибок:

```python
# При возникновении ошибки
if state_manager.record_error():
    # Превышен лимит (5 ошибок) - остановка бота
    await state_manager.trigger_emergency_shutdown("5+ consecutive errors")
```

### 2. Сброс счётчика

При успешной операции счётчик автоматически сбрасывается:

```python
# После успешной операции
state_manager.reset_error_counter()
```

### 3. Критическое уведомление

При остановке администраторы получают уведомление:

```
🚨 КРИТИЧЕСКАЯ ОСТАНОВКА БОТА

⚠️ Причина: 5+ consecutive errors
📊 Последовательных ошибок: 5

🔴 Все операции ОСТАНОВЛЕНЫ
🛠️ Необходима проверка логов

Для возобновления используйте команду:
/resume
```

---

## 🎯 Использование команды /resume

### Команда

```
/resume
```

### Доступ

- ✅ Только для **администраторов** (из `config.security.admin_users`)
- ❌ Обычные пользователи получат отказ

### Что происходит при /resume

1. Проверяется, что бот действительно на паузе
2. Сбрасывается счётчик `consecutive_errors` до 0
3. Снимается флаг `is_paused`
4. Отправляется подтверждение:

```
✅ Работа бота возобновлена!

📊 Сброшено 5 последовательных ошибок
🔄 Операции восстановлены

⚠️ Внимательно следите за логами и уведомлениями.
```

---

## ⚙️ Конфигурация

### StateManager параметры

```python
state_manager = StateManager(
    session=db_session,
    max_consecutive_errors=5,  # Лимит ошибок (по умолчанию: 5)
)
```

### Настройка администраторов

В `.env`:

```env
# Список администраторов (через запятую)
ADMIN_USERS=123456789,987654321
```

Или в `config.yaml`:

```yaml
security:
  admin_users:
    - 123456789
    - 987654321
```

---

## 📊 Мониторинг

### Проверка статуса

```python
# Проверить, на паузе ли бот
if state_manager.is_paused:
    logger.warning("Bot is PAUSED")

# Получить количество ошибок
errors = state_manager.consecutive_errors
logger.info(f"Consecutive errors: {errors}/5")
```

### Логи

Все события логируются:

```
WARNING - Consecutive error recorded: 1/5
WARNING - Consecutive error recorded: 2/5
WARNING - Consecutive error recorded: 3/5
WARNING - Consecutive error recorded: 4/5
CRITICAL - Maximum consecutive errors (5) reached! Triggering shutdown...
CRITICAL - EMERGENCY SHUTDOWN: 5+ consecutive errors
WARNING - Bot operations PAUSED
```

---

## 🔍 Типичные сценарии

### Сценарий 1: Временные сбои API

```
1. DMarket API недоступен → ошибка (1/5)
2. Повторная попытка → ошибка (2/5)
3. Еще попытка → ошибка (3/5)
4. API восстановился → успех ✅
5. Счётчик сброшен → 0/5
```

### Сценарий 2: Критический сбой

```
1. Ошибка подключения к БД → (1/5)
2. Rate limit DMarket → (2/5)
3. Ошибка парсинга → (3/5)
4. Timeout → (4/5)
5. Ошибка валидации → (5/5)
6. 🚨 ОСТАНОВКА БОТА
7. Уведомление админам
8. Проверка логов → исправление проблемы
9. /resume → возобновление работы
```

### Сценарий 3: Ложная тревога

```
1. Случайные ошибки сети → (1/5, 2/5, 3/5)
2. Успешная операция → сброс на 0/5
3. Бот продолжает работать ✅
```

---

## 🛡️ Лучшие практики

### 1. Настройте администраторов

Убедитесь, что в конфигурации указаны правильные Telegram ID администраторов:

```bash
# Узнать свой ID
# 1. Отправить /start боту @userinfobot
# 2. Скопировать ID из ответа
```

### 2. Мониторьте уведомления

- Включите уведомления от бота на телефоне
- Настройте звук для критических уведомлений
- Проверяйте уведомления хотя бы раз в день

### 3. Изучайте логи перед /resume

**ВСЕГДА** проверяйте логи перед возобновлением:

```bash
# Просмотр последних 50 строк
tail -n 50 logs/dmarket_bot.log

# Поиск критических ошибок
grep "CRITICAL" logs/dmarket_bot.log
grep "ERROR" logs/dmarket_bot.log | tail -20
```

### 4. Не злоупотребляйте /resume

❌ **Неправильно**: Сразу жать /resume без анализа
✅ **Правильно**:

1. Проверить логи
2. Понять причину остановки
3. Исправить проблему (если нужно)
4. Только потом /resume

---

## 🔧 Интеграция в код

### Пример использования в сканере

```python
from src.utils.state_manager import StateManager

async def scan_market(state_manager: StateManager):
    # Проверить, не на паузе ли бот
    if state_manager.is_paused:
        logger.warning("Operations are paused, skipping scan")
        return

    try:
        # Выполнить сканирование
        results = await dmarket_api.get_market_items()

        # При успехе - сбросить счётчик
        state_manager.reset_error_counter()

        return results

    except Exception as e:
        logger.exception("Market scan failed")

        # Записать ошибку
        if state_manager.record_error():
            # Лимит превышен - критическая остановка
            await state_manager.trigger_emergency_shutdown(
                f"Market scan error: {str(e)}"
            )

        raise
```

---

## 📞 Поддержка

При проблемах:

1. Проверьте логи: `logs/dmarket_bot.log`
2. Создайте Issue на GitHub с логами
3. Опишите шаги воспроизведения

---

## 📚 Связанные документы

- [SECURITY.md](SECURITY.md) - Безопасность
- [QUICK_START.md](QUICK_START.md) - Быстрый старт
- [PROJECT_ROADMAP.md](../PROJECT_ROADMAP.md) - План развития

---

**Версия документа**: 1.0
**Последнее обновление**: 23 ноября 2025 г.
