{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Claude Desktop MCP configuration for DMarket Telegram Bot development",
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Library documentation (60+ libs). Usage: 'use library /encode/httpx for API and docs.'",
      "env": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
      }
    },
    "sqlite": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-sqlite", "--db", "data/bot.db"],
      "description": "Database queries via natural language. Usage: 'Покажи последние 5 сделок'"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-github"],
      "description": "GitHub Issues, PRs, branches integration. Usage: 'Найди Issue про rate limiting'",
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-fetch"],
      "description": "Web fetch for live API documentation. Usage: 'Получи документацию DMarket API'"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-sequential-thinking"],
      "description": "Enhanced reasoning for complex tasks. Auto-activated for complex algorithms."
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-playwright"],
      "description": "Web automation and E2E testing. Usage: 'Открой DMarket и найди цену AWP Dragon Lore'"
    },
    "skillsmp": {
      "command": "npx",
      "args": ["-y", "skillsmp-mcp-server"],
      "description": "AI Skills Marketplace. Usage: 'Найди skills для арбитража', 'Установи trading skill'",
      "env": {
        "SKILLSMP_API_KEY": "${SKILLSMP_API_KEY}"
      }
    }
  },
  "defaultTools": [
    "context7",
    "sqlite",
    "fetch",
    "skillsmp"
  ],
  "settings": {
    "autoActivateMcp": true,
    "logMcpCalls": true
  }
}
