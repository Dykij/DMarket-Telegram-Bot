# Папка .config - Устаревшие конфигурации

## ⚠️ Внимание

Эта папка содержит **устаревшие** конфигурационные файлы, которые были перемещены в корень проекта.

## 📦 Текущая структура конфигурации

Все конфигурации теперь находятся в **одном файле** в корне проекта:

```
BotDmarket-master/
├── pyproject.toml          # ✅ ВСЕ настройки здесь (Ruff, MyPy, pytest, coverage и т.д.)
└── .config/
    ├── README.md           # Этот файл
    ├── pyrightconfig.json  # Только для Pyright (IDE)
    └── linters/
        └── .editorconfig   # Настройки редактора (опционально)
```

## 🗑️ Удаленные файлы

Следующие файлы были **удалены** как дубликаты:

- ❌ `.config/pyproject.toml` → ✅ `pyproject.toml` (корень)
- ❌ `.config/pytest.ini` → ✅ `[tool.pytest.ini_options]` в `pyproject.toml`
- ❌ `.config/setup.py` → ✅ `[project]` в `pyproject.toml`
- ❌ `.config/setup.cfg` → ✅ Устарел, не используется
- ❌ `.config/linters/.ruff.toml` → ✅ `[tool.ruff]` в `pyproject.toml`
- ❌ `.config/linters/.black.toml` → ✅ `[tool.ruff.format]` в `pyproject.toml`
- ❌ `.config/linters/.pylintrc` → ✅ Не используется (есть Ruff)
- ❌ `.config/type-checkers/mypy.ini` → ✅ `[tool.mypy]` в `pyproject.toml`

## 📝 Использование конфигураций

Все инструменты автоматически читают настройки из `pyproject.toml`:

```bash
# Ruff (линтинг и форматирование)
ruff check .
ruff format .

# MyPy (проверка типов)
mypy src/

# Pytest (тестирование)
pytest

# Coverage (покрытие кода)
pytest --cov=src
```

## 🔄 Миграция завершена

Все настройки были консолидированы в соответствии с современными стандартами Python (PEP 518, 621).

Это упрощает:
- ✅ Управление конфигурациями
- ✅ Понимание структуры проекта
- ✅ Избежание конфликтов настроек
- ✅ Соответствие best practices

## 📚 Документация

См. `docs/code_quality_tools_guide.md` для подробной информации об использовании инструментов качества кода.
